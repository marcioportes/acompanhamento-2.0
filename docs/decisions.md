# Decision Log (DEC-001..090+)

> Registro de decisões arquiteturais. **Nunca remover** — marcar como SUPERSEDED se inválida.



> Registro de decisões arquiteturais significativas. **Nunca remover entradas** — marcar como `SUPERSEDED` se inválida.
> Formato: `| ID | Decisão | Issue | Sessão | Data/Hora |`

| ID | Decisão resumida | Issue | Data |
|----|-----------------|-------|------|
| DEC-001 | CSV Import usa staging collection (csvStagingTrades) — não escreve direto em trades | #23 | 07/03/2026 |
| DEC-002 | Direção de trade inferida por buyTimestamp vs sellTimestamp (Tradovate) | #23 | 07/03/2026 |
| DEC-003 | Inferência genérica de direção no CSV — campo configurável no template | #23 | 08/03/2026 |
| DEC-004 | Locale pt-BR para todas as moedas via Intl.NumberFormat | — | 08/03/2026 |
| DEC-005 | Compliance sem stop: loss→risco retroativo, win→N/A, BE→0 | — | 10/03/2026 |
| DEC-006 | Compliance sem stop — fórmula definitiva (C1-C5) | — | 10/03/2026 |
| DEC-007 | RR assumido via plan.pl (capital base), não currentPl flutuante | — | 11/03/2026 |
| DEC-008 | Navegação contextual Feedback ↔ Extrato via flag `_fromLedgerPlanId` | — | 12/03/2026 |
| DEC-009 | riskPercent usa plan.pl como denominador primário | — | 14/03/2026 |
| DEC-010 | EV esperado e EV real — leakage = 1 - (EV_real / EV_esperado) | — | 15/03/2026 |
| DEC-011 | Layout MetricsCards em 3 painéis temáticos com tooltips diagnósticos | — | 15/03/2026 |
| DEC-012 | Payoff como indicador de saúde do edge (semáforo ≥1.5/1.0/<1.0) | — | 18/03/2026 |
| DEC-013 | Operacional 5D com emotion_control herdado do emocional | #92 | 20/03/2026 |
| DEC-014 | Cross-check inter-dimensional — 5 flags iniciais | #92 | 20/03/2026 |
| DEC-015 | Randomização de alternativas via persistência Firestore (não PRNG puro) | #92 | 20/03/2026 |
| DEC-016 | Sondagem adaptativa pós-questionário (3-5 perguntas IA, transparente) | #92 | 20/03/2026 |
| DEC-017 | Scoring mensal 3 camadas: score_trades + mentor_delta + score_final | #92 | 20/03/2026 |
| DEC-018 | Mentor aplica delta (não score absoluto) no review mensal | #92 | 20/03/2026 |
| DEC-019 | Gates de progressão hardcoded, avaliação híbrida (CF + mentor confirma) | #92 | 20/03/2026 |
| DEC-020 | Regressão de stage nunca automática — alerta + decisão do mentor | #92 | 20/03/2026 |
| DEC-021 | Stage diagnosticado por IA (pattern-matching contra framework, não fórmula) | #92 | 22/03/2026 |
| DEC-022 | Marco zero tábula rasa: gates_met=0 independente de respostas | #92 | 22/03/2026 |
| DEC-023 | Assessment acionado pelo mentor, não automático | #92 | 22/03/2026 |
| DEC-024 | Parciais são campo inline `_partials` no documento — subcollection eliminada | — | 22/03/2026 |
| DEC-025 | Firestore rules read = isAuthenticated() — simplificação de isMentor()/isOwner() | — | 23/03/2026 |
| DEC-026 | saveInitialAssessment escreve onboardingStatus: 'active' direto via updateDoc | #92 | 24/03/2026 |
| DEC-027 | Onboarding UX: BaselineReport redesenhado, IncongruenceFlags rich detail, prompt framework-aligned, rename Experiência→Maturidade | #92 | 25/03/2026 |
| DEC-028 | Consolidação documental: PROJECT.md como single source of truth, issue-NNN.md por issue ativo | — | 26/03/2026 |
| DEC-029 | Marca pessoal "Marcio Portes" como guarda-chuva — não institucional | #100 | 29/03/2026 |
| DEC-030 | ~~"Modelo Portes" como nome público do framework comportamental (4D + TEF + maturidade)~~ **SUPERSEDED por DEC-086 (29/04/2026)** | #100 | 29/03/2026 |
| DEC-031 | "Espelho" como nome público da plataforma SaaS — codebase/repo/Firebase permanecem "acompanhamento-2.0" | #100 | 29/03/2026 |
| DEC-032 | "Mentoria Alpha" como nome do serviço premium individual (substitui "Tchio-Alpha" externamente) | #100 | 29/03/2026 |
| DEC-033 | "Diagnóstico Comportamental" como lead magnet #1 — assessment gratuito com IA baseado no Modelo Portes | #100 | 29/03/2026 |
| DEC-034 | Dois tiers: Espelho self-service (KPIs + diário + gates) e Mentoria Alpha (+ ciclos + assessment + SWOT + feedback) | #100 | 29/03/2026 |
| DEC-035 | SWOT dinâmico exclusivo Mentoria Alpha — analisa KPIs + diagnostica por gate/dimensão + prescreve evolução | #100 | 29/03/2026 |
| DEC-036 | KPIs alimentam nota de evolução por dimensão (gates) — visível para ambos tiers. SWOT interpreta e prescreve — exclusivo Alpha | #100 | 29/03/2026 |
| DEC-037 | Fibonaccing como motor de aquisição principal — 100h+ conteúdo gratuito, funil: Fibonacci → Diagnóstico → Espelho → Alpha | #100 | 29/03/2026 |
| DEC-038 | Rename externo via custom domain (app.marcioportes.com.br) + UI (title, logo) — sem refactoring de codebase | #100 | 29/03/2026 |
| DEC-039 | GitHub é SSOT para numeração de issues — PROJECT.md reflete o GitHub, nunca o contrário | — | 29/03/2026 |
| DEC-040 | Apenas 2 milestones: v1.1.0 Espelho Self-Service (prioridade) + v1.2.0 Mentor Cockpit. Student Experience absorvido pelo Espelho | — | 29/03/2026 |
| DEC-041 | #101 é épico Torre de Controle — agrupa todos os sub-issues do dashboard mentor. #1 (Upload Seed) fechado como não relevante | #101 | 29/03/2026 |
| DEC-042 | Torre de Controle: header redesenhado (4 KPIs operacionais), seções Ranking por Aluno + Ranking por Causa (dual view), SWOT e Stop por Motivo movidos para nova tela Performance (#103) | #101 | 29/03/2026 |
| DEC-043 | useProbing rehydrata savedQuestions do Firestore + effectiveStatus resolve status preso ai_assessed quando probing já gerado | #92 | 30/03/2026 |
| DEC-044 | INV-13: rastreabilidade obrigatória — toda modificação de código exige issue GitHub + arquivo docs/dev/issues/issue-NNN.md + branch nomeada. Template formal definido na seção 4.0 | — | 30/03/2026 |
| DEC-045 | Revisão semanal é evento persistido (collection reviews), não visualização on-the-fly. CF createWeeklyReview congela snapshot + gera SWOT + persiste. Independente do fechamento de ciclo (#72) | #102 | 02/04/2026 |
| DEC-046 | #45 (Aba Precisam de Atenção) absorvido pelo Ranking por Aluno da Torre de Controle (#101) | #45 | 02/04/2026 |
| DEC-047 | Barra de Contexto Unificado: Conta > Plano > Ciclo > Período, persistente no topo, reativa. Governa todas as views do Dashboard-Aluno. Fundação arquitetural — implementar antes de refatorar views | #3 | 03/04/2026 |
| DEC-048 | Overtrading detectado por clustering temporal (janela configurável: windowMinutes, maxTradesInWindow, cooldownMinutes), não por maxTradesPerDay fixo. Base: Barber & Odean 2000 | #113 | 03/04/2026 |
| DEC-049 | BE threshold configurável no compliance (percentual do capital base ou valor absoluto), não hardcoded | #114 | 03/04/2026 |
| DEC-050 | Desvio padrão (Coefficient of Variation) como métrica de consistência operacional. CV < 0.5 consistente, 0.5-1.0 moderado, > 1.0 errático. Alimenta Dashboard, Torre, Revisão e SWOT IA | #115 | 03/04/2026 |
| DEC-051 | Onboarding Automatizado: pipeline CSV performance + ordens → cruzamento → indicadores → Kelly Criterion → plano sugerido. Self-service aceita direto, Alpha mentor valida. Mínimo 30 trades para relevância estatística | #116 | 03/04/2026 |
| DEC-052 | Chunks mapeados no issue do GitHub (campo obrigatório). Issues concretos mapeados em batch, épicos mapeados na decomposição em sub-issues. Modo leitura não requer lock, modo escrita requer lock exclusivo | #117 | 03/04/2026 |
| DEC-053 | Revisão de escopo #52 (Prop Firms): regras Apex março 2026 incorporadas — campos removidos (maeRule, maxRR), campos adicionados (dailyLossAction, evalTimeLimit, bracketOrderRequired, dcaAllowed, restrictedInstruments, qualifyingDays). Templates agora diferenciam Apex EOD vs Intraday como produtos separados | #52 | 03/04/2026 |
| DEC-054 | Feedback semântico (#31) em 2 fases: Fase 1 rule-based (custo zero, dados existentes), Fase 2 Gemini Flash (incluso no Google Workspace, mesmo ecossistema GCP/Firebase). Claude API descartado por custo recorrente | #31 | 03/04/2026 |
| DEC-055 | Subscriptions como subcollection de students (`students/{id}/subscriptions`), não collection raiz. Assinatura é entidade dependente — nunca existe sem aluno. Mentor queries via `collectionGroup('subscriptions')` | #94 | 04/04/2026 |
| DEC-056 | Campo `type: trial/paid` + `trialEndsAt` na subscription + `accessTier` no student. Separa leads (trial) de convertidos (paid). Trial sem cobrança, CF expira automaticamente. `accessTier` derivado da subscription ativa, sincronizado pela CF `checkSubscriptions` | #94 | 04/04/2026 |
| DEC-057 | Campo `whatsappNumber` como propriedade do documento `students/{id}`, não subcollection de contatos. WhatsApp é atributo direto do aluno, acesso em leitura única, sem necessidade de query adicional. Subcollection seria over-engineering para um único campo string | #123 | 05/04/2026 |
| DEC-058 | `formatDateBR` usa `getUTCDate/getUTCMonth/getUTCFullYear` em vez de `toLocaleDateString('pt-BR')`. Datas ISO midnight (ex: `2026-05-01T00:00:00Z`) em fuso BR (UTC-3) convertem para dia anterior via `toLocaleDateString`. Teste de regressão em `renewalForecast.test.js` pegou o bug antes da UI | #122 | 05/04/2026 |
| DEC-059 | `RenewalForecast` implementado como componente collapsible (colapsado por default) na `SubscriptionsPage`, não como bloco fixo. Projeção de caixa é consulta ocasional do mentor, não informação de primeira camada. Preserva espaço vertical para lista de subscriptions | #122 | 05/04/2026 |
| DEC-060 | **Plano de ataque prop firm — 5 perfis determinísticos instrument-aware** (CONS_A 10% DD, CONS_B 15% ★, CONS_C 20%, AGRES_A 25%, AGRES_B 30%). Lógica invertida: mais risco = menos trades (conservadores 2/dia, agressivos 1/dia). RR fixo 1:2. `roUSD = drawdownMax × roPct`, `stopPoints = roUSD / instrument.pointValue`. Viabilidade por 3 critérios + sugestão micro. Substitui modelo binário conservador/agressivo — `normalizeAttackProfile()` compat legado. **Fundamento probabilístico** (recuperado 25/04/2026): 200k iterações Monte Carlo em Apex EOD 25K, sweet spot CONS_B WR50% → 91.2% aprovação / 3.4% bust / 9.1 dias médios; death line WR<33.3% (breakeven RR 1:2); tabela de stop por estilo operacional (scalp 6-21% / day 21-33% / swing 33-50% / convicção 50-70% do range NY). Ver `docs/dev/research/spec-original-attack-plan-2026-04-07.md` (tabela determinística v2.0 com simulação completa), `docs/dev/research/spec-original-ai-prompt-v1.0-2026-04-07.md` (prompt original IA planejadora — v1.1 em produção é regressão) e `docs/dev/research/conversa-opus-attack-plan-2026-04.md` (sessão Opus 05-08/04 com derivação combinatória + tabela seção 7 de stop por estilo) | #52 | 07/04/2026 |
| DEC-061 | **Restrição de sessão NY** — stops abaixo de `NY_MIN_VIABLE_STOP_PCT = 12.5%` do range NY não viáveis na sessão NY, mas viáveis em Ásia/London. Flag `sessionRestricted` + `recommendedSessions`. Threshold 12.5% genérico; calibração com ATR real v2: NQ NY range 329.4 pts → 12.5% = ~41 pts mínimo | #52 | 08/04/2026 |
| DEC-062 | **Engine prop firm duplicado (Opção A)** — `src/utils/propFirmDrawdownEngine.js` (ESM, testado 58 testes) e `functions/propFirmEngine.js` (CommonJS para CFs) são cópias manuais. Header de aviso obrigatório. DT-034 registra unificação futura via build step ou monorepo workspace | #52 | 09/04/2026 |
| DEC-063 | **Order Import cria trades automaticamente** após staging review — airlock = tela de seleção do aluno, criação é consequência da confirmação. GhostOperationsPanel (botão manual) descartado | #93 | 10/04/2026 |
| DEC-064 | **Confronto Enriquecido via updateDoc** com `_enrichmentSnapshot` inline — preserva campos comportamentais (emoção, setup, feedback), sobrescreve snapshot anterior (sem histórico infinito). DELETE+CREATE descartado | #93 | 10/04/2026 |
| DEC-065 | **Categorização de ops em 3 grupos**: toCreate (0 correlações) / toConfront (1 trade) / ambiguous (2+ trades). Lookup por `_rowIndex` — sem fallback por instrumento que causa falsos positivos. Ops mistas nunca caem em limbo | #93 | 10/04/2026 |
| DEC-066 | **Throttling de criação em batch**: ≤20 → Promise.allSettled paralelo; >20 → for/await sequencial com progresso dinâmico ("Criando trade N de M...") | #93 | 10/04/2026 |
| DEC-067 | **Badges "Importado" + "Complemento pendente"** em 4 componentes do diário. "Importado" (blue, permanente) = `source === 'order_import'`. "Pendente" (amber, transitório) = `!(emotionEntry\|\|emotion) \|\| !setup`. emotionExit não entra no critério | #93 | 10/04/2026 |
| DEC-068 | **Renomear `masterRules` → `fundedDrawdown`** no schema do template. Nomenclatura Ylos usa "Funded", não "Master". Campo `fundedDrawdown` é drawdown ativo quando `phase === 'SIM_FUNDED' \|\| 'LIVE'`; ausente (Apex) → cai em `template.drawdown` | #136 | 11/04/2026 |
| DEC-069 | **Plano é mecânica, não estatística.** `periodStop = maxTrades × RO`, `periodGoal = maxTrades × RO × RR`. Day RR === per-trade RR por construção. `dailyTarget` (EV profitTarget÷evalDays) é contexto de acumulação, NUNCA meta do plano | #136 | 11/04/2026 |
| DEC-070 | **Daily loss mesa no resumo do plano é condicional** — só aparece quando `suggestedPlan.dailyLossLimit > 0`. Contas Ylos Challenge (null) não mostram linha | #136 | 11/04/2026 |
| DEC-071 | **Engine phase-aware.** `calculateDrawdownState` aceita arg `phase`, resolve `activeDrawdown = getActiveDrawdown(template, phase)`. EVAL → `template.drawdown`, SIM_FUNDED/LIVE → `template.fundedDrawdown ?? template.drawdown`. Back-compat Apex (sem fundedDrawdown) | #136 | 12/04/2026 |
| DEC-072 | ~~**`riskPerOperation = periodStopPct`** (teto diário por trade), não `roPerTrade/pl` (sizing mínimo de 1 contrato). Permite Path A (N trades × 1 contrato) e Path B (1 trade × N contratos) sem flag compliance~~ — **superseded by DEC-AUTO-201-06** (#201, 26/04/2026): após sizing dinâmico, RO/trade = `roEffective` real (já considera contracts × stop × pointValue), não mais o teto diário. Path A/B virou escolha de profile (cadência), não bypass de sizing | #136 | 12/04/2026 |
| DEC-073 | **Preview attack plan em 3 blocos**: (1) Constraints da mesa, (2) Mecânica do plano com stop/meta operacional + caminhos de execução, (3) Ritmo de acumulação rotulado como contexto | #136 | 12/04/2026 |
| DEC-074 | **Shadow Behavior em 3 camadas de resolução** (LOW/MEDIUM/HIGH). Camada 1 (todos os trades, parciais + contexto inter-trade) sempre ativa — shadow nunca fica vazio. Camada 2 (orders brutas) enriquece quando disponíveis. Trades manuais recebem análise LOW; trades importados recebem HIGH | #129 | 13/04/2026 |
| DEC-075 | **Guard `onTradeUpdated:1033` já cobre `shadowBehavior`** — early return automático quando só `shadowBehavior` muda (resultChanged/planChanged/complianceChanged todos false). Zero edição na CF para o guard | #129 | 13/04/2026 |
| DEC-076 | **`ShadowBehaviorPanel` em `src/components/Trades/`** (não OrderImport) — domínio de trades, consumido por TradeDetailModal e FeedbackPage | #129 | 13/04/2026 |
| DEC-077 | **Engine shadow puro espelhado em `functions/analyzeShadowBehavior.js`** — mesmo padrão DT-034 do propFirmEngine. Header de aviso obrigatório nos dois arquivos | #129 | 13/04/2026 |
| DEC-078 | **DIRECTION_FLIP** (14º padrão, Layer 1, janela 120min) — virada de mão no mesmo instrumento após loss. Mapeamento: CONFUSION. Adicionado em validação real após algoritmo retornar vazio para 2 losses opostas | #129 | 14/04/2026 |
| DEC-079 | **UNDERSIZED_TRADE** (15º padrão, Layer 1) — risco real <50% do RO planejado. Mapeamento: AVOIDANCE. Caller enriquece trade com `planRoPct`. Detecta disfunção financeira: subdimensionar silenciosamente em vez de renegociar o plano | #129 | 14/04/2026 |
| DEC-080 | **StudentContextProvider instanciado DENTRO do StudentDashboard.jsx** (não em App.jsx). Mantém refactor atômico contido. Delta para App.jsx fica como follow-up quando outros consumidores (fora do StudentDashboard) precisarem do contexto | #118 | 15/04/2026 |
| DEC-081 | **Sincronização bidirecional `filters.accountId ↔ ctx.accountId` via useEffect** — contexto é fonte de verdade para conta; `filters` multi-campo local (period/ticker/setup/emotion/etc.) preserva estrutura original sem ripple nos consumidores prop-drilled | #118 | 15/04/2026 |
| DEC-082 | **Adaptador temporário `selectedPropAccountId` para #134** — CHUNK-17 liberado após merge #133 (15/04/2026 tarde). Derivation mantida no commit de #118; migração dos componentes PROP (PropAccountCard, PropAlertsBanner, PropPayoutTracker) + hooks (useDrawdownHistory, useMovements) para consumir contexto direto fica em sessão subsequente | #118 | 15/04/2026 |
| DEC-083 | **cycleKey canônico:** "YYYY-MM" (Mensal) ou "YYYY-Qn" (Trimestral). Formato determinístico, parseável, ordenável por string DESC. Evita Dates com timezones em localStorage | #118 | 15/04/2026 |
| DEC-084 | **`alunoDoneIds` separado de `item.done`** — checkbox do aluno no card Pendências do dashboard (mutação via `arrayUnion`/`arrayRemove` em status=CLOSED, rule granular via `affectedKeys().hasOnly(['alunoDoneIds'])`) NÃO encerra o takeaway oficialmente. Semântica dual: mentor encerra (`item.done=true`, emerald ✓) vs aluno executa (`alunoDoneIds.includes(id)`, amber ✓). No TakeawayItem da WeeklyReviewPage, mentor vê badge "aluno ✓" como sinal de accountability sem confundir com deliberação | #102 | 20/04/2026 |
| DEC-085 | **Carry-over de takeaways `!done` entre revisões do mesmo plano** — ao criar novo DRAFT via `createWeeklyReview`, hook cliente busca última CLOSED/ARCHIVED do mesmo plano (em memória, sem índice composto novo), replica items não-encerrados pelo mentor com ids novos + campo `carriedOverFromReviewId` para rastreabilidade (badge `↻ anterior` sky no TakeawayItem). Revisão anterior permanece congelada. Implementação client-side (não CF) porque: (a) evita redeploy de functions; (b) cliente já tem permissão write em DRAFT; (c) best-effort — falha em getDocs/updateDoc não aborta criação da revisão, só loga warn | #102 | 20/04/2026 |
| DEC-086 | **REVOGAÇÃO de DEC-030 — "Modelo Portes" sai do uso público.** Termo não é usado no site `marcioportes.com.br`, na UI do Espelho ou em material de mentoria. Substituído por descrição neutra: "framework comportamental de 4 dimensões". Razão: o nome próprio empurrava a identidade para postura "fundador-distante", incompatível com o posicionamento alvo "mentor próximo + rigor estatístico" (revisao.txt 28/04/2026). Termo permanece em docs internos (research, specs técnicos como `docs/maturity-engine-spec.md`, `docs/dev/research/*`). Decisão correspondente no portal: DEC-P02-REV em `marcioportes_portal_v3_0.md`. Auditoria: nenhuma ocorrência em `src/`, `public/` ou `functions/` — UI limpa | — | 29/04/2026 |
| DEC-AUTO-183-01 | **REMAP (não DELETE) dos planos órfãos do #183.** Intenção original era delete puro (evita heurística: mentor pode ter plano em conta própria/teste). Pivot durante validação em `localhost:5184` ao descobrir que planos legados podem ter trades reais vinculados. Script `scripts/issue-183-repair-orphan-plans.mjs` atualiza `plan.studentId/Email/Name` via `account.studentId` com cascade em `trades` (filtram por `studentId`/`studentEmail`). Safety nets com skip registrado: plano sem `accountId`, account inexistente, account sem `studentId`, account também do mentor. Backup dos valores antigos em `_repairedByIssue183PreviousStudentId/Email` + timestamp `_repairedByIssue183At` no doc do plano | #183 | 24/04/2026 |
| DEC-AUTO-183-02 | **Critério de órfão = `plan.studentEmail == 'marcio.portes@me.com'`** (`MENTOR_EMAIL` em `src/firebase.js:30`). Não há campo `role` no Firestore; derivação do papel vive nas rules (`firestore.rules:22`) e no frontend (`AuthContext`). Filtro trivial, resiliente a mudanças de UID | #183 | 24/04/2026 |
| DEC-AUTO-191-01 | **Janela do gate `compliance-100` (Metódico→Profissional) = união dos ciclos ativos do trader, todos os planos.** Maturidade é do trader, não de plano específico — não faz sentido avaliar aderência por plano isoladamente. Para cada plano, ciclo ativo é derivado por `adjustmentCycle` (Mensal/Trimestral/Semestral/Anual). Mínimo 20 trades CLOSED na união; se não atinge, retrocede simultaneamente 1 ciclo em CADA plano e recoleta. Repete até bater 20 ou esgotar (iteração que não acrescenta nada; cap defensivo `MAX_LOOKBACK_CYCLES=36`). Substitui o alias `complianceRate100 = complianceRate` (cálculo da janela total — semanticamente errado para um gate chamado "compliance-100") | #191 | 25/04/2026 |
| DEC-AUTO-191-02 | **Estado "insuficiente" do `complianceRate100` = `null`** (não flag adicional). Mapeia automaticamente para `met: null`/`reason: 'METRIC_UNAVAILABLE'` em `evaluateGates.js`: gate fica pendente — **não promove** (`gatesMet < gatesTotal` bloqueia `proposeStageTransition`) e **não rebaixa** (`detectRegressionSignal` não consome este campo, DEC-020 preservada). Alternativa descartada: flag `complianceRate100Sufficient: false` separada — desnecessária, `null` já carrega a semântica. Trader sem histórico suficiente segue no stage atual sem bloqueio até atingir o mínimo | #191 | 25/04/2026 |
| DEC-AUTO-188-01 | **Schema do lock comportamental — 5 campos inline no doc trade + array append-only `_mentorEdits`** (não map, não subcollection). Inline porque fluxo de leitura sempre carrega o trade completo (FeedbackPage, ExtractTable, TradeDetailModal) — subcollection criaria N reads extras. Array append-only preserva auditoria de cada edição (uma entry por mudança de campo). `_studentOriginal` é gravado APENAS na 1ª edit do mentor e fica IMUTÁVEL após — preserva o que o aluno declarou originalmente. Alternativas descartadas: subcollection `trades/{id}/edits` (overhead read), map indexado por timestamp (perde ordem semântica), regravar `_studentOriginal` a cada edit (perde a fonte original) | #188 | 25/04/2026 |
| DEC-AUTO-188-02 | **Escopo do lock limitado a 3 campos comportamentais** — `emotionEntry`, `emotionExit`, `setup`. Campos factuais (entry/exit/qty/result/stopLoss/side) seguem fluxo normal sem lock. Justificativa: a finalidade pedagógica do lock é registrar a leitura corrigida do mentor sobre o estado psicológico do aluno e o setup escolhido — coisas subjetivas. Dados objetivos (preços, quantidades, resultado) não dependem de interpretação e devem permanecer atualizáveis pelo aluno e pelo broker. Whitelist explícita em `tradeGateway.MENTOR_EDITABLE_FIELDS` rejeita qualquer outro campo | #188 | 25/04/2026 |
| DEC-AUTO-188-03 | **Import (CSV/Order) destrava lock server-side via CF preservando auditoria; broker é fonte de verdade superior ao mentor.** CF `onTradeUpdated` detecta `before.importBatchId !== after.importBatchId && after.importBatchId != null && after._lockedByMentor === true` e grava `_lockedByMentor=false` + `_unlockedAt` + `_unlockedBy.{uid:'system', email:'system', reason:'import:<batchId>'}` via admin SDK (bypassa rules). `_mentorEdits[]` e `_studentOriginal` permanecem intactos para auditoria. Lógica server-side (não no gateway client) porque firestore.rules bloqueiam aluno de tocar metadata de lock — admin SDK é o único caminho legítimo | #188 | 25/04/2026 |
| DEC-AUTO-188-04 | **Admin destrava lock manualmente sem UI dedicada v1.** Casos extremos (mentor errou edição, dispute aluno-mentor): admin edita campo `_lockedByMentor=false` direto no Firestore console + grava `_unlockedBy.reason: 'admin:<reason>'`. Função `unlockTradeByMentor(tradeId, ctx, deps)` existe no gateway para uso futuro mas não tem UI. Justificativa: caso de uso raro, UI dedicada v1 seria over-engineering. Pode virar issue separado se frequência justificar | #188 | 25/04/2026 |
| DEC-AUTO-188-05 | **Agregação multi-moeda no MentorDashboard via stack vertical; FX conversion fora de escopo v1.** `aggregateTradesByCurrency(trades)` retorna `Map<currency, {totalPL, count, currency}>` sem somar cross-currency. `MultiCurrencyAmount.jsx` renderiza single-currency com cor semântica ou stack vertical (uma linha por moeda) com fundo neutro slate. P&L Total Turma, ranking, lista alunos, detalhe aluno aplicam essa regra. FX conversion descartada porque (a) requer fonte de cotação confiável + decisão sobre data de referência, (b) introduz erro de tradução em métricas que devem ser exatas, (c) mentor lê melhor "$5k USD + R$2k BRL" do que "R$28k convertidos" para análise comportamental. Pode virar feature toggle futura | #188 | 25/04/2026 |
| DEC-AUTO-188-06 | **ContextBar é Source of Truth única de janela temporal no StudentDashboard; `filters.period` legado removido sem override vitalício.** TODOS os cards/calendários/charts respeitam `periodRange` da ContextBar (incluindo MaxDD, EquityCurve, ConsistencyCV — historicamente "vitalícios"). Decisão por ordem direta do Marcio em caps: "TODAS AS SEÇÕES DO DASHBOARD DEVEM OBEDECER O FILTRO DA BARRA DE CONTEXTO". `useDashboardMetrics` aceita `context{accountId, planId, cycleKey, periodRange}` e filtra `filteredTrades` por janela (end inclusivo 23:59:59). `Filters.jsx` perdeu dropdown "Período" (grid 6→5). Alternativa descartada: override "vitalício" para MaxDD/EquityCurve — rejeitada como exceção que confunde mais do que ajuda | #188 | 25/04/2026 |
| DEC-AUTO-188-07 | **`onTradeUpdated.complianceFields` passa a incluir `emotionEntry`** — fix de bug pré-existente entra junto com o Sev1. Antes: array `['stopLoss','entry','exit','qty','side']` não detectava mudança de emoção, deixando `BLOCKED_EMOTION` redFlag estale quando aluno/mentor mudava emoção pós-criação. Agora: `['stopLoss','entry','exit','qty','side','emotionEntry']`. Reconstrução de redFlags filtra `BLOCKED_EMOTION` antes de regerar contra `after.emotionEntry` vs `plan.blockedEmotions`. `setup` e `emotionExit` deliberadamente fora — `setup` não afeta cálculo de RR/RO/redFlags (geométrico), `emotionExit` não é consumido por nenhum dim do engine. Maturity engine roda incondicionalmente em todo update então `setup` é capturado via `helpers.js` strategy-consistency mesmo sem estar em `complianceFields` | #188 | 25/04/2026 |
| DEC-AUTO-197-01 | **`meetingLink`/`videoLink` em `students/{uid}/reviews/{rid}` são metadata operacional (não conteúdo congelável).** Editáveis por mentor em DRAFT e CLOSED via `useWeeklyReviews.updateMeetingLinks` (update parcial: `{meetingLink, videoLink, updatedAt}` sem mudar `status`). ARCHIVED bloqueia (read-only via `canEdit`). Não entram no `frozenSnapshot` ao publicar — preserva imutabilidade da análise (takeaways/SWOT/snapshot/maturity) sem travar metadata operacional. Justificativa: link da gravação só existe DEPOIS da reunião terminar, depois do mentor publicar a revisão; tratá-los como conteúdo congelável criava cenário operacionalmente impossível. `firestore.rules` linhas 65-71 já permitiam mentor `CLOSED→CLOSED` com qualquer campo — sem alteração de regras. Validação via `validateReviewUrl` (regex https + allowlist hosts: zoom.us, meet.google.com, teams.microsoft.com, loom.com, youtube.com, drive.google.com, vimeo.com) acontece client-side antes do `updateDoc`. Alternativa descartada: nova rule com `hasOnly(['meetingLink','videoLink','updatedAt'])` — duplicaria permissão já existente sem ganho de segurança | #197 | 25/04/2026 |
| DEC-AUTO-201-01 | **Estilo operacional é eixo INDEPENDENTE do profile.** Profile (CONS_A..AGRES_B) define orçamento (RO%) e cadência (trades/dia). Estilo (scalp/day/swing/conviction) define a estrutura do stop via fração de ATR diário (`STYLE_ATR_FRACTIONS = { scalp: 0.05, day: 0.10, swing: 0.20, conviction: 0.30 }`). Aluno escolhe ambos explicitamente no preview do plano. Calibração derivada da tabela §7 do `docs/dev/research/spec-original-attack-plan-2026-04-07.md` (NY range = 60% ATR diário). Substitui o back-calc legado `stopPoints = roUSD / pointValue` que produzia stops desconectados da volatilidade real do instrumento (causa raiz do bug 187pts × 1 contrato MNQ Apex 50K) | #201 | 25/04/2026 |
| DEC-AUTO-201-02 | **Banda ±10% de stopBase entre profiles dentro do mesmo estilo (`PROFILE_STOP_VARIANCE = 0.10`).** CONS_B (15%) é referência (variance = 1.0); CONS_A → +6.7% stop (mais largo, mais defesa de entrada), CONS_C → -6.7%, AGRES_A → -10% (clamp), AGRES_B → -10% (clamp). Mantém o sinal estrutural do estilo (scalp ≠ swing) sem destruir hierarquia de profile. Calibração inicial; revisar quando dataset Monte Carlo (Fase C roadmap) tiver dados reais por (template × profile × estilo) | #201 | 25/04/2026 |
| DEC-AUTO-201-03 | **Instrument é mandatório no preview do plano (`mode: 'abstract'` deprecated).** Aluno escolhe instrumento ANTES de gerar plano — todo plano tem stop em pontos reais e sizing computado. Hard validation no motor: `calculatePlanMechanics` rejeita se `instrument` ausente. UIs (AddAccountModal, AccountsPage) tornaram seletor de instrumento mandatório. PropFirmPage prefere motor novo quando `propFirm.suggestedPlan.instrument` salvo, fallback legado para contas pré-#201. Wrapper `attackPlanCalculator.calculateAttackPlan` mantido @deprecated para zero regressão dos call sites | #201 | 25/04/2026 |
| DEC-AUTO-201-04 | **ATR estático no MVP — refresh manual mensal pelo mentor via PR.** Provider externo (Polygon.io ou Databento) fica como fast-follow (Fase E roadmap). Justificativa: volume baixo, mudança trimestral, hardcoded em `instrumentsTable.js` é prático no MVP. Disclaimer: número desatualizado de ATR distorce stop estrutural — calibrar com captura TradingView ATR(14) diário no início de cada mês | #201 | 25/04/2026 |
| DEC-AUTO-201-05 | **DT-042 resolvido dentro do motor: floor effective = `max(MIN_VIABLE_STOP[type], instrument.minStopPoints || 0)`.** O motor `calculatePlanMechanics` consume tanto o floor type-level (em `MIN_VIABLE_STOP` de propFirmDefaults) quanto o per-instrument (em `minStopPoints` de instrumentsTable), tomando o maior. Substitui ordem ad-hoc de prioridade, garante consistência. RTY scalp (avgDailyRange 70 × 0.05 = 3.5pts) é clipado pelo floor 15 (type) > 3 (per-instrument) → `clippedByFloor=true` → `incompatible` com `violation: stop_below_min_viable`. MNQ scalp (27.45pts) usa floor 20 (per-instrument) > 15 (type), sem clip | #201 | 25/04/2026 |
| DEC-AUTO-201-06 | **`riskPctPerOp = roPerTrade/pl` direto (supersede DEC-072) + adapter expõe `roEffective`, não `roBudget`.** 3 mudanças semânticas aplicadas durante validação browser do PR #202: (a) `propPlanDefaults.computePropPlanDefaults`: `riskPctPerOp = toPct(attackPlan.roPerTrade, pl) || periodStopPct || DEFAULT_RISK_PCT` — wizard recebe RO/trade real (0.66% no Apex 50K CONS_B, não 1.5% periodStop); (b) `toLegacyAttackPlanShape`: `roPerTrade = roEffective ?? roBudget` (foi `roBudget`) + adiciona campo `roBudget` separado — UIs mostram dinheiro realmente em risco ($329,40 com sizing 3 contratos no MNQ day, não $375 alocado); (c) Engine `evPerTrade/winUSD/lossesToBust` usam `roRealized = sizingMeta.roEffective ?? sizingMeta.roBudget` — EV/análise de bust refletem operação real. Justificativa: pós-sizing dinâmico, `roBudget` é teto alocado (orçamento) e `roEffective` é o que vai à ordem de fato (`contracts × stop × pointValue`); rotular `riskPctPerOp = periodStopPct` (DEC-072) era artefato do mundo "sizing=1 fixo" — não faz sentido após motor #201. Campo `roBudget` permanece exposto para diagnóstico (label "(% DD)") | #201 | 26/04/2026 |
| DEC-AUTO-187-01 | **Storage MEP/MEN como preço puro (`mepPrice`/`menPrice`).** Em vez de salvar pontos (futures) ou % (ações) crus do CSV ProfitPro, parser converte para preço na entrada — invariante a unidade, casa com `entry`/`exit`/`stopLoss` que já são preço, e elimina necessidade de campo extra `unitType`. Camadas downstream (UI, validação, engine) consomem tudo como número de preço sem branching por instrumento. Trade-off aceito: parser absorve complexidade de conversão (futures: `entry ± \|raw\|`, equity: `entry × (1 ± \|raw\|/100)`) — única lógica que precisa conhecer instrumento. Validação por lado: LONG → `mepPrice ≥ max(entry,exit)` e `menPrice ≤ min(entry,exit)`; SHORT inverte. Aceita só um campo preenchido | #187 | 27/04/2026 |
| DEC-AUTO-187-02 | **Yahoo Finance 1m + compute&discard, janela 7d hard-coded.** Fonte gratuita pra MEP/MEN de US futures (CME) quando broker não exporta (Tradovate). Free tier serve só últimos 7d em granularidade 1m — limitação aceita: aluno que não importa em 7d "não tem maturidade pra rastreabilidade que o gate de maturidade exige" (decisão direta do Marcio). Tickers continuous (`MNQ=F`, `ES=F` etc) — implica contract-roll: trade de contrato vencido lido fora do front-month vigente retorna dados errados; mitigado por janela 7d que coincide com front-month em produção real. Bars OHLC fetchados → `max(high)`/`min(low)` no range entre `boughtTimestamp` e `soldTimestamp` → grava em trade → bars descartadas (sem cache, sem nova collection). LONG: `mep=max(highs)/men=min(lows)`; SHORT inverte. Falha (HTTP 5xx, timeout, shape inválido) → `excursionSource: 'unavailable'` sem rethrow. BR futures (WIN/WDO/IND) sem cobertura por design — sem fonte 1m gratuita de B3, ProfitPro entrega nativamente | #187 | 27/04/2026 |
| DEC-AUTO-187-03 | **Gate Stage 3→4 `advancedMetricsPresent` é opcional/condicional — `null` ou `true`, NUNCA `false`.** Mudança no `preComputeShapes.js`: substitui o stub `advancedMetricsPresent = false` (literal — bloqueava promoção de QUALQUER aluno por falta de dado) por `deriveAdvancedMetricsPresent(trades)` que retorna `null` quando insuficiente, `true` quando satisfaz threshold. `evaluateGates` já trata `null` como `METRIC_UNAVAILABLE` (gate pendente: não promove + não rebaixa, DEC-020 preservada). Resultado: aluno sem MEP/MEN não é rebaixado, fica parado em Metódico até começar a registrar. Quem investe tempo (manual ou import ProfitPro) → progride. Quem não → espera. Auto-imposto pela falta de dado, não bloqueio explícito | #187 | 27/04/2026 |
| DEC-AUTO-187-04 | **Threshold de derivação de `advancedMetricsPresent`: ≥10 trades + ≥80% com MEP+MEN não-null.** Constantes `ADVANCED_METRICS_MIN_TRADES = 10` e `ADVANCED_METRICS_MIN_RATIO = 0.8` em `preComputeShapes.js`. <10 trades → `null` (insuficiente). ≥10 trades, <80% rastreados → `null`. ≥10 trades, ≥80% → `true`. Por que 80%: equilíbrio entre "habitual" (signal claro de tracking) e "razoável" (não quebra por 1 trade esquecido em 5). Por que mínimo 10: estatística mínima pra inferir "rastreamento habitual" — abaixo disso é ruído. Calibração inicial sem dataset real; revisar se uso real mostrar threshold inadequado | #187 | 27/04/2026 |
| DEC-AUTO-187-05 | **UX de MEP/MEN é pts (futures) ou % (equity); storage permanece em preço (clarifica DEC-AUTO-187-01).** Aluno digita pts/% no AddTradeModal (suffix dinâmico por `detectInstrumentType(ticker)`); submit converte via `convertExcursionRawToPrice` antes do gateway. Display em qualquer renderização deriva pts/% via novo helper `derivePtsFromPrice` (sinal: MEP `+`, MEN `-`). Componente reutilizável `ExcursionDisplay` (variants `compact`/`full`) elimina lógica duplicada — usado em TradesList, ExtractTable, FeedbackPage, TradeDetailModal, ReviewTradesSection, MentorDashboard, StudentFeedbackPage. Mockup M1 do issue (bloco "Métricas avançadas" colapsável com ⓘ) finalmente implementado. Fix de gap descoberto após PR #206 abrir: implementação original tinha inputs preço puro sem unidade visível e display só em 2 lugares | #187 | 28/04/2026 |

---


- **DEC-AUTO-208-01** (29/04/2026): Taxonomia dos 5 eventos comportamentais de execução — STOP_TAMPERING, STOP_PARTIAL_SIZING, RAPID_REENTRY_POST_STOP (literatura: Kahneman&Tversky 1979, Shefrin&Statman 1985, Coval&Shumway 2005, Locke&Mann 2005, Odean 1998), HESITATION_PRE_ENTRY (heurística), CHASE_REENTRY (heurística, agregado de Barber&Odean 2000). Janelas operacionais 10min/30min são heurísticas — calibrar empiricamente.
- **DEC-AUTO-208-02** (29/04/2026): Persistência Opção C — eventos computados on-the-fly em `recomputeMaturity` via `executionBehaviorMirror` CommonJS, sem campo Firestore novo. INV-15 preservada. Migração para inline (`_executionEvents`) ou subcollection (`behavioralEvents`) reservada a issue futuro após validação empírica.
- **DEC-AUTO-208-03** (29/04/2026): Gates Stage 3→4 condicionais — `no-stop-tampering`, `no-chase`, `disciplined-sizing` em `GATES_BY_TRANSITION['3-4']`. Cobertura mínima `EXECUTION_COVERAGE_FLOOR=30` trades com order data; abaixo disso retorna `null`/METRIC_UNAVAILABLE (padrão DEC-AUTO-187-03). DEC-020 preservada — gates não promovem nem rebaixam sem evidência.
- **DEC-AUTO-208-04** (30/04/2026): Stop implícito — trade que fechou em loss sem stop formal NÃO emite NO_STOP red flag (a saída em prejuízo conta como stop praticado, exit-as-stop). Cenários que SEGUEM gerando NO_STOP: trade sem stop em win ("risco não mensurado"), trade sem stop em breakeven, trade sem Order Import correlacionada e sem stopLoss informado. Aplicado em `src/utils/compliance.js` + 2 paths em `functions/index.js` (onTradeCreated red flag + recompliance update). Supersedes parcialmente DEC-006 (NO_STOP segue como violação fora do caso de stop implícito).
- **DEC-AUTO-208-05** (29/04/2026): Pesos `EVENT_PENALTIES` (HESITATION 5, CHASE 10, RAPID_REENTRY 15, PARTIAL_SIZING 10, STOP_TAMPERING 20) são calibração heurística inicial — não derivam de literatura. Validação empírica obrigatória em N alunos por 90 dias antes de promoção a gates bloqueantes ou de ajuste fino.
- **DEC-AUTO-208-06** (29/04/2026): RAPID_REENTRY_POST_STOP usa critério `prev.result < 0` em vez de "stop FILLED" — saída manual em loss (Zeragem) é equivalente comportamental a stop disparar (loss-chasing independe do mecanismo de saída). Validado contra dataset 24/04 onde T1→T2 e T3→T4 são saídas manuais em loss e ambos disparam o detector como esperado.
- **DEC-AUTO-208-07** (30/04/2026): `correlatedTradeId` é populado na ingestão de orders via correlação ANTES do `ingestBatch`, não depois. Inclui cancels via `correlateCancelledOrders` (overlap temporal vida-da-ordem ↔ vida-do-trade, confidence 0.7). Causa raiz do bug "0/N trades correlacionados": pipeline anterior chamava `ingestBatch(batchId, {}, ...)` e o cálculo de correlação posterior ficava em memória sem persistência.
- **DEC-AUTO-208-08** (30/04/2026): `isStopOrder` no parser ProfitChartPro classifica EXCLUSIVAMENTE por tipo de ordem (`STOP`/`STOP_LIMIT`), não pelo campo `Preço Stop` preenchido. ProfitChartPro preenche esse campo em entradas SuperDOM com stop anexado e legs OCO com gatilho — classificá-las como stop bagunçava STOP_TAMPERING/STOP_PARTIAL_SIZING.
- **DEC-AUTO-221-01** (01/05/2026): Chave de identificação de violações limpas pelo mentor em `mentorClearedViolations: string[]` — compliance usa o próprio code (`NO_STOP`, `RR_BELOW_MINIMUM`, `RISK_EXCEEDED`, `DAILY_LOSS_EXCEEDED`, `BLOCKED_EMOTION`); evento emocional usa `${eventType}:${tradeId}`. Plano original usava `${eventType}:${eventTimestamp}` — `tradeId` é mais estável (timestamp pode ser reescrito em re-enrich), mantém isolamento por trade e empata com a granularidade do filtro `effectiveEmotionalEventsForTrade`.
- **DEC-AUTO-221-02** (01/05/2026): `mentorClearedViolations` sem audit metadata — array de strings simples, sem campos `reason`/`clearedAt`/`clearedBy`. Auditoria visual fornecida via bloco "Limpas pelo mentor" no `FeedbackPage` (toggle `↺ Restaurar` faz `arrayRemove`). Trade-off: identidade de quem limpou e razão não persistida no Firestore — aceito porque mentor toggle é "lei dentro de v1" (sem histórico de override). Reabrir se entrar requisito de compliance externo.
- **DEC-AUTO-221-03** (01/05/2026): Detectores comportamentais não re-rodam quando mentor limpa violação — só agregação filtra via `effectiveRedFlags`/`effectiveEmotionalEvents*`. Preserva séries históricas (`detectTilt` segue reportando TILT em sequências futuras mesmo se um evento individual passado foi limpo) e mantém pipeline de detecção determinístico. Filtro vive na camada de consumo (compliance rate, period score, gates de maturity).
- **DEC-AUTO-229-01** (01/05/2026): Tolerance dos detectores STOP_BREAKEVEN_TOO_EARLY + STOP_HESITATION é resolvida por prefixo de ticker (WIN=5, WDO=0.5, IND=5, MNQ/NQ/MES/ES=0.25 — match longest-first), com fallback `max(0.01, 0.05%·entry)` para tickers desconhecidos. Mantém paridade ESM↔CJS (`getInstrumentTolerance` em `src/utils/executionBehaviorEngine.js` e `functions/maturity/executionBehaviorMirror.js`).
- **DEC-AUTO-229-02** (01/05/2026): Janela "cedo demais" do STOP_BREAKEVEN_TOO_EARLY usa SOMENTE critério de tempo na v1 (Δt < 5min entre entry e reissue do stop). Critério complementar de runUp (stop em breakeven antes de 50% do alvo atingido) fica como fast-follow: requer integração com MEP intermediário durante a vida do trade, dado não disponível no schema atual (#187 captura MEP final). Conservador na v1 — falsos negativos preferíveis a falsos positivos sem evidência.
- **DEC-AUTO-229-03** (01/05/2026): STOP_HESITATION NÃO entra em `TILT_EXEC_TYPES` no `detectTiltV2` (ao contrário de STOP_TAMPERING + STOP_PARTIAL_SIZING + STOP_BREAKEVEN_TOO_EARLY). Sinal sutil de indecisão, não tilt-state propriamente. Ainda rebate em `calculatePeriodScore` via `EVENT_PENALTIES` (penalty 5).
- **DEC-AUTO-235-01** (2026-05-02): rfr = Selic histórica diária via BCB SGS-11 descontada apenas em dias com trade; fallback hardcoded quando cache vazio.
- **DEC-AUTO-235-02** (2026-05-02): N mínimo por `adjustmentCycle` — Mensal=10, Trimestral=30, Semestral=60, Anual=120; abaixo retorna `null` (estado "Insuficiente — opere mais X dias").
- **DEC-AUTO-235-03** (2026-05-02): Sharpe per-ciclo anualizado por √252 (252 dias úteis) — padrão indústria; variância amostral N-1 (Bessel) consistente com `computeSharpe` legado.
- **DEC-AUTO-235-04** (2026-05-02): Sharpe-no-ciclo NÃO entra em gate de maturity v1 — gates rolling (`monthlySharpe`/`annualSharpe`) continuam intactos.
- **DEC-AUTO-235-05** (2026-05-02): Threshold semáforo Sharpe — >2 excelente · 1.5–2 bom · 1–1.5 ok · 0–1 fraco · <0 destrói.
- **DEC-AUTO-235-06** (2026-05-02): Sortino NÃO entra na v1 — escopo separado (fast-follow se skew negativa for relevante).
- **DEC-AUTO-235-07** (2026-05-02): Visualização vive no card "Consistência Operacional" do StudentDashboard; quando #72 Fase 1 aterrissar, também no `CycleSummary`.
- **DEC-AUTO-235-08** (2026-05-02): Schema cache Selic (INV-15 aprovada via `#issuecomment-4364111738`) — subcollection `systemConfig/selic/history/{YYYY-MM-DD}` + doc-pai `systemConfig/selic` com metadata.
- **DEC-AUTO-235-09** (2026-05-02): Fallback Selic indisponível — constante hardcoded em `functions/marketData/selicFallback.js` (atual 14.75% a.a.), atualizada manualmente em PR; `rfrSource: 'fallback'`.
- **DEC-AUTO-235-10** (2026-05-02): Selic faltante para dia operado — carry-forward (último valor conhecido) + flag `rfrInterpolated: true` no audit; gap maior que threshold → fallback constante.
- **DEC-AUTO-235-11** (2026-05-02): Padrão de orquestração — CF própria `fetchSelicDaily` (cron diário 09h BRT `America/Sao_Paulo`); refatorar para gateway se chegarmos a ≥5 jobs diários OU dependência entre jobs.
- **DEC-AUTO-235-12** (2026-05-02): CV normalizado SUBSTITUI CV puro no card de Consistência Operacional (não coexistem na UI nova); plano sem `targetRR` → métrica null + CTA "definir RR alvo no plano".
- **DEC-AUTO-235-13** (2026-05-02): Threshold semáforo CV normalizado — ≤1.2 verde · 1.2–1.5 âmbar · 1.5–2.0 laranja · >2.0 vermelho · <0.5 âmbar (suspeitamente estável).
- **DEC-AUTO-235-14** (2026-05-02): Fonte da WR para CV esperado — WR efetiva do aluno na janela do ciclo (mais responsivo); fallback `plan.expectedWinRate` quando amostra <minDays.
- **DEC-AUTO-235-15** (2026-05-02): MEP/MEN coverage threshold — `coverage < 0.7` exibe badge `⚠ MEP/MEN em N de M trades` no card (transparência de amostra).
- **DEC-AUTO-235-16** (2026-05-02): Migração de Revisão Semanal — reviews já fechadas mantêm CV puro (`coefVariation`) congelado SEM mitigação técnica (sem backfill, sem label "legado", sem tooltip, sem banner); novos snapshots ganham `cvNormalized`; mentor humano explica descontinuidade caso a caso. Risco UX absorvido por proximidade Marcio↔alunos + amostra pequena.
- **DEC-AUTO-235-17** (2026-05-02): Sharpe rolling no engine de maturity (`monthlySharpe`/`annualSharpe`) INALTERADO — gates `sharpe-1_2`/`sharpe-1_5` calibrados em #119 ficam com `rfr=0`.
- **DEC-237-01** (2026-05-03): `contacts/` collection descartada durante a issue; consolidação em `students/{uid}/subscriptions/` (DEC-055/056). UI unificada em `/assinaturas` com criação inline de aluno (modal Nova Assinatura: nome+celular+email opcional).
- **DEC-237-02** (2026-05-03): Aluno pré-Alpha pode existir sem Auth — `students/{uid}` criado via createInlineStudent com uid local quando email ausente. Sem dashboard até email ser adicionado.
- **DEC-237-03** (2026-05-03): Plano `vip` é primeiro-classe (`PLAN_LABELS.vip='VIP'`, badge fuchsia), distinto de `alpha`/`self_service`. Modais força `plan='vip'` quando `type='vip'`.
- **DEC-237-04** (2026-05-03): Saída de Alpha não deleta student — botão excluir removido do `StudentsManagement`. Mentor muda plano da subscription preservando trades/maturity. Limitação de acesso por plano fica como issue futura.
- **DEC-237-05** (2026-05-03): Busca de proximidade no `StudentsManagement` restrita a Alpha não-cancelled; "Usar este" só completa email (se vazio) e celular (se diferente), nunca sobrescreve nome/plano/pagamento.
- **DEC-AUTO-240-01** (04/05/2026): `planCoversDate` (`src/utils/planCoverage.js`) compara dia local UTC normalizado (`YYYY-MM-DD`) ao invés de timestamp ms. Plano criado às 14h passa a cobrir ordem das 11h do mesmo dia. `closedAt` segue mesma lógica. Tradeoff de borda noturna (entre 21h BRT e meia-noite UTC) é aceito porque `entryTime` do CSV é serializado em hora local sem fuso, mantendo simetria.
- **DEC-AUTO-240-02** (04/05/2026): `useCsvStaging.activateTrade/Batch` reusa `checkDuplication` exportado de `orderTradeCreation.js` (DRY). Ambos pipelines de import (Order Import e CSV de performance) passam a aplicar o mesmo critério de dedup (ticker+side+entryTime±5min+qty), independente do `source` do trade existente (manual / order_import / csv).
- **DEC-AUTO-240-03** (04/05/2026): Auto-enrich silencioso de MEP/MEN/`excursionSource` quando duplicata é detectada e o trade existente NÃO tem o campo. Conservador: nunca sobrescreve, sempre via `updateTrade` (gateway oficial), validado por `validateExcursionPrices` antes do write. Default `excursionSource: 'profitpro'` quando CSV não vem com origem explícita. Falha de validação degrada para skipped puro, sem quebrar o batch.
- **DEC-AUTO-242-01** (04/05/2026): `stopSemantic` compara `Preço Stop` contra o LIMITE da entrada (`entryOrders[0].limitPrice`), NÃO `avgFillPrice`. Justificativa: stop é configurado no momento da entrada baseado no preço-alvo planejado; slippage não muda intenção de proteção. Confirmado contra fórmula RR=(target-entry_lim)/(entry_lim-stop_trig) usada na reconstrução manual. Bate exatamente com a tabela de critério de aceite do issue.
- **DEC-AUTO-242-02** (04/05/2026): Função nova `enrichOperationsWithStopSemantic(operations)` posicionada entre `associateNonFilledOrders` e `enrichOperationsWithStopAnalysis` em `OrderImportPage.handlePlanConfirm`. Não toca pipeline existente. Percorre todas as ordens (entries+exits+cancelled+stops) — `associateNonFilledOrders` só cobre não-FILLED, então não consegue marcar saídas FILLED com `Preço Stop` (caso trade #1).
- **DEC-AUTO-242-03** (04/05/2026): Campos derivados (`stopSemantic` por ordem; `hasRealStopLoss` por operação) ficam **in-memory** no shape de `ReconstructedOperation` — sem schema novo na collection `trades` (INV-15 não acionada). `hasRealStopLoss` fica disponível para `executionBehaviorEngine` e issues posteriores (NO_STOP_LOSS_AT_ENTRY).
- **DEC-AUTO-242-04** (04/05/2026): `enrichOperationsWithStopSemantic` lê `entryOrders[0].limitPrice` (com fallback para `.price`), NÃO `entryOrders[0].price` direto. Justificativa: descoberto durante regression test integrado (`040526-clear-daytrade.csv`) — `normalizeOrder` (`orderNormalizer.js:47`) sobrescreve `price` com `filledPrice/avgFillPrice` e move o `Preço` original (Limite) para `limitPrice`. Sem essa correção, trades com slippage (#2 entry 190.565 → fill 190.365; #4 entry 189.645 → fill 189.830) classificavam stop incorretamente. Reconfirma DEC-AUTO-242-01 — referência continua sendo o LIMITE, só muda o nome do campo.
- **DEC-AUTO-263-06** (11/05/2026): REVOGADA — aluno sem email volta a aparecer em `classifyStudent` com sub Alpha/Espelho ativa/trial. Domínio fechou "WhatsApp-only não existe": Acompanhamento é o lugar primário do registro e o mentor cadastra o email no ritual via drawer. A versão anterior filtrava sem email retornando `null`, escondendo candidatos legítimos.
- **DEC-AUTO-263-20** (11/05/2026): novo campo `loginBlockedReason: 'manual' | 'auto' | null` em `/students`. `setStudentLoginBlocked` (callable do mentor) grava `'manual'`; `checkSubscriptions` (scheduled) grava `'auto'` ao virar sub overdue. Permite que o desbloqueio automático não sobrescreva bloqueio manual.
- **DEC-AUTO-263-21** (11/05/2026): autobloqueio + auto-unblock por sub status. `checkSubscriptions`: ao virar `status='overdue'` (ultrapassou `gracePeriodDays`), no mesmo batch marca `student.loginBlocked=true` + `reason='auto'` e desabilita Auth user. Idempotente: NÃO sobrescreve `reason='manual'`. Trigger novo `onSubscriptionStatusChange` (onUpdate em `students/{id}/subscriptions/{subId}`): se status volta de `overdue` para `active`/`pending`, desbloqueia somente quando `reason='auto'` (manual fica preservado).
- **DEC-AUTO-263-22** (11/05/2026): remoção do bucket `'aguardando-plano'` em `classifyStudent` + `TIER_CONFIG`. Domínio fechou: Acompanhamento mostra apenas alunos com sub Alpha/Espelho/Trial atribuída. Aluno em ritual sem sub atribuída precisa que o mentor crie a sub via aba Assinaturas primeiro — workaround intermediário do DEC-AUTO-263-10 foi removido.
- **DEC-AUTO-266-01** (11/05/2026): auto-recovery no CF `checkSubscriptions` é safe-by-default — status `'overdue'` só é mantido quando `renewalDate < today − graceDays`. Para qualquer outro caso (futuro, hoje, dentro do grace), batch update `status:'active'` e roteia para `expiringToday`/`expiringSoon`/silenciosa conforme urgência. Reconcilia divergência entre UI (computa status on-the-fly em `useSubscriptions.deriveStatus`) e CF (lia literal de Firestore). Fix do bug raiz #266 sem precisar de migração manual de dados.
- **DEC-AUTO-266-02** (11/05/2026): desbloqueio de Auth user no auto-recovery é condicional a `student.loginBlockedReason === 'auto'`. Bloqueios manuais (mentor via UI) NÃO são revertidos automaticamente — mentor controla esse caso. Mirror simétrico do autobloqueio em G1 #263 (DEC-AUTO-263-21), preservando idempotência e respeito à intenção operacional do mentor.
- **DEC-AUTO-271-01** (12/05/2026): allowlist do `isOwner` em `firestore.rules:45` passa a incluir `'accessStatus'` junto com `status`, `firstLoginAt`, `onboardingStatus`. Propaga DEC-AUTO-263-07 para a regra — antes, o cliente escrevia o campo no `activateStudent` mas a regra rejeitava o update inteiro (rules.hasOnly), causando regressão silenciosa onde nada era gravado no 1º login. Invariante: qualquer adição de campo no payload de `activateStudent` exige atualização simultânea da allowlist; teste `buildActivatePayload — payload ⊆ allowlist` em `studentActivation.test.js` protege isso.
- **DEC-AUTO-271-02** (12/05/2026): guard de `activateStudent` passa de `status === 'pending'` para `accessStatus !== 'active'` (lógica extraída para `src/utils/studentActivation.js` como `shouldActivateStudent`). Cobre cenário onde aluno tem `status='active'` por outro caminho (createInlineStudent → createStudent modo PROMOTE, ou legados pré-DEC-AUTO-263-07) mas `accessStatus='pending'`. Idempotente — chamada redundante não dispara escrita. Política em util puro testável; side-effect (`updateDoc`) permanece no AuthContext.
- **DEC-AUTO-278-01** (22/05/2026): Escala UNDERSIZED_TRADE usa comparação `<=` em `mediumRatio` e `highRatio` (50% utilização do RO → MEDIUM, não LOW). Threshold de saída segue `>=` (65% escape). Decisão alinhada com tom donut — 50% utilização não é "desperdício leve", é o ponto onde a estatística R-local começa a falsificar (cada gain meia-stop vira R inflado em Payoff/PF/EV).
- **DEC-AUTO-278-02** (22/05/2026): Em trades de loss ou breakeven subdimensionados (scenario `LOSS_BE`), o bloco educacional adapta texto sem citar "alvo do plano" (não se aplica sem gain). Campos win-only (`expectedGainAtPlanRR`, `rGapVsPlan`, `planRsDelivered`, `rrLocalAchieved`) ficam null. UI renderiza `null → '—'` no accordion técnico, evitando literal "null".
- **DEC-AUTO-278-03** (22/05/2026): Copy do bloco educacional idêntica para aluno e mentor. `ShadowBehaviorPanel` continua mentor-only por default (`isMentor=false` retorna null no topo); quando aluno tem acesso (FeedbackPage), vê o mesmo conteúdo. Evita drift de manutenção dupla — o insight comportamental vale igual para os dois lados.
- **DEC-AUTO-278-04** (22/05/2026): `tradeId` muted (`text-xs font-mono text-slate-500/40`) renderizado APENAS no header do `TradeDetailModal`, não no `ShadowBehaviorPanel` embedded em `FeedbackPage`. Painel embedded fica limpo. Tooltip via `title="Click para copiar"` (navegador nativo), click usa `navigator.clipboard?.writeText` silencioso + `e.stopPropagation()` para não fechar o modal.
- **DEC-AUTO-259-01** (24/05/2026): Symlink `~/projects/issue-259/node_modules → ~/projects/acompanhamento-2.0/node_modules` (root) pra Vitest rodar no worktree; `functions/` reusa via `../node_modules/.bin/vitest`. Evita reinstalação 800MB. Decisão técnica autônoma.
- **DEC-AUTO-259-02** (24/05/2026): Email digest pra mentor cortado (over-engineering); badge intra-app `<ClosurePendingBadge>` cobre o sinal sem canal externo.
- **DEC-AUTO-259-03** (24/05/2026): Customização de SWOT por mentor adiada pra issue #262 (fast-follow). 1A entrega heurísticas universais hardcoded.
- **DEC-AUTO-259-04** (24/05/2026): A10.1 (polish CycleConsistencyCard labels) skipped: card shipped em v1.54.0, labels já claras, mexer arrisca regressão sem ganho real. Foco no fechamento de #259.
- **DEC-AUTO-259-05** (24/05/2026): `useCycleExpiredQueue` aceita `{plans, trades}` injetados; dashboards passam os arrays já carregados (mesma fonte que renderiza ledger). Hook continua chamando `usePlans/useTrades` por simetria de hooks rules, mas resultado é descartado quando inject vier. Resolve divergência studentEmail vs studentId.
- **DEC-AUTO-259-06** (24/05/2026): `startDate` da fila = `min(planCreatedAt, firstTradeDate)`. Cobre import CSV histórico depois de criar plano. Trade datado antes do `createdAt` é ground truth.
- **DEC-AUTO-259-07** (24/05/2026): Ciclo vazio entra na fila apenas se atraso ≥7d. Sub-7d skip preserva UX limpa em ciclos no início; ≥7d força reflexão (TILT/pausa/abandono são sinais valiosos, não buracos).
- **DEC-AUTO-259-08** (24/05/2026): Fast-follow mobile-responsive ritual proposto como issue separada. Wizard atual desktop-first; mentor camera (inbox grid-12 + sticky comment panel) inviável em mobile vertical.
- **DEC-AUTO-259-09** (24/05/2026): Step3Reflect Q3 (atribuição) realinhada ao trader_evolution_framework (Kahneman + Mark Douglas + Stage 1-2 sob frustração). Banner âmbar empático em ciclos críticos, pergunta vira convite (não cobrança), 4 atribuições com EVIDÊNCIA derivada, denialFlag novo em behavioralSummary, MentorClosuresInbox sinaliza atribuição externa apesar de erros internos detectados.
- **DEC-AUTO-259-10** (24/05/2026): Step5Check reescrito pedagogicamente. `maxDDPercent` retorna `null` quando `initialBalance` falsy (fix de bug `0 ≤ 5` virando ✓ verde). `GATES_BY_TRANSITION` ganha campos `friendlyLabel/unit/whatIs/howTo` pros 36 gates. Cards expansíveis 3 estados (✓/✗/⊘), agrupamento por dimensão, "Foco pro próximo ciclo" com top 3 gates mais perto. MFE/MAE → MEP/MEN PT-BR.
- **DEC-AUTO-259-11** (24/05/2026): Estatísticas de ciclo (Sharpe/CV/Excursion + maturityRecompute gate) computam sobre todos os trades com `result` numérico, sem filtro `status==='CLOSED'`. Trade é unidade monolítica; revisão (CLOSED/OPEN/QUESTION/REVIEWED) é enriquecimento periférico que NÃO deve gatear cálculo.
- **DEC-AUTO-259-12** (24/05/2026): `deleteAccount` faz cascade em 7 estágios (movements/plans/trades/orders/cycleClosures/cycleAlerts/account). Tools de purga de órfãos vivem em `src/utils/cleanupOrphans.js` (client `window.__cleanup`) + `scripts/cleanup-orphan-cycle-docs.mjs` (Admin SDK, dry-run default, dupla confirmação pra execute).
- **DEC-AUTO-259-13** (24/05/2026): `closeCycle` é READ-ONLY no plano para campos semânticos (`pl/riskPerOperation/rrTarget`): só escreve com `valor > 0`; `newRiskPerOp: 0` da REGRA PAUSA é sinal documental (closure.behavioralSummary.critical + notifyMentor), nunca state destrutivo. Modal usa `??` em vez de `||` para preservar 0 visível.
- **DEC-AUTO-259-14** (24/05/2026): Gate `pl ≤ availableBalance` originalmente isolado a `mode === 'create'`. Smoke posterior mostrou que edição que aumenta ou mantém pl acima do saldo livre também precisa do gate. Decisão consolidada: gate dispara em qualquer transição cujo `newPl > availableBalance`, independente do modo; protege contra realocação de capital inexistente sem corromper o capital base histórico (que só baixa quando o aluno deliberadamente reduz pl).
- **DEC-AUTO-259-15** (24/05/2026): Gate de saldo no fechamento de ciclo (`closeCycle` CF). `effectivePL = planUpdate.pl ?? plan.pl` é comparado contra equity do ciclo. Ultrapassagem → `HttpsError('failed-precondition')` com mensagem instruindo recalibração. Banner amber no Step6Adjust avisa visualmente antes da tentativa. Fecha o ciclo completo de proteção do capital base.
- **DEC-AUTO-259-16** (24/05/2026): Handlers `onSnapshot/onMetrics/onPatterns/onAar/onSwot/onMaturity/onForward/onNotes` do `CycleClosureWizard` estabilizados via `useCallback([replaceSection])`. Padrão a seguir para qualquer step que registra a callback como dep de `useEffect` interno — arrow inline gera loop. `replaceSection` já é estável (`useCycleClosureDraft.js:186`), então a dep é segura.
- **DEC-AUTO-259-17** (24/05/2026): Em qualquer dep de `useEffect` dos steps, valores que poderiam ser `array literal` em fallback (ex.: `maturity?.regression ?? []`) devem ser envoltos em `useMemo` cujo dep é o campo original. Literal `[]`/`{}` inline gera ref nova a cada render, refire o effect e cria loop quando o effect emite setState upstream.
- **DEC-AUTO-259-18** (24/05/2026): Gates do servidor (B14-B17) precisam de espelho na UI que bloqueie ações que levariam a 400 silencioso. Servidor permanece como autoridade; UI só impede o clique inútil. Implementação: step emite flag via prop callback, wizard armazena em useState e propaga pra footer (`disableNext`) e botão Selar (`canSeal=false`). Mensagens de erro de submit visíveis em 2 lugares (topo + acima do footer).
- **DEC-AUTO-259-19** (24/05/2026): Erros do ciclo unificam compliance violations + behavioral events (revenge/overtrading/tilt/stopTampering/etc) sob a mesma rubrica "Erros do ciclo" com peso pedagógico equivalente. Mark Douglas: comportamento repetido sob pressão = regra interna violada. Catálogo único PT-BR em `ERROR_LABELS_PT` (cycleMetrics.js). UI diferencia tipograficamente via `source: 'compliance' | 'behavioral'` sem hierarquizar.
- **DEC-AUTO-259-20** (24/05/2026): Contrato C1: `plan.pl` é imutável após criação. Única rota legítima de mudança é o fechamento de ciclo (closeCycle grava PL do próximo ciclo). Edição via Modal é bloqueada (read-only + texto explicativo + cascade delete como única alternativa). Defesa em profundidade: `usePlans.updatePlan` faz strip do campo antes do updateDoc.
- **DEC-AUTO-259-21** (24/05/2026): Contrato C2: saldo do plano é derivado on-the-fly (`pl + Σ trades_ciclo_aberto`) via `planBalance.computeCurrentPl(plan, trades)`. Não persistido. Ciclo aberto começa no dia seguinte ao `lastClosedCycleEnd` (ou todos os trades se nunca fechou ciclo). Resolve por construção a divergência detectada pela auditoria. `plan.currentPl` fica no schema como legado.
- **DEC-AUTO-259-22** (24/05/2026): Contrato C3: `closeCycle` persiste em `cycleClosures` doc 2 campos novos — `cycleBaseline` (plInicial/saldoFinal/plFinal) com ground truth do servidor lendo plan na transaction, e `preClosePlanSnapshot` (pl + RO + RR + 4 metas) como foto pré-fechamento para reabertura restaurar. schemaVersion bumped 2→3. Regra da virada: `plan.pl` após fechamento = ajuste explícito (se aluno aplicou) ou `cycleBaseline.plFinal` por default.
- **DEC-AUTO-259-23** (24/05/2026): Contrato C4: `reopenCycle` restaura plano (pl + RO + RR + 4 metas) a partir de `closure.preClosePlanSnapshot` no mesmo update que remove o sealed range. Decrementa `currentCycleNumber` se havia incrementado. Cache `lastCycleClosureId` recua pro penúltimo restante. Closures antigos sem o snapshot → log warning, plano permanece.
- **DEC-AUTO-259-24** (24/05/2026): Contrato C5: trades retroativos bloqueados após qualquer fechamento. `tradeGateway.createTrade` rejeita `trade.date ≤ plan.lastClosedCycleEnd` (via `isTradeBeforeLastClosedCycle` em sealCheck.js + mirror CJS). Cobre criação manual e CSV import (que passa pelo mesmo path). Edit de trade existente em período retroativo mantém só o range check original. Sem ciclo fechado: aceita qualquer data (onboarding livre).
- **DEC-AUTO-259-25** (24/05/2026): Composite indexes adicionados pra cycleClosures: `(studentId, status)` pra queue do dashboard (filter closures CLOSED do aluno) + `(status, closedAt)` pra inbox do mentor (janela 7d). Sem esses índices, queries falhavam silenciosamente no front (onSnapshot error callback só logava console.error), e o ciclo recém-fechado reaparecia como aberto.
- **DEC-AUTO-259-26** (24/05/2026): Gate de saldo no Step6/closeCycle revisado pós-C2/C3 — `newPl` agora é comparado contra `cycleBaseline.plFinal` (equity do ciclo isolado sendo fechado), não contra `account.currentBalance` (que inclui trades de ciclos futuros já abertos). Ciclo é unidade discreta de alocação; só o que ele gerou vira capital do próximo. Step6 mostra equity preciso (centavos), input pré-preenche com max alocável.
- **DEC-AUTO-259-27** (24/05/2026): Audit button (`recalculateCompliance` CF) e `usePlans.diagnosePlan` (client) usam filtro C2 — somam apenas trades do ciclo aberto (`date > lastClosedCycleEnd`), não todos os trades desde a criação. Pré-C2 estava certo (plan.pl imutável); pós-C2 (plan.pl muda a cada close) dupla-contava trades dos ciclos fechados, gerando `currentPl` errado no plano e UIs derivadas.
- **DEC-AUTO-259-28** (24/05/2026): `reopenCycle` só aceita reabrir o último closure da cadeia — gate novo na CF (`plan.lastCycleClosureId !== closureId → HttpsError`) + UI espelhada em MentorClosureView (botão disabled + tooltip explicativo). Reabrir closure no meio quebra a cadeia: closures posteriores foram calculados sobre o plan.pl pós-aquele close. Política: reopen sempre do head pra trás.
- **DEC-AUTO-259-29** (24/05/2026): `closurePlanAdvisor` REGRA 0 PAUSE — `stopTampering` reclassificado como sinal de APOIO. Sozinho não justifica banner crítico vermelho + size=0 (pode ser trail legítimo). Só agrega à rationale quando algum trigger primário já dispara (tradesAfterStop≥3, tiltDays≥5, revenge≥3, perda≥1.5×cap). Stop tampering isolado cai pra REGRA 3 (scale_down preventivo).
- **DEC-AUTO-259-30** (24/05/2026): `PlanLedgerExtract` lê PL(0) histórico do closure quando ciclo está fechado, via novo hook `usePlanClosures(planId)`. Prioridade: `cycleBaseline.plInicial` (C3+) → `snapshot.plStart` (closures pre-C3) → `plan.pl` (fallback pra ciclo aberto). Antes, qualquer ciclo selecionado mostrava `plan.pl` corrente (que é o do próximo ciclo após o close) — extrato de março mostrava PL de abril.
- **DEC-AUTO-259-31** (24/05/2026): Sistema inline de toast/confirm substitui `window.alert()` e `window.confirm()` nativos do browser. `ToastProvider` + `useToast()` em `src/contexts/ToastContext.jsx` (stack canto superior direito, glass-card padrão, auto-dismiss 5s). `ConfirmDialog` + `useConfirmDialog()` em `src/components/ConfirmDialog.jsx` (tones danger/warning/neutral). 25 ocorrências (15 alerts + 10 confirms) refatoradas em 13 arquivos. Preservado: `cleanupOrphans.js` (dev tool via console).
- **DEC-AUTO-282-01** (27/05/2026): Vocabulário canônico de métricas de ciclo = label técnico (Sharpe, CV norm., Profit Factor, Expectancy (R)…) + tooltip didático, aplicado igual no card "Consistência Operacional" (dashboard, #235) e no wizard de Fechamento de Ciclo (Step1Read, #259). Decisão de produto do Marcio — coerência conceitual entre os dois pontos onde o aluno lê os mesmos indicadores.
- **DEC-AUTO-282-02** (27/05/2026): Métricas de consistência (Sharpe / CV norm. / MEP médio / MEN médio) no wizard de fechamento são display-time — calculadas on-the-fly via `useCycleConsistency`, NÃO entram no `frozenSnapshot` do `closeCycle`. Evita tocar o contrato C4 (snapshot/restore) e INV-15; Sharpe pode variar com Selic histórica, mas os trades estão selados (C5).
- **DEC-AUTO-282-03** (27/05/2026): Breakdown do TPS no Step1 re-rotulado pro vocabulário técnico (Profit Factor / Max Drawdown / Expectancy (R) / Aderência) para coerência com os tiles na mesma tela; "Consistência semanal" mantida (métrica própria do TPS, sem conflito de nome).
- **DEC-AUTO-267-01** (27/05/2026): `entryTime`/`exitTime` naive (sem offset) interpretados como America/Sao_Paulo (UTC-3 fixo, Brasil sem DST desde 2019) na busca de barras Yahoo do enrichment MEN/MEP; strings com offset/`Z` são respeitadas. Antes a string sem offset era lida como UTC e deslocava a janela, produzindo min/max de minutos errados silenciosamente.
- **DEC-AUTO-267-02** (27/05/2026): `enrichTradeWithExcursions` aborta (`excursionSource: 'unavailable'`) quando falta `entryTime` OU `exitTime`, em vez de cair em fallback `from = entryTime || date` / `to = exitTime || entryTime` (janela errada / duração-zero). MEN/MEP só com horário real do trade.
- **DEC-AUTO-267-03** (27/05/2026): carry-over de patrimônio da EquityCurve por forward-sum derivado (alternativa 3b), não placeholder-snapshot (3a). `abertura(janela) = Σ aporte + Σ result(trades antes da janela) + Σ ajuste-não-trade(fechamentos com cycleEnd < janela)`. Sem escrita nova (INV-15) nem poluição de `cycleClosures` (que alimenta inbox do mentor + fila sequencial de ciclos vencidos). Ajuste não-trade só nasce no ritual de fechamento, então onde não há fechamento `aporte+Σtrades` é exato.
- **DEC-AUTO-267-04** (27/05/2026): carry-over aplica-se ao caminho single-currency da EquityCurve (`windowOpeningBalance`/`cycleOpeningBalance` em `useDashboardMetrics`); o modo multi-moeda (tabs, ≥2 moedas) segue usando o saldo inicial por moeda — limitação conhecida, fora do escopo do bug report (trader vendo o ciclo mensal em moeda única).
- **DEC-AUTO-267-05** (27/05/2026): `mentorClearedViolations` propaga ao 4D (decisão real de maturidade, não cosmética — confirmado por Marcio). `calcComplianceRate` (server, `preComputeShapes`) passa a contar via `hasEffectiveRedFlags` → reflete nos gates Compliance ≥80%/≥95% e na dimensão Operacional; `ExtractTable` suprime badges NO_STOP/RO/RR limpos pelo mentor. Eventos de execução (#208: stop-tampering/chase, de order data) ficam fora — não são limpáveis pelo mentor (sem chave de cleared).
- **DEC-AUTO-302-01** (03/06/2026): metadados de revisão do mentor (`mentorFeedback`, `feedbackDate`, `feedbackHistory`, `status`, `updatedAt`, `mentorClassification*`, `mentorClearedViolations`) ficam isentos do hard seal #259 no update de `/trades` quando são o único delta — dispensa o `get()` do plano (resolve plano órfão) e libera revisão/classificação em ciclo fechado. Edição de campo financeiro/comportamental em ciclo selado segue bloqueada. Decisão de produto (Marcio, 01/06/2026): mentor revisa/classifica trade em qualquer estado de ciclo.
- **DEC-AUTO-301-04** (03/06/2026): campo inline `trade.behaviorProfile` (snapshot do motor `detectBehavior`, Fase 2 do #301) — opção A (field inline) sobre subcollection/collection raiz. Razão: acesso por-trade é o padrão dominante (mentor/aluno lê junto do doc; B/C cobram +1 read por trade em listas) e espelha o precedente já aprovado `shadowBehavior`; custo é a reescrita do doc já feita no recompute. Escrita só por CFs (admin SDK); **read liberado ao aluno** (educativo — Marcio: aluno vê tudo). Compliance (`redFlags`, fato/INV-21) permanece campo separado, fora do `behaviorProfile`. INV-15 aprovado por Marcio em 03/06/2026.
- **DEC-AUTO-301-01** (01/06/2026): shadow (`byTrade`) é ESM-only — sem mirror CJS (maturidade server-side não consome shadow per-trade). Paridade ESM≡CJS cobre só a superfície compartilhada (`events` + `scoreInputs`).
- **DEC-AUTO-301-02** (01/06/2026): `byFamily` colapsa por `(tradeId, family)`; precedência DEC-074 (maior `resolutionLayer`; empate→`events`).
- **DEC-AUTO-301-03** (02/06/2026): `gateInputs` é sinal de gate UNIFICADO (execução+shadow+emocional) — tilt/revenge emocional dobram em TILT/LOSS_CHASING; gate cruza por FAMÍLIA (membro não-gate aciona família gate, ex.: IMPULSE_CLUSTER→OVERTRADING).
- **DEC-AUTO-301-05** (04/06/2026): Confronto Emocional — matriz categoria-da-emoção-declarada (`getEmotionConfig.analysisCategory`) × severidade-do-padrão-dominante → `ALIGNED`/`ATTENTION`/`MISALIGNED`/`NO_DECLARED`. Sub-campo `behaviorProfile.emotionConfront`. Tom espelho (não acusação); aluno vê tudo; v1 só entrada (`emotionExit` fast-follow). Matriz aprovada por Marcio 04/06/2026.
- **DEC-AUTO-305-01** (05/06/2026): penalidade comportamental no 4D é **rate-normalized** — `min(CAP, round(Σ peso_severidade na dim / N_trades_com_profile × SCALE))`, pesos HIGH=3/MED=2/LOW=1, `SCALE=8`, `CAP=25`, `BONUS_CAP=10`. Substitui a tentativa count-absoluto (que saturava o cap com 2 findings, sem discriminar intensidade nem tamanho da janela). Proporcional ao rate. Aprovado por Marcio 05/06/2026. Números/tabela de impacto em `docs/dev/behavioral-weight-map.md` §Calibração.
- **DEC-AUTO-305-02** (05/06/2026): a **migração do Emocional (E)** dos `EVENT_PENALTIES` ad-hoc (`emotionalAnalysisV2.js`) pra escala unificada do motor ficou **fora do #305** — fast-follow em issue própria. Razão: maior blast-radius (CHUNK-06 + display emocional, re-baseline maior). No #305, B1 modula só F/O (que não pesavam comportamento); E segue penalizando via o caminho legado, sem double-count com o novo (não há sobreposição porque o net não é aplicado a E no B1).
- **DEC-AUTO-305-03** (05/06/2026): `ruleViolationRate = trades_com_violação / trades_com_profile` (vida nova: trade sem behaviorProfile fora do numerador E do denominador). Floor de cobertura 10 trades com profile → `null` (METRIC_UNAVAILABLE, gate pendente, DEC-020 preservada). Gates §5.3: 1→2 ≤0.30 · 2→3 ≤0.15 · 3→4 ≤0.05 · 4→5 ≤0.01. Clearing comportamental usa chave `canonicalCode:tradeId` (espelha o emocional).
