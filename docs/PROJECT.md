# PROJECT.md — Acompanhamento 2.0
## Documento Mestre do Projeto · Single Source of Truth

> **Versão:** 0.40.43  
> **Última atualização:** 04/09/2026 — v0.40.43: Encerramento #423 v1.88.2 — **o menu do mentor recupera os destinos; pendências acima da dobra** (PR #424, squash `764ab550`). Regressão de **desenho** do #144, reportada por Marcio ao usar a v1.88.1 em produção: *"ficou uma bosta, cadê o resto das coisas? cadê o dashboard?"*. O menu foi de 9 para 5 itens e os quatro destinos removidos (Análises, Fila de Revisão, Aguardando Feedback, Fechamentos) passaram a existir só dentro da Torre. **O que o mockup escondeu:** a faixa "A Turma" renderiza a turma INTEIRA, sem limite de linhas (`turma.map` sem `slice`) — então a ordem real era `tiles → Prioridade → alertas → A TURMA (n linhas) → Minhas Pendências → rodapé`, e com 12 alunos tudo que o mentor usa todo dia caía **abaixo da dobra**, com Análises reduzida a um link cinza de 12px no pé da página. O mockup aprovado desenhou a Torre como caixa compacta; ela não é, e a aprovação foi dada sobre um desenho que não representava a tela. **Correção:** os cinco destinos voltam ao menu com badge (o contador é o que faz o item ser procurado); "Minhas Pendências" sobe para antes de "A Turma" — o que decide o dia não fica depois de uma lista que cresce com a turma; o link de rodapé sai; e "Precisam Atenção" volta como tela com os critérios originais. **DEC-423-01..05 revogam DEC-144-01/02/04:** a Torre é a *home*, não a única porta — item de menu é onde se procura o que se usa todo dia, e enterrar destino dentro de página de altura variável não é hierarquia, é esconder. **DEC-423-05** é a lição de método: mockup de tela com lista de tamanho variável deve declarar o **pior caso de altura**, não a caixa compacta. **Fica de pé do #144** o que não estava em questão: roteador (URL, back, deep link, fim dos oito contextos de retorno), casca única e escala do design system; a Torre segue como primeiro item e destino do login. Sem persistência nova, sem CF tocada. 4.778 testes.  
> **Última atualização (histórica):** 04/09/2026 — v0.40.42: Encerramento #421 v1.88.1 — **hotfix: a tela do mentor não subia em produção** (PR #422, squash `abb5fd8e`). A v1.88.0 foi ao ar e o mentor viu tela branca: `Cannot access 'K' before initialization`, depois de os listeners subirem — no render do `MentorDashboard`. **Causa:** `selectedStudent` era `useState` no topo do componente; ao virar derivado da rota no #144 (`useMemo` sobre `studentIdSelecionado`) foi declarado na linha 238, enquanto `selectedStudentTrades` o lê na 187, durante o render. `const` em TDZ derruba a árvore inteira. **Por que atravessou tudo:** build verde, lint sem erro novo e 4.768 testes verdes — e **nenhum teste montava o `MentorDashboard`**. A suíte cobre utilitários, hooks e componentes pequenos, e o teste de rotas do próprio #144 stuba a página; erro de ordem de declaração só aparece executando o corpo do componente. É AP-08 (Build Verde, App Quebrada) em estado puro, e o gate que falhou foi de **verificação**, não de código: "suíte verde" foi reportado como se fosse cobertura da tela, quando a tela nunca era executada em teste nenhum. **O que impede a repetição:** `__tests__/pages/MentorDashboardMount.test.jsx` monta a página nas quatro views e nos três casos da ficha, e foi verificado contra o código quebrado que estava no ar — falha com a mensagem exata do incidente. **Lição operacional registrada:** página que só é exercitada por teste de rota com stub não tem cobertura de render; refatoração que muda `useState` para valor derivado move a declaração, e mover declaração em componente React é mudança de ordem de execução. Sem mudança de UI, de comportamento ou de persistência. Sem CF tocada.  
> **Última atualização (histórica):** 04/09/2026 — v0.40.41: Encerramento #144 v1.88.0 — **a Torre é a única porta: roteador real, shell e design system adotado** (PR #420, squash `c0ad32e3`). Marcio, 03/09: *"minha torre e dashboard de mentor não parece nada com um SaaS profissional, não há encaixe e naturalidade de navegação, não consigo competir"*. O sintoma era um, as causas eram três, e a medição no código separou as três. **(1) Não existia roteador.** A navegação inteira era uma string em `App.jsx` (`currentView`): sem URL, sem back do browser, sem link compartilhável, e F5 devolvia o usuário ao início. E porque não havia rota, **cada travessia entre telas foi resolvida à mão** — oito estados de contexto de retorno (`feedbackReturnPlanId`, `feedbackReturnReviewContext`, `ledgerReturnReviewContext`, ...) mais flags penduradas no próprio objeto do trade (`_fromLedgerPlanId`, `_fromReviewContext`) só para decidir o destino do botão voltar. A navegação parecia arbitrária porque **era**: cada par origem→destino tinha regra própria. `react-router-dom` estava instalado desde sempre e era usado em **um** lugar (`useParams` no onboarding, sem Router montado — inerte). **`App.jsx` cai de 562 para 28 linhas**: providers e a tabela de rotas. A casca virou `AppShell`, o papel virou guard de rota em vez de `&& isMentor()` repetido em cada ramo, o endereço virou SSoT em `routes/paths.js`, e voltar virou histórico — com fallback num caso só, quem abriu o link direto (`useVoltar`, via `location.key`). O que era estado de abertura virou query string (`?revisao=`, `?conta=`). **(2) Dois sistemas de navegação concorrentes.** Sidebar de 9 itens e 6 abas do dashboard, colados por um dicionário de tradução — e o efeito concreto era que **o mentor logava em Análises**, não na Torre, que o próprio código declarava ser a home. Decisão de Marcio: **a Torre é a única porta** (DEC-144-01). Menu de 9 para 5 itens, sobrando o que não é trabalho do dia; a barra de abas e o dicionário morreram; três das quatro filas viraram a faixa **Minhas Pendências** da Torre — agora com **Fechamentos**, que só existia no menu —, em três linhas iguais onde contador zerado fica cinza em vez de sumir, porque caixa vazia é informação (DEC-144-03). **A quarta fila saiu com perda de sinal declarada:** "Precisam Atenção" recortava por performance acumulada (prejuízo, WR < 40%, PF < 0,8) e a faixa "A Turma" ordena por conduta e presença — **não são a mesma população**, e o critério de performance não sobreviveu em lugar nenhum (DEC-144-02). Análises desceu para saída de rodapé: é diagnóstico, serve depois de escolher a pessoa (DEC-144-04). **(3) O design system existia e era contornado.** Cada página declarava o próprio topo — seis paddings de container e seis tratamentos de `<h1>` convivendo —, e o topo mudava de altura e de peso a cada navegação: a inconsistência que o olho lê como "amador" sem conseguir nomear. Container passa a ser do `AppShell` e tipografia do `PageHeader`, com o `pb-20` do DebugBadge valendo para todas de uma vez em vez de cada página lembrar (DEC-144-08). A escala que já existia de fato foi **escrita**: `2xl` card, `xl` controle, `lg` painel, `full` só chip — `rounded-md` absorvido, porque a escala tem três degraus e não quatro (DEC-144-07). 44 superfícies idênticas viraram primitiva, incluindo 15 painéis escritos em **duas ordens de classe diferentes**, o que escondia que eram a mesma coisa. **O que não foi convertido tem motivo:** as 152 superfícies cruas restantes não são cards esperando `glass-card` — são controles de formulário com paddings diferentes e chips, e trocar tudo empilharia dois vidros e apagaria a hierarquia. **Três trechos de código morto** saíram no caminho, todos revelados pela mudança e nenhum novo: o bloco `activeView === 'students'` ("Lista de Alunos"), inalcançável desde que o App interceptava a view antes de o dashboard montar, e com ele o `StudentEmotionalCardWrapper`; o `AddTradeModal` do nível do App, **um segundo modal órfão** que só abria com um id que nenhum menu emitia; e o `useMentorClosureInbox` do Sidebar, listener do Firestore assinado só para pintar um número. Um teste de invariante (`invariants/pageChrome.test.js`) segura a drift do chrome, que volta sozinha porque tela nova copia a anterior. Deep link em produção já estava coberto: `vercel.json` tem o rewrite catch-all. Sem persistência nova, sem CF tocada. **Segue aberto:** a decomposição de componentes e os listeners duplicados (fora de escopo, eram as Fases 5 e 6 do épico original) e **PEND-144-01** — qual dos dois idiomas de botão primário é o do produto, gradiente ou chapado. 4.768 testes (+72).  
> **Última atualização (histórica):** 03/09/2026 — v0.40.40: Encerramento #418 v1.87.0 — **a etapa Ajustar explica Kelly e Monte Carlo, e o histograma passa a ser o real** (PR #419, squash `51f5ccbf`). Marcio, revendo o fechamento de agosto: *"não informa o que Kelly faz e por que, nem MC — o aluno às vezes não tem noção desses modelos matemáticos"*. A etapa 6 era **a única das oito sem título, sem frase de abertura e sem uma única explicação clicável**: o aluno saía da etapa Avaliar, que ensina cada critério com "O que é / Como atingir", e caía num card "Risco ótimo (Kelly ¼)" com badge escrito `matemática`, seguido de percentis crus `p10/p50/p90`. **A pergunta de desenho era em que ordem colocar a matemática**, e a resposta veio de Marcio: não escolher. A etapa passa a ter veredito em uma linha → *De onde vem esse número* (os dois modelos expansíveis) → *A recomendação* → *Sua decisão* — quem confia decide na primeira linha, quem quer entender encontra os modelos **antes** dos botões, e ninguém é obrigado a atravessar a conta. O que precisa ser visto não depende de clique: a faixa de resultado e a conta do Kelly ficam visíveis com o card fechado. **Kelly deixa de ser "risco ótimo"** e vira *até quanto dá pra arriscar sem se tirar do jogo*; **Monte Carlo deixa de ser "1000 cenários"** e vira *que faixa de resultado esperar* — com **"em N dos 1000 cenários o próximo ciclo termina no vermelho"** como manchete, qualificada pelo tamanho do pool quando a amostra é pequena. **Um defeito de credibilidade saiu junto:** o histograma era decorativo desde o #259 — `McHistogram` desenhava 17 barras com altura derivada do **índice da barra** (`Math.abs(i - bars/2)`), com o comentário "Distribuição estilizada — não temos os outcomes brutos", enquanto `runMonteCarloBootstrap` calculava os 1000 outcomes e descartava todos menos cinco percentis. O motor passa a devolver `histogram` e `pLoss`, **aditivos**, e um teste de não-regressão compara a saída campo a campo com e sem `histogramBins`; bin vazio não vira barra, que é o que faz cauda e assimetria aparecerem. **Jargão que sai da tela do aluno:** badge `matemática`, `heurística v1.58`, `[em v1.59 vira IA real]`, `Decisão registrada: suggestion_accepted` — e, **achado durante a implementação**, os textos de risco do próprio advisor (`Sample 20 insuficiente — esperar n ≥ 50 antes de escalar`), que o teste de jargão pegou depois de o plano dá-los por fora de escopo. A cópia lê `triggeredRule` e números, **nunca re-deriva regra**; `rationale` e `risks` crus seguem para o Firestore e para o `MentorClosureView`, onde o vocabulário técnico é adequado. **Três bugs menores no caminho:** `R$` hardcoded em dois pontos (conta em dólar mostrava R$), Kelly de 0,0285% exibido como **"0.0%"** — lido ao pé da letra, manda parar de operar —, e o mesmo dado com dois nomes na mesma tela ("Vitória vale × perda" vs. "RR alvo"). **A matemática não muda:** Kelly, bootstrap e `closurePlanAdvisor` intactos, e as asserções numéricas do #416 (`+1.3%`, `-4.5%`, `+7.5%`, `+40.5%` ausente) ficaram no lugar sem afrouxar — guarda de regressão não se solta junto com a redação. `ExplainerCard` duplica a casca do `GateCard` de propósito: unificar arrastaria semântica de gate (met/gap/threshold) que não existe aqui (AP-03). Sem persistência nova — `histogram` e `pLoss` são display-time e ficam fora do draft, mesma política do #282. Sem CFs tocadas. 4.696 testes (+50).  
> **Última atualização (histórica):** 02/09/2026 — v0.40.39: Encerramento #416 v1.86.1 — **a esteira de fechamento para de mentir** (PR #417, squash `f12e147e`). O fechamento real do ciclo de agosto foi lido passo a passo contra o código, e a esteira exibia números e textos que não correspondiam ao dado. **Causa raiz única em seis dos treze defeitos: texto ou número derivado de um PROXY em vez do dado** — faixa de pontuação, taxa, constante fixa, string fixa, sinal do P&L, outlier. **O pior era forward-looking:** a projeção do próximo ciclo mostrava mediana **+40,5%** e p10 **−135,5%**, resultado que significaria capital negativo. O motor estava CERTO — bootstrap aditivo em R$, sem composição — e os testes passavam porque verificavam os percentis em R$; o erro era a constante `1000` hardcoded no divisor, **na mesma linha do JSX que imprimia `baseCapital`**. Os valores reais: +1,3% e −4,5%. **O mais perigoso era prescritivo:** SUSTENTAR sugeria "replicar a configuração de entrada" do melhor trade num ciclo com vingança detectada, e propagava ao SWOT do mentor como Força — o guard comportamental existia seis linhas acima, aplicado ao sustain de aderência e não aos de outlier. **E um era pedagogicamente invertido:** o dia com vingança rendeu +R$ 560 e aparecia em VERDE, com a seção "Custo emocional" trocando o ícone para carinha sorrindo, contra dias limpos em vermelho — reforço intermitente escrito em CSS. Junto: hints do TPS afirmando "ganhos médios menores que perdas" com payoff 1,18; "4 violações de regras" calculado como `(1 − aderência) × total` e contradizendo a tela anterior; "dias limpos" que incluíam hesitação e perseguição. **No motor, três defeitos de contrato:** `ruleViolationRate` ignorava `feedsGates` e reprovava saída antecipada num gate onde o #101 decidiu que ela não pesa — `behavioralDetection` já respeitava o flag via `GATE_CODES`, `aggregateBehaviorWeights` não; aderência contava trade **sem** objeto `compliance` como não-aderente, transformando ausência de dado em comportamento e alimentando o TPS com peso 20; e o gate "6 meses com a mesma estratégia" descartava o parâmetro `plans` na primeira linha (`void plans`) e contava meses em que um único `trade.setup` dominava >60% — impossível por construção para playbook multi-setup, mesma família do `strategy-8-weeks` do #376. Passa a medir meses sem mudança em `RISK_FIELDS` (SSoT novo em `src/utils/planRiskFields.js`) lidos de `plan.editHistory`. **Correção do próprio spec:** o issue afirmava que `feedsGates` não era lido em lugar nenhum — a busca foi pelo nome literal e não viu a via derivada; o defeito real era mais estreito, e o body foi corrigido. **Achado que a spec não previa:** `handleMentorSavePlan` nunca passou `changedFields`, então o caminho do mentor gravava `fields: []` desde sempre e a métrica nova jamais veria uma edição de mentor. **Um `STOP: INVARIANT` correto durante o loop:** `ResultadoDoAluno.jsx:58` (entrou em `4ab2c3eb`/#411, depois da redação do spec) consome `computeRuleAdherenceRate` cru — trocar o contrato apagaria o tile de Aderência do MentorDashboard sem erro e sem teste vermelho, em CHUNK-16, que o issue não detém. Resolvido mantendo `number | null` e expondo a cobertura por função irmã com predicado compartilhado: o denominador correto chega às duas telas sem escrever em chunk sem lock. **Fora de escopo, nomeado:** pesos e escalas do TPS, gates estatísticos com n mínimo ou IC (hoje `evaluateGates` compara ponto contra threshold, sem termo de amostra — WR 10/20 vira verde), redundância Sharpe × CV, ranking de erros por custo em R, Kelly simétrico e `bestTradeId`. Sem persistência nova. 23 DEC-AUTO. 4.646 testes no front, 260 em `functions/`. Executado em modo autônomo (§13), 8 tasks, sem intervenção humana no loop.  
> **Última atualização (histórica):** 31/08/2026 — v0.40.38: Encerramento #414 v1.86.0 — **relatório do mês no lugar do Diário** (PR #415, squash `503c7739`). O Diário era segunda via do Dashboard (mesmos `TradesList`/`AddTradeModal`/import) e não respondia a pergunta de fim de mês do aluno. A rota passa a servir uma linha por trade **com feedback do mentor**: trade · observação de entrada + refs HTF/LTF · feedback. Três regras sustentam a tela: "tem feedback" lê `mentorFeedback` legado E `feedbackHistory` com dedup (ler um esconde metade da base); o recorte é `trade.date`, não `feedbackDate` (o mês é do trade, não da resposta); e o total é **por moeda**, nunca um número só (#289/#408). Sem persistência nova. `TradesJournal.jsx` apagado.
> **Última atualização (histórica):** 30/08/2026 — v0.40.37: Encerramento #408 v1.85.0 — **Aguardando Feedback vira árvore aluno → dia → plano → trade** (PR #412, squash `94dbeee5`). A fila entregava uma lista plana por aluno: cada operação chegava sem passado e o mentor reconstruía o contexto de cabeça. Agora é árvore sobre o `buildPeriodState` do #402, sem cálculo nem persistência nova. **O nível de PLANO é chave, não atributo:** o período é medido por plano, com limiares e moeda próprios — aluno com B3 e mesa no mesmo dia tem dois períodos, e juntá-los somaria reais com dólares contra o stop de um só; com um plano só, o nível colapsa. A revisão semanal ganhou **"Para lembrar na conversa"**, com os fatos do período que a lista de trades não carrega, e os lembretes BONS entram junto com os ruins ("bateu a meta e parou" nunca é dito porque não gera alarme). **Três correções de fundo.** (1) `frozenSnapshot.periodTrades` é PROJEÇÃO, não trade (`tradeId`/`pnl`/`symbol`/`closeTime`, sem `date`/`planId`/`studentId`): a seção descartava a revisão inteira em silêncio — adaptador na fronteira, que serve ao congelado e ao rebuild live (DEC-AUTO-408-04). (2) *"Operação aberta sem orçamento"* virou **"Aberta sem previsão de stop"** e SAIU do lembrete do dia para a linha do trade: é fato atômico, e o docstring de `authorizationNotice` já dizia *"mora no painel do trade, nunca no card do período"* (DEC-AUTO-408-01/02/03). (3) `orderingConfidence` não via duas operações no MESMO segundo — devolvia "confiável" e a ordem saía do desempate por id; agora é `tied_instants`, e **com a ordem em dúvida a tela cala a sequência** (ordinal, autorização por operação, "continuou depois do stop", "bateu a meta e parou") e mantém o que não depende dela (líquido, "ultrapassou por quanto", "fechou na meta") — DEC-AUTO-408-05/06. **Isso desenterrou a contradição inversa do #402:** ao calar o aviso, a caixa verde do `BehaviorPanel` afirmava "execução alinhada" sobre predicado NÃO AVALIADO — silêncio virando absolvição (DEC-AUTO-408-07). E um defeito que nunca apareceu em tela: o aviso de ordem no card do plano lia `ordem.detail ?? ordem.title`, campos que `dayOrderingNotice` não devolve. **Dados:** apagado `trades/blS5d3FkJax0qR4CBcQK`, um doc com `behaviorProfile` e nada mais (resíduo de merge em trade apagado) — 380 trades, **todos com plano**, confirmando a regra de domínio *"aluno sem plano não tem trade"* (DEC-AUTO-408-08). Criado **`docs/data-dictionary.md`**, gerado da PRODUÇÃO com taxa de preenchimento por campo (`scripts/gerar-dicionario-dados.mjs`), depois de três erros de inferência de nome no mesmo dia (`entryPrice`→`entry`, `takeProfit`→`plan.rrTarget`, e a projeção do snapshot); regra correspondente no CLAUDE.md. 8 DEC-AUTO. 4.475 testes. 44 CFs deployadas no passo 0a.  
> **Última atualização (histórica):** 29/08/2026 — v0.40.36: Encerramento #101 v1.84.0 — **Torre de Controle: a tela que diz o que fazer** (PR #411, squash `4ab2c3eb`). O épico estava travado desde 27/07 e foi construído em cinco fases numa sessão. **A decisão que definiu o desenho veio da medição, não da spec:** na semana de 24-28/08, **8 dos 12 alunos não operaram**, a fila de feedback tinha **2 trades** e cinco alunos com assinatura viva estavam sem operar há 15, 78, 92, 127 e 176 dias. As duas telas anteriores triavam um fluxo de violações que quase não existe e **nenhuma mostrava quem sumiu**. Por isso o padrão adotado é **painel de elenco** (Khan, TrainingPeaks, painel de pacientes) e não console de incidentes (PagerDuty, Zendesk): o alarme virou COLUNA e critério de ordenação, e a lista é a turma inteira, sempre (DEC-AUTO-101-19/20). A Torre é **aba do Dashboard** em três faixas — Agir agora, A turma, Minhas pendências — e a Visão Geral virou **Análises**, o nível de diagnóstico. A ficha do aluno foi reconstruída em torno do **Plano de Conversa** (mudar no operacional, mudar no emocional, preservar), com evidência numérica e fala pronta por linha; o detalhamento virou três tabelas e o Perfil Emocional saiu — submetido ao teste da conversa, sobrou 1 item de 6 (issue #409 parada com a decisão de produto). **Bugs de domínio corrigidos no caminho, todos medidos na base:** o dia é por **CONTA**, não por aluno (aluno com duas contas tinha −350 USD e −350/−520 BRL somados contra o stop de uma delas); **promover virava regressão** porque o detector compara métricas com o estágio atual — a promoção passa a **zerar a régua** via `stageSince` (DEC-AUTO-101-22); o recompute **apagava o histórico de promoções** (`stageHistory: []` fixo no payload, e `{merge:true}` não protege array); **regressão sugerindo abaixo do baseline** atingia 7 dos 8 alunos acusados, um deles com ZERO trades (DEC-020 aplicada no detector + filtro de leitura, DEC-AUTO-101-23); **rebloqueio de login virou invariante** e não evento (desbloquear um inadimplente valia para sempre); **saída antecipada nunca é violação grave** — separar medo de proteção exigiria dado de mercado pós-saída, e futuros B3 não têm série de 1 minuto gratuita, então o teto vale na LEITURA sem backfill (DEC-AUTO-101-24); o **calendário do mentor renderizava vazio** (lia `dayOfWeek`/`pl`, campos que `generateCalendarData` não devolve mais); a **fila de revisão trazia aluno que já saiu** (filtrava por campo legado que vale 'active' para 67 dos 68); e o **painel de alertas rodava em loop de render** por carimbo `new Date()` em alerta sem campo `date`. **Fora do CHUNK-16, assumido:** assinaturas/auth (rebloqueio) e maturidade (promoção). 25 DEC-AUTO. 4.406 testes. 28 CFs deployadas — três delas (`checkSubscriptions`, `recomputeStudentMaturity`, `enrichTradeWithExcursions`) ficaram fora do primeiro deploy e exigiram deploy dirigido. **Épico fechado sem tocar nos filhos** #103/#72/#70/#31, todos verificados abertos depois.  
> **Última atualização (histórica):** 27/08/2026 — v0.40.35: Encerramento #402 (reaberta) v1.83.33 — **a caixa verde contradizia a ressalva de abertura** (PR #407, merge `2b1250bd`). A entrega da 1.83.32 recriou, ao lado do defeito que ela removeu, o mesmo tipo de contradição: o painel dizia *"Aberta sem orçamento — restavam R$ 0,00 de folga e o plano autoriza R$ 40,00 por operação"* e, dois blocos abaixo, *"Nenhuma violação de plano nem padrão de risco neste trade — execução alinhada"*. **Causa:** `BehaviorPanel.jsx:327` condicionava a verde a `effective.length === 0 && cleared.length === 0` — olhava só os red flags e não conhecia o `authNotice`; como `SEM_FOLGA` é aviso e não violação (DEC-AUTO-402-03), ele não entra em `effective`. A verde passa a exigir `!authNotice`, e com ressalva de abertura o texto afirma só o que pode: *"Nenhum padrão comportamental de risco na execução — veja a ressalva sobre a abertura, acima"*, sem estender para "nenhuma violação de plano". **Achados de ferramental no encerramento, todos com efeito real:** o `cc-close-issue.sh` resolve UM PR por issue (`gh pr list --search` pega o primeiro dos mergeados) e o #402 teve quatro — o CHANGELOG saiu creditando só o #403 e omitindo layout, virada de mão e linha de posição; as 14 `DEC-AUTO-402-*` nunca chegaram ao `decisions.md` porque o doc de controle que as listava é arquivado no passo 4; e, na segunda passagem, com duas linhas de `#402` no registry, o script pegou a primeira e **reverteu o `version.js` de 1.83.33 para 1.83.32**, além de desfazer o crédito dos três squashes. Os três foram corrigidos à mão neste commit. DEC-AUTO-402-12/13/14 registradas. 4.146 testes no front. Sem deploy de CF.  
> **Última atualização (histórica):** 27/08/2026 — v0.40.34: Encerramento #402 v1.83.32 — **o sistema acusava o trade errado, e o fato do dia morava dentro do trade** (PRs #403 `56c0dea8` / #404 `761145b4` / #405 `298a3245`). Um trade de −R$ 250, **primeira operação do dia** contra um limite diário de ~R$ 501, foi acusado de estourar o stop diário. Causa imediata: `getDailyLoss` somava o dia inteiro **sem corte temporal** e **só as perdas**, e o importador gravou o 2º trade 3s antes do 1º — a acusação dependia da **ordem de escrita do lote**, não do que o aluno fez. Debaixo disso, um problema estrutural: `LOSS_DIARIO_EXCEDIDO` era o único fato **AGREGADO** guardado num container **ATÔMICO** (`trade.redFlags[]`), então todo trade do dia levava uma cópia e `complianceRate` era deflacionado por N/total. **Na base: 34 acusados, 3 violações reais, 31 falsas, zero falsos negativos.** **Três fatos, três donos** (DEC-AUTO-402-01/02): o que é da operação fica na operação; o resultado do período é **LÍQUIDO** e pertence ao período; o stop do período governa **autorização para ABRIR** (DEC-069), não é carimbo retroativo. Novos SSoT espelhados ESM/CJS: `tradeInstant` (ordem canônica `data → instante → createdAt → id`, total e determinística — substitui 5 ordenações ad-hoc, duas por `createdAt`, isto é, por ordem de importação) e `dayState` (motor do período). `getDailyLoss` e o bloco emissor **deletados**; o tipo entra em `REVOKED_RED_FLAG_TYPES` — nada reescrito, trades `DISCUSSED` intactos (precedente #376). **Fim da contradição do painel:** `shouldEvaluateRR` vira predicado único, o painel deixa de julgar o que o motor absteve, e `riskIsTautological` rotula o stop informado no próprio preço de saída. **Três regras de domínio definidas por Marcio na sessão:** margem de manejo de 2% (`planTolerance` — plano de trade não é contrato de precisão de centavo; o tick do WIN vale R$ 5, e restar R$ 251 para um RO de R$ 252 não é indisciplina); alarme do cockpit **só para aluno com assinatura ativa** (203 dos 588 eram de 6 alunos que já tinham saído); e virada de mão reescrita em **dois estados** — desespero (≤5' da saída, após loss) e perdido (2+ inversões em 30') —, aposentando a janela de 2h com gradiente de severidade que chamava de sinal uma virada 97 minutos depois (16 → 2 na base). UI: `dayMetricTiles` + `DayResultCard` no padrão SSoT do `cycleMetricTiles` — o período **declara, não acusa** —, e `tradePositionInPeriod` situa o trade na sequência do dia em vez de empilhar um painel do período, afirmando a ordinal só quando ela é demonstrável. **Impacto:** `RISCO_ACIMA_PERMITIDO` 82 → 63; alarmes 588 → 385; `DIRECTION_FLIP` 16 → 2; planos incoerentes 6 → 4; maturidade com 4 alunos mudando, **1 gate mudando de estado em toda a base**, zero promoções e zero rebaixamentos novos. **Desvio deliberado:** `computePeriodState` NÃO delega para `dayState` (DEC-AUTO-402-08) — recebe trades já ordenados e carrega uma máquina de estados que o `dayState` não modela; o bug daquele arquivo era a ordenação, a montante, e essa foi corrigida. **Follow-up nomeado:** snapshot do período no `frozenSnapshot` da Revisão (Marcio: "o que congela o trade é a Revisão"); unificar os ~12 agregadores de período e as 3 cópias de `complianceRate`. 11 DEC-AUTO. 4.143 testes no front. Deploy de CF em duas rodadas.  
> **Última atualização (histórica):** 24/08/2026 — v0.40.33: Encerramento #387 v1.83.31 — **o card não pulava ao carregar; ele pulava a cada escrita de Cloud Function** (PR #401, merge `5a7f7da2`). O #387 nasceu descrevendo o primeiro paint: o card Consistência Operacional trocava o layout inteiro por um esqueleto mais baixo enquanto o Sharpe — única métrica com I/O — não resolvia, e crescia quando o conteúdo chegava. Marcio reabriu o assunto depois de uma importação: *"a tela fica piscando enquanto existem ordens que estão chamando a CF"*. A causa em regime era outra camada: `useCycleConsistency` comparava `trades`/`plan` por IDENTIDADE, e cada trade importado produz dois streams de snapshot (o trade regravado pelos triggers, o plano regravado por `updatePlanPl`) — o efeito reentrava a cada um, religava `loading` e redisparava um `getDoc` de Selic por dia operado, sem cache. **Escopo aprovado A+B**, contra o corpo da issue: A sem B esconderia o sintoma que originou a conversa. **Três correções:** o card nasce com a altura final (o grid de `MetricsCards` é `auto-rows-fr`, então qualquer variação estica o vizinho da linha — a altura passa a ser reservada, e não muda nem quando o Sharpe resolve em valor, em "insuficiente" ou em erro); deps por VALOR sobre exatamente os campos que os três helpers leem (`status` fica de fora — nenhum helper o lê e ele é escrito pela cascata, DEC-AUTO-387-05); e memoização de `getSelicForDate` guardando a PROMISE, já que os lookups saem em `Promise.all` e chamadas concorrentes devem compartilhar a leitura in-flight. **Divergência deliberada de espelho** (DEC-AUTO-387-07): o cache entra só no lado web — ele ataca reentrada de UI, inexistente numa invocação de CF efêmera, e replicá-lo arrastaria deploy de Cloud Function para uma issue de layout; contrato de valores idêntico dos dois lados. **Ferramental, dois no-ops silenciosos:** o `cc-close-issue.sh` casava a primeira linha de `#NNN` no registry e teria consumido a reserva 1.83.24 (pulada) em vez da 1.83.31; e o bump do `version.js` era feito por quatro `sed` na forma de objeto literal que o próprio #376 aposentou — gravou 1.83.31 no CHANGELOG e deixou o arquivo em 1.83.30. O segundo agora aborta em vez de silenciar. Terceira vez em três dias que o número da versão sai de sincronia por escrita duplicada. `functions/**` intocado — sem deploy de CF. 11 DEC-AUTO. 3.920 testes no front.  
> **Última atualização (histórica):** 23/08/2026 — v0.40.32: Encerramento #376 v1.83.30 — **passar não levava a lugar nenhum, e o sistema elogiava quem quebrou o plano** (PRs #399/#400, merges `05673e51`/`915f6964`). **O que estava acontecendo:** Marcio abriu o card do Rafael, com 9/9 gates, e disse *"não entendi"*. Tinha razão — **NADA no sistema promovia ninguém**. O motor calculava os gates, gravava `proposedTransition: { proposed: 'UP', blockers: [] }` e parava aí; o `currentStage` só mudava se o ASSESSMENT mudasse, nunca por cumprir os gates. Pior: o fechamento de ciclo comparava o OBJETO `proposedTransition` com a string `'PROMOTE'` (`Step5Check.jsx:312`), valor que o motor nunca produziu — `promotionEligible` era false por construção, e a promoção não aparecia nem no ritual onde a conversa aconteceria. Sexto caso do dia da mesma família: uma coisa que nunca pode acontecer, parecendo apenas nunca ter acontecido. Explica a frase que abriu a issue melhor que os gates explicavam — não é só que ninguém passava, é que **passar não levava a lugar nenhum**. **A decisão de Marcio** (DEC-AUTO-376-07): *"avisa da promoção tanto para o aluno quanto para mim, e eu faço a promoção avisando o aluno"* — subir de estágio é conversa de mentoria, não efeito colateral de recompute. Nasce `promotionReadiness` (ESM + espelho CJS com teste de paridade): UMA regra consumida pelo card do aluno, pelo alerta do mentor, pelo fechamento de ciclo e pela revalidação no servidor — deliberadamente o oposto do padrão que causou os bugs do dia (mesma regra escrita duas vezes, cópias divergindo). Callable `promoteStudentStage` mentor-only, gravando `currentStage` + entrada em `stageHistory` (campos existentes, INV-15 preservada) com de/para, quando e quem promoveu. **A segunda metade** veio de Marcio olhando o WINV26 de +R$ 610: o card listava *"Risco 1,7% excede o máximo do plano"* e, logo abaixo, *"Condução de sizing — o risco continuou dentro do RO"* e *"Execução limpa — é exatamente assim que o plano espera que você opere"*. Ele contestou a violação (*"movi o stop para 250 pontos no 171595"*) e a conferência mostrou que **o sistema já usava esse stop**: 247,5 pontos × 10 contratos = R$ 495 contra R$ 252 autorizados; pelas ordens da corretora, R$ 600. O RO já estava estourado na PRIMEIRA entrada. A violação era real — **o elogio é que era falso**. Causas: `detectCleanExecution` olhava só os padrões comportamentais e ignorava violações de plano; `detectSizingDiscipline` mede risco pelas ordens de stop VIVAS enquanto o compliance mede pelo stop DECLARADO, e a frase afirmava categoricamente algo falso. **DEC-AUTO-376-08:** padrão positivo exige ausência de red flag VIGENTE (revogada não conta; limpa pelo mentor não conta) e `roStatus !== 'FORA_DO_PLANO'` — elogio é presença de sinal bom, não ausência de sinal ruim. **DEC-AUTO-376-09:** setup vazio ou "Indefinido" emite `UNDECLARED_MODEL` (Operacional, sem emoção — é processo) e impede a execução limpa; acertar sem saber por quê não é execução limpa. Não alimenta gate: é sinal para a conversa, não trava. **Impacto:** 33 trades perdem elogio indevido (todos com `RISCO_ACIMA_PERMITIDO` real), 33 ganham modelo não declarado, 108 perfis recalculados, **zero** gates destravados ou travados a mais; segunda passada do recompute deu zero. **Higiene:** `version.display` e `version.full` passam a DERIVAR de `version`/`build` — eram três literais com o mesmo número, e foi o terceiro esquecimento em dois dias (#394 foi o `full`). Corrigir o valor não resolvia; resolver era parar de escrever o número três vezes. **Ferramental:** `check-closing.sh` ganhou guarda de issue reaberta, e ficou registrado que `cc-close-issue.sh` lê a versão de `version.js` em vez da reservada no registry — sobrescreveu o registro do #397 e exigiu correção manual duas vezes neste mesmo issue. 3.903 testes no front, 246 em `functions/`.  
> **Última atualização (histórica):** 23/08/2026 — v0.40.31: Encerramento #376 v1.83.29 — **a dimensão Financeira media performance, não maturidade** (PR #398, merge `92a90ced`). Marcio, olhando o detalhamento da nota: *"eficiência, payoff, consistência e drawdown são indicadores de PERFORMANCE, não de maturidade. Se um aluno teve drawdown de 100% respeitando o limite de sua perda, usou o modelo de entrada, parou o ciclo e se manteve disciplinado sem operar até o fechamento e discussão — como pode ser penalizado?"* **O diagnóstico:** das três dimensões, Emocional e Operacional sempre mediram o que o aluno FEZ; a Financeira media o que ele GANHOU, e era a única assim. Pior, **dois dos quatro componentes eram constantes na base real**: `eScore` (30%) dava **100 para TODOS os 16 alunos com dados** — `evLeakage` é null no servidor e o atalho comparava o número consigo mesmo, virando um sim/não pelo sinal da expectativa —, e `cvScore` (20%) dava **0 para 12 dos 16**, porque a escala foi calibrada para dispersão de resultado MENSAL e o que entrava era dispersão trade a trade, estruturalmente maior. Sobravam payoff e drawdown decidindo, com a nota espremida entre 50 e 65 — por isso `financial-solid` era o segundo maior travador da base. Não era régua rigorosa; era régua quebrada parecendo rigorosa. **A reescrita** (DEC-AUTO-376-04): três componentes de peso igual, todos com dado disponível (`riskPerOperation` e `cycleStop` em 28/28 planos, `roStatus` em 346/351 trades) — risco por operação dentro do RO que o plano autoriza; queda medida contra o **cycle stop do PRÓPRIO aluno**, não contra régua absoluta; e proteção definida antes de entrar. **A decisão que responde ao exemplo** (DEC-AUTO-376-05): queda DENTRO do limite pontua 100 qualquer que seja o tamanho — quanto da folga o aluno consumiu é performance, ter respeitado a folga é conduta; estouro decai proporcional e zera ao dobro do limite. Disciplina de tamanho **não** virou quarto componente: piramidação, sub-sizing, ganância e saída antecipada já entram em F pela modulação comportamental (`netByDimension.F`), e somar de novo dobraria a punição pelo mesmo comportamento. **O sinal inverteu na direção certa:** André Campos 52,6→86,4, Joe Hott 51,4→80,8 e Daniel Barbosa 30,7→68,7 (operam disciplinado e eram punidos pelo resultado); **Antonio Pina 51,3→42** e Wilkinson 39,9→49,2 (0% de proteção e metade dos trades acima do RO). Recompute aplicado: 5 alunos ganham gate; as promoções seguem em 2 porque o que segura os demais agora é constância, compliance e taxa de padrões — tudo conduta. Payoff, expectativa, consistência e drawdown absoluto continuam calculados e visíveis nos painéis, só pararam de decidir promoção. **Ferramental:** `check-closing.sh` ganhou guarda de issue reaberta — ele casava o worktree do branch NOVO com o PR já mergeado e mandava rodar o `cc-close-issue.sh`, cujo passo 8 apaga a branch remota e fecharia o PR aberto em cima dela. 14 casos de teste novos, o primeiro sendo literalmente o exemplo do Marcio; 3.875 no front e 246 em `functions/`.  
> **Última atualização (histórica):** 23/08/2026 — v0.40.30: Encerramento #376 v1.83.28 — **as duas primeiras promoções desde que a mentoria abriu** (PR #397, merge `4eb158b4`). Marcio: *"sair abaixo do alvo não é violação de plano, é comportamento"* e *"precisamos relaxar mais as regras de promoção de maturidade... mais frouxo nessa primeira rodada, aperta depois"*. **A revogação do R:R:** `RR_ABAIXO_MINIMO` era o flag **mais comum da base — 106 de 282, 38% de todas as violações**. Sai da emissão em `compliance.js` e nos três pontos de `functions/index.js`; o número continua calculado e exibido, o que caiu foi a acusação. **A regra que mudou o desenho** (DEC-AUTO-376-01): eu ia rodar um backfill reescrevendo `redFlags` nos 106 trades, e Marcio cortou — *"não se pode modificar trade que já foram discutidos"*. **53 dos 106 estão em `status: 'DISCUSSED'`**, que o `tradeGateway` trata como imutável desde o #269 v2 — mas scripts via admin SDK passam por fora dessa trava. O script foi descartado e a revogação passou a agir no **consumo**: `REVOKED_RED_FLAG_TYPES` em `violationFilter` (ESM + espelho CJS, com teste de paridade), filtrando o tipo em `effectiveRedFlags`. O histórico preserva que a violação existiu, nenhuma métrica volta a contá-la, e o registro da conversa com o aluno fica intacto — mesmo princípio display-time do #282. **A régua:** 39 gates relaxados nas 4 transições (`compliance-95` 95→70, `rule-violation-rate-15` 0,15→0,35, `financial-solid` 70→55, `strategy-8-weeks` 8→6, `payoff-1_2` 1,2→1,0; nos estágios altos os gates de tolerância zero viraram teto ≤2). **O que a revisão de código pegou, e o mais grave estava invisível:** os **40 rótulos** de `functions/maturity/constants.js` ficaram com os números **pré-recalibração** — e é o label do BACKEND que `evaluateGates` grava em `maturity/current` e que as telas renderizam. O aluno leria "Compliance ≥ 95%" enquanto o motor cobrava 70: régua nova invisível, régua velha na cara dele. Não havia teste de paridade; agora há, cobrindo id, métrica, operador, threshold **e label**. **O backfill descartado também estava errado por dentro** — não escrevia `hasRedFlags`, então os trades que ele "limpava" seguiam contando como violação (`hasEffectiveRedFlags` cai nesse escalar quando o array esvazia), e uma escrita só em `redFlags` não casa nada em `tradeChangeScope`, ou seja, não dispararia recompute nenhum. **Constância gap-blind:** `sortedWeeks` só tem as semanas COM trade, e o run contava adjacência nessa lista esparsa — 4 semanas de janeiro + 4 de julho valiam **8 "consecutivas"**; sob a janela de 45 dias isso ficava estruturalmente limitado, mas desde o #396 (histórico completo) virou ilimitado. Agora exige vizinhança de calendário com tolerância de 1 período. **Dois gates impossíveis por construção, a mesma doença que abriu o issue** (DEC-AUTO-376-02): (1) `computeRuleAdherenceRate` comparava com `'OK'`, valor que **nunca existiu** nos dados — o domínio é `CONFORME`/`NAO_CONFORME`/`FORA_DO_PLANO` — devolvendo 0 para todo trade real, e o `Step5Check` **sobrescreve** o gate de compliance com esse 0, de modo que relaxar a régua não mudaria nada no fechamento de ciclo; só as fixtures usavam `'OK'`, o que escondia o bug; (2) `initialBalance` **não existe em nenhum dos 27 planos** — o campo de capital do produto é `pl` —, então `maxDDPercent` era null para a base inteira e o gate de drawdown nunca passava, inclusive na transição 1→2, onde estão os alunos novos. **R:R saiu de mais três superfícies** onde ainda era quebra de plano: marcador no Extrato — que o mentor **não tinha como limpar**, porque o botão nasce de `effectiveRedFlags` e o flag deixou de existir —, evento `RR_FORA` no razão do plano e 1/3 da nota de aderência do snapshot mentor/IA. **Recompute em produção:** **Rafael Perilo 7/9 → 9/9 e Wilson Fu 6/9 → 9/9**, ambos com `proposedTransition: UP` e `blockers: []` — as primeiras promoções da história da mentoria. Henrique Yotani 4/9 → 8/9 (falta só constância: 3 semanas, precisa 6), German 4/9 → 7/9, Thiago 5/9 → 7/9; **nenhum aluno regrediu**. Segunda passada do recompute deu zero mudanças, provando idempotência. Testes: 3.870 no fuso local **e** em `TZ=UTC`, 246 em `functions/`.  
> **Última atualização (histórica):** 23/08/2026 — v0.40.29: Encerramento #394 v1.83.27 — **exposição que o aluno fechou parou de travar progressão, e a revisão de código pegou oito defeitos, cinco meus do próprio dia** (PR #395, merge `71be0203`). **O fix original:** o #375 alargou `UNPROTECTED_SIZE` de "falta de cobertura em quantidade" (sempre grave) para "qualquer intervalo nu acima de 20s", onde MEDIUM significa que o aluno **recolocou** a proteção. Nem `gateInputs` nem a contagem de violações olhavam severidade — um stop colocado 30s após a entrada travava igual a nunca ter colocado. Impacto medido: aluno `41Dhjb` foi de 21,4% para 14,3% de padrões e **cruzou o teto de 15%** do gate Reativo→Metódico. **A revisão, e é aqui que a lição mora:** (1) o próprio fix, como escrito, desligava o gate **inclusive no caso grave** — `buildBehaviorProfile` monta detecções sem `severity`, então `travaProgressao(codigo, undefined)` respondia "não trava" sempre, e um trade que nunca colocou stop saía com `gateInputs: []`; (2) **em produção**, `detectHesitation` fabricava hesitação a partir de ordem sem data, porque `orderInstantMs` devolve `null` e `null < n` coage para `0 < n` — o mesmo defeito do #388 por outra porta; (3) **em produção**, `detectStopPanic` **nasceu inerte** ao descartar o cancelamento do OCO olhando só o último stop tocado, que num bracket é sempre esse; (4) o mesmo detector contava **apertar** o stop como pânico; (5) `detectLateExit` confundia substituir com remover, acusando trailing stop; (6) `detectGreedCluster` comparava o trade âncora consigo mesmo, excluindo do cluster o vencedor que o abriu; (7) três chaves duplicadas em `EMOTION_MAPPING`, erro de lint que passou porque o build não roda ESLint; (8) `version.full` divergia de `version` — o selo que o mentor fotografa mostrava a versão anterior. **DEC-AUTO-394-01:** ensaio contra produção não se lê pelo número, se lê pela CAUSA do número. Duas vezes no dia um "zero marcações" foi interpretado como base limpa e era detector quebrado; o ensaio agora conta **pré-condições**, e o zero legítimo ficou demonstrado (58 dos 351 trades têm ordens da corretora, 24 têm perna de stop, 1 tem stop tocado antes da saída, 3 têm entradas espaçadas, 0 tem perda com stop cancelado). **Recompute:** 5 perfis, todos MEDIUM saindo do gate; os HIGH — inclusive um trade que ficou nu 495s de 495s — continuam travando. Segunda passada do recompute deu zero, provando idempotência. 12 testes novos; suíte verde no fuso local e em `TZ=UTC`.  
> **Última atualização (histórica):** 23/08/2026 — v0.40.28: Encerramento #392 v1.83.26 — **quatro análises de comportamento estavam escritas e desligadas há dois meses** (PR #393, merge `6da8e7f5`). Marcio: *"ninguém tá passando porque ninguém está acreditando no processo e estou perdendo aluno."* **O que se descobriu:** o motor do servidor foi criado em 04/06 (#301) declarando no próprio cabeçalho ser a "mesma lógica" do motor cliente — **vieram 10 de 15 detectores**, e ninguém corrigiu a frase. A decisão da época (DEC-AUTO-301-01) justificava não portar porque "a maturidade server-side não consome comportamento por trade" — premissa que **caducou**: hoje o card do trade, o gate de progressão e a taxa de padrões leem exatamente esse registro. Resultado: `AVERAGING_DOWN`, `STOP_PANIC`, `LATE_EXIT`, `GREED_CLUSTER` e `FOMO_ENTRY` escritas, testadas e **nunca aplicadas a um trade**. **Decisão de Marcio:** ligar as quatro primeiras; **descartar FOMO na entrada** — *"entrada a mercado é perfeitamente viável e nem sempre é corrida atrás do preço"*. **Três falsos positivos fechados ANTES de subir** (DEC-AUTO-392-01): rodar os detectores contra a base real marcou 6 trades, incluindo o de +R$ 520 que custara o dia inteiro para limpar — proteção cancelada **no alvo** virava "pânico no stop"; duas pernas de entrada com **3 segundos** de diferença viravam "piramidação"; e execução correlacionada ao trade errado, **uma hora depois** de ele fechar, também. Guardas: cancelamento simultâneo à saída é OCO e não decisão; entradas da mesma leva (<60s) são entrada escalonada; execução fora da vida da posição não conta — cada uma com teste do caso real que a originou. Impacto após as guardas: **zero trades marcados na base atual**; as quatro valem para o que vier. **Faxina junto:** o campo `shadowBehavior` parou de ser gravado (escrito a cada análise, lido por ninguém desde o #301, e ainda disparava `onTradeUpdated` por tabela); saíram os painéis órfãos `ShadowBehaviorPanel`, `ExecutionPatternsPanel` e `executionPatternsDisplay`; e o ramo `byTrade` do motor ESM foi alinhado ao espelho CJS, que já o devolvia vazio — a divergência "intencional" era o que permitia um fix chegar num lado e não no outro. 21 testes novos, suíte verde no fuso local **e** em `TZ=UTC`.  
> **Última atualização (histórica):** 22/08/2026 — v0.40.27: Encerramento #389 v1.83.25 — **mesma origem, mesmo resultado** (PR #390, merge `06845123`). Regra de Marcio, depois do incidente do dia: *"se a origem é igual, o resultado deveria ser o mesmo, e não concordo que o fato de eu dar feedback recalcule o comportamento — se algum parâmetro muda, ele precisa estar na minha tela antes de eu submeter"*. **Caso:** trade WINV26 de 21/08, +R$ 520, revisado limpo às 14:11:11 e regravado com "Hesitação" às 14:11:21, pelo próprio ato de enviar o feedback; o aluno abriu na versão nova. **Diagnóstico que corrigiu uma tese minha:** não era divergência de motor na tela — o mapeamento mostrou que **todas as telas, mentor e aluno, leem o mesmo campo** `trade.behaviorProfile`, e que o motor cliente de 15 detectores é **código morto** (nenhum importador de produção). Era o mesmo documento em estados diferentes no tempo. **Fase 1 — a janela era um input escondido:** o botão "Recalcular Comportamento" calculava com janela de um dia e o gatilho automático com o histórico completo; `recomputeBehaviorProfiles` ganhou `writeScope`, calculando sempre com o conjunto completo e gravando só o recorte pedido. Ressalva registrada por honestidade: ao verificar o peso real, `adjacent` filtra por mesmo dia (`buildBehaviorProfile.js:189`), então a dependência de janela era **menor** do que eu havia afirmado — a Hesitação veio do fuso (#388), não da janela. **Fase 2 — o gatilho virou módulo testável:** a regra de "o que mudou de verdade" saiu de dentro de `onTradeUpdated` e virou `functions/shared/tradeChangeScope.js`. Era ali que o #383 a quebrou, comparando um mapa por identidade e abrindo o guard para sempre. Enviar feedback, comentar, anexar reflexão ou marcar como discutido **não recalculam**; stop, entrada, saída, quantidade, lado, emoção, resultado, plano, horário e dispensa de violação **recalculam**. 23 testes novos, suíte verde no fuso local **e** em `TZ=UTC`. **Recompute:** 2 perfis corrigidos, incluindo a "Hesitação fóssil" do trade de referência, que voltara porque o loop o regravou com código velho antes do deploy do #388. **Lição de deploy registrada:** `firebase deploy --only functions` pulou silenciosamente a função corrigida duas vezes (`Skipped — No changes detected`) com o código correto em disco; só o deploy direcionado (`--only functions:onTradeUpdated`) subiu. Deploy só conta como feito com `Successful update operation` na função que interessa. **Aberto para decisão:** #391 (veredicto relacional não deveria morar dentro do trade — dois cards, atômico e de sessão).  
> **Última atualização (histórica):** 22/08/2026 — v0.40.26: Encerramento #385 v1.83.23 — **o card morria inteiro porque um chunk não carregou** (PR #386, merge `6e77b4b2`). Marcio: *"pode checar por que não foi possível carregar métrica do ciclo no card consistência operacional na minha conta"*. **O cálculo nunca foi o problema:** simulados contra os dados reais dele (16 trades, `pl: 30.000`, janela 04→21/08), os três helpers respondem certo — CV 1,75 · MEP 0,150 · MEN −0,111 · Sharpe 2,12 — e a base de Selic está completa, 665 documentos até 21/08, com a query exata do `getSelicForDate` funcionando. **O que quebra é o carregamento do módulo:** `computeCycleSharpe:140` faz `await import('../marketData/getSelicForDate.js')`, e no bundle isso vira um **chunk próprio** (`dist/assets/getSelicForDate-<hash>.js`, confirmado no build). O hash muda a cada build que toca o módulo; aba aberta **antes** de um deploy pede o chunk pelo nome antigo, recebe 404 e o `import()` rejeita. Foram três deploys em 21/08 (#375, #381, #383) — exatamente a janela em que ele viu. **Os dois defeitos nossos, que transformam o gatilho em card morto:** (1) `getSelicForDate` já tinha fallback interno para falha de rede/permissão, mas ele vive **dentro** do módulo — sem o módulo, nunca é alcançado; agora o import dinâmico é guardado e degrada para a taxa de fallback, sinalizada em `fallbackUsed`; (2) o hook zerava `...NULL_METRICS` no `.catch`, e CV/MEP/MEN — puros, síncronos, sem dependência de Selic — sumiam junto com o Sharpe: o aluno perdia três métricas boas por causa de uma. Agora sobrevivem, a mensagem de erro só aparece quando **nada** foi calculado, e o card orienta a recarregar em vez de só sumir. **Lição estrutural:** todo `import()` dinâmico no front é uma dependência de rede que envelhece a cada deploy — precisa de fallback no ponto de chamada, não só dentro do módulo importado. 5 testes novos, 253 arquivos verdes.  
> **Última atualização (histórica):** 21/08/2026 — v0.40.25: Encerramento #383 v1.83.22 — **uma SSoT de R:R; três fórmulas viraram uma** (PR #384, merge `f4cb25f9`). Marcio, depois do #381: *"eu não quero defeito, quero que o sistema leia e apresente os cards corretamente."* O #381 blindou os detectores, mas o escalar `trade.rrRatio` continuava errado no banco e continuava sendo lido por dashboard, extrato e ledger — **9 de 161 trades com stop divergindo do R:R real**, inclusive losses que tomaram o stop cheio (−1R) gravados como −0,2 e marcados CONFORME. **Causa 1:** `useTrades.js:365` mantinha uma terceira implementação da conta, dividindo por `tickerRule.pointValue` — campo que é **null** no WIN, onde a especificação vem como `tickSize: 5`/`tickValue: 1` — com fallback `|| 1`: `520 / (125 × 1 × 10) = 0,42`, o número exato da tela. **Causa 2:** o mesmo bloco gravava `{ rrRatio, rrAssumed }` num **write separado**, por cima do que a CF acabara de calcular; era por isso que o documento tinha `rrRatio: 0.42` e `compliance.rrStatus: 'CONFORME'` ao mesmo tempo, sem nada reconciliá-los. **Causa 3:** `tickerRule` estava fora de `complianceFields`, então a especificação carimbada pelo import **depois** da criação não disparava recálculo. **Correção (DEC-AUTO-383-01):** `realizedRR` — geometria de preço, sem dependência de `tickerRule`/`pointValue`/`result` — vira SSoT em `compliance.js` e em `functions/shared/realizedRR.js`, consumida por compliance, detectores e espelhos; o bloco cliente foi **removido** (o write único já dispara `onTradeUpdated`, que grava o par junto — um escritor só); e sem `exit` só converte por tick **com a especificação completa**, nunca assumindo 1. **Backfill:** 10 trades corrigidos, com a regra "onde já existe valor, torná-lo verdadeiro; onde não existe, não inventar" — criar entrada nova mudaria as médias do dashboard. Auditoria pós-fix: **0 divergentes** em 161. Teste de invariante (`rrRatio == realizedRR` em seis cenários, mais `expect(realizedRRShadow).toBe(realizedRR)`) é o que impede a quarta implementação.  
> **Última atualização (histórica):** 21/08/2026 — v0.40.24: Encerramento #381 v1.83.21 — **o card acusava saída antecipada num trade que passou do alvo** (PR #382, merge `7d61c4b8`). Marcio, olhando o painel logo após o #375: *"fiz uma posição que tomou 125 de risco para 250 de alvo, saí antecipado por quê?"* A conta dele estava certa e o detector, errado: saída em 174.290 contra alvo de 174.280 — **dez pontos além**, R:R de **2,08**. O card exibia `RR 0.42` e ainda emparelhava "Saída antecipada · Alta" com "Alvo atingido (2:1)" no mesmo trade; pior, depois do #375 a saída antecipada virou a família dominante e passou a **ditar o confronto emocional** ("a execução sugere Medo"). **Causa:** `detectEarlyExit` não calculava nada — lia o escalar `trade.rrRatio` gravado pela CF de compliance, e o valor no documento era o que a fórmula produz **sem `tickerRule`**: R$ 520 convertidos em 52 pontos em vez de 260. O mesmo documento trazia `compliance.rrStatus: 'CONFORME'` contra alvo 2 — incompatível com 0,42, prova de que objeto e escalar foram escritos em momentos diferentes e o escalar envelheceu. **Correção (DEC-AUTO-381-01):** `realizedRR` deriva o R:R da **geometria de preço** — `(saída − entrada) ÷ |entrada − stop|` — que é a mesma régua com que `detectTargetHit` já calculava o alvo. Com os dois na mesma base, a contradição fica **impossível por construção**, e o número deixa de depender de `tickerRule`, de `result` ou de qualquer campo que envelheça; mesmo princípio do #373. **Segundo defeito no mesmo caminho:** os detectores liam `trade.planRR`, campo que **nunca existiu** no modelo (o anexado é `planRrTarget`), e o `?? 2.0` mascarava a ausência — todo aluno era julgado contra 2:1 qualquer que fosse o `rrTarget` do plano dele. AP-07 vivo em produção. **Armadilha evitada:** `Number(null)` é `0` e passa por finito; sem guarda, trade sem stop viraria "risco = a entrada inteira" e R:R 0,00 — lido como saída antecipada gravíssima. É a mesma cilada que o #373 pegou em `rrBreakdown`, e este issue quase repetiu: barrada por teste. **Recompute:** 83 perfis atualizados. O trade de referência ficou com três padrões, todos positivos (`SIZING_DISCIPLINE`, `TARGET_HIT`, `CLEAN_EXECUTION`), `gateInputs: []` e confronto `ALIGNED` sem banner — declarou Calmo e executou limpo. 10 testes novos, um deles exigindo que `EARLY_EXIT` e `TARGET_HIT` **nunca coexistam** em quatro cenários.  
> **Última atualização (histórica):** 21/08/2026 — v0.40.23: Encerramento #375 v1.83.20 — **a proteção existia; o sistema é que a lia três horas fora do lugar** (PRs #379/#380, merges `080ab2d7`/`8d929a63`). Marcio: *"por que todos os trades que estou importando estão vindo como sem stop?"* **Causa raiz:** `orders` guarda instante ingênuo (`"2026-08-21T11:27:51"`) e `trades` guarda com offset explícito desde o #285/#292 (`"...-03:00"`); `new Date()` lê string sem fuso no fuso DO PROCESSO, e a Cloud Function roda em UTC. A ordem virava 11:27:51Z contra um trade em 14:27:51Z, `liveStopsAt` descartava toda perna como cancelada 3h antes da saída, e **todo** trade com ordem correlacionada saía com `UNPROTECTED_SIZE` HIGH e cobertura zero — o gate travando progressão de estágio em posição integralmente protegida. Reprodução determinística: mesmos dados, `TZ=UTC` emite o alerta e `TZ=America/Sao_Paulo` não emite nada. Colateral achado no caminho: `detectStopBreakevenTooEarly` **nunca disparava**, pelo mesmo desvio invertido. **Hipótese refutada:** o issue nasceu apontando dedup de pernas irmãs em `protectiveLegsOf`; o probe em produção mostrou `submittedAt` e `externalOrderId` distintos e derrubou a tese — registrado no corpo do issue em vez de apagado. **Regra de negócio nova (Marcio, 21/08):** *cancelar o stop não é o problema; o problema é a posição ficar sem stop enquanto está aberta — cancelar e criar outra é condução, o sistema mostra e não acusa*. Nasce `protectionTimeline`, que cruza contratos **abertos** × **cobertos** no tempo e devolve janelas de exposição; janela ≤ **20s** é troca de ordem (número dele). `UNPROTECTED_SIZE` passa a medir a janela — HIGH quando nunca houve proteção, quando a exposição chega até a saída ou toma a maior parte da posição; janela curta e fechada vira MEDIUM. **Emoção derivada do verbo** (DEC-AUTO-375-01): quem **retira** a proteção carrega `HOPE` (Esperança) ou `DENIAL` (Negação, quando ainda aumenta a posição); quem **entra nu** não carrega emoção — é processo, e o gate já diz. Vingança foi descartada: é reação a prejuízo consumado, e aqui o prejuízo ainda não foi aceito. **Painel:** `TradeOrdersPanel` checava `CANCELLED` antes de qualquer teste de proteção — bracket morto pelo OCO no alvo virava "Cancel" e o cabeçalho anunciava "Sem stop"; agora consome a mesma leitura do motor, com quatro estados de proteção, badge de três estados e faixa de exposição. **Confronto:** padrão de gate sem emoção deixou de concorrer a "a emoção do trade" — era o que imprimia a palavra `null` na cara do aluno. **Recompute:** 42 perfis atualizados em produção; 6 trades perderam gate falso (incluindo o de referência, que zerou `gateInputs`) e **14 ganharam gate verdadeiro** — um aluno entra a mercado e só manda o stop minutos depois (11m31s de posição descoberta num trade de 15m47s), padrão que o detector antigo não via porque só olhava a cobertura no instante da saída. 35 testes novos; o de fuso falha se a correção for revertida, em qualquer máquina. **Pendente:** deploy das CFs (o recompute rodou com o código corrigido localmente, mas o trigger em produção segue no código antigo até o deploy).  
> **Última atualização (histórica):** 21/08/2026 — v0.40.22: Encerramento #373 v1.83.19 — **o R:R deixou de ser um múltiplo abstrato** (PR #374, merge `1e9ee0cb`). O painel dizia apenas `RR 1.2x abaixo do mínimo (2x)`: nem quanto dinheiro, nem contra o quê. Marcio: *"RR precisa mostrar dentro do valor do plano × resultado — ou pelo menos precisa mostrar os dois. Ainda não sei como é calculado esse comportamento."* **O cálculo** (`compliance.js:97-110`, com stop e sem alvo) usa o resultado realizado contra a distância do stop, em pontos. No trade de 20/08 (WINV26 LONG 10, +R$ 610): risco de 247,5 pts = **R$ 495** (1,65% do capital), ganho de 305 pts, R:R **1,23x** — viola o mínimo de 2x. Contra o RO que o plano autoriza (R$ 30.000 × 0,84% = **R$ 252**), o mesmo trade é **2,42x** — dentro. Os dois estão certos e respondem perguntas diferentes: *o ganho valeu o que arrisquei?* e *o ganho valeu o risco permitido?* E o confronto revela o que a tela não dizia: as DUAS violações do trade (risco acima do RO e R:R abaixo do mínimo) têm a mesma causa — posição dobrada sem encurtar o stop na proporção; o risco subiu e, sendo ele o denominador, o R:R desabou. Corrigido o sizing, as duas somem juntas. **Decisão de Marcio:** mostra os dois em dinheiro, **violação continua no risco tomado** — é o que aconteceu com o dinheiro dele; o R:R contra o plano é referência do que o trade teria sido no sizing correto (DEC-AUTO-373-01). **Implementação:** helper puro `rrBreakdown(trade, plan)` e bloco dentro da caixa de violações do `BehaviorPanel`, decimais em pt-BR. **Display-time** (DEC-AUTO-373-02): derivado do trade + plano vigente, sem congelar snapshot nem reescrever `redFlags` gravadas — mesmo princípio da SSoT de tiles do #282, e dispensa recompute de histórico por causa de exibição. **Bug que o teste pegou:** `Number(null)` é `0` e passa em `Number.isFinite`, então trade sem stop informado calculava risco de R$ 343.685 — a entrada inteira virando distância. Ausência não é zero. 15 testes novos; 3.731 no front. Sem deploy de CF (não tocou `functions/`).  
> **Última atualização (histórica):** 21/08/2026 — v0.40.21: Encerramento #371 v1.83.18 — **o import apagava o stop do aluno e o motor cobrava um risco que nunca existiu** (PR #372, merge `34e1ee99`). Marcio auditou o painel de comportamento dos próprios trades e trouxe três perguntas; as três eram defeito. **(1) Risco medido pelo preço errado:** `stopRiskBreakdown` lia a perna de proteção pelo preço ENVIADO mesmo quando a ordem executou. No trade de 20/08 (SHORT 5 WINV26) o extrato traz `Preço 170.280 | Preço Médio 170.130` — limite com folga para garantir preenchimento. O detector cobrou 400 pts × R$ 0,20 × 5 = R$ 400 contra RO de R$ 252 e emitiu `RISK_OVER_RO` HIGH, que **trava progressão de estágio**; o risco real foi R$ 250, dentro do plano. Como `RISK_OVER_RO` mapeia para GREED, o confronto emocional ainda acusava "declarou Ansioso, execução foi de Ganância" sobre um número inexistente. Agora `_price` CLASSIFICA a perna (alvo × proteção) pelo enviado e `_riskPrice` MEDE pelo executado, só quando houve execução real (DEC-AUTO-371-01). Perna viva ou cancelada segue pelo enviado — ali o risco está parado no book, que é o alerta do #359. **(2) Dado do aluno é imutável pelo import (DEC-AUTO-371-02, regra de Marcio):** `buildEnrichmentPayload` devolvia `stopLoss: null` sem proteção encontrada e o patch gravava por cima do que o aluno digitou. Ausência de dado não é dado: o campo nem entra no payload; divergência vira registro (`importDivergences`); preenchimento só no vazio, com origem marcada (`stopLossSource`). Ajuste explícito no modal continua mandando, inclusive para limpar (`stopLossFromUser`) — a proteção é contra o import decidir sozinho, nunca contra o aluno editar o que é dele. **(3) Reconstrução e detector discordavam:** `associateNonFilledOrders` só via proteção em `isStopOrder`, enquanto o detector também reconhece a perna adversa emitida como Limite (#359/DEC-AUTO-242-01) — que é como esta corretora emite. A mesma ordem lida de dois jeitos foi o que produziu o `null`. Alinhados, com a guarda de que proteção nasce COM a posição: ordem anterior à entrada é tentativa abortada (#369), não bracket (DEC-AUTO-371-03). **Passivo corrigido:** `issue-357-recompute-behavior.mjs` recalculou 11 perfis de Marcio — **8 `RISK_OVER_RO` e 3 `UNPROTECTED_SIZE` eram falsos e sumiram**; 2 viraram `SIZING_DISCIPLINE` (valência positiva: aumentou posição mantendo o risco no RO) e 1 `CLEAN_EXECUTION`. Restaram 2 findings de gate, legítimos. **Erros meus corrigidos no caminho:** usei o preço executado também para classificar a perna (quebrou 7 testes: mudou o que é alvo e o que é proteção) e usei `filledPrice` sem checar se a ordem executou — um fixture com ordem CANCELLED carregando `filledPrice` expôs. Dois testes antigos reescritos, ambos fixando o comportamento que a regra elimina. 15 testes novos; 3.716 no front, 246 em functions. **Deploy:** 43 funções atualizadas — ao contrário do encerramento do #369, em que o mesmo comando reportou `Skipped` para as 42 e nada subiu (DT-046).  
> **Última atualização (histórica):** 21/08/2026 — v0.40.20: Encerramento #369 v1.83.17 — **as ordens que você monta e desmonta agora pertencem ao trade vizinho** (PR #370, merge `39dabdca`). Marcio, testando o motor comportamental, colocou ordens e as retirou — e descobriu que aquilo não era registrado em lugar nenhum. Dois pontos descartavam a evidência pelo mesmo pressuposto errado, o de que ordem cancelada pertence a uma posição que existiu AO MESMO TEMPO: `associateNonFilledOrders` só anexava ordem que caísse DENTRO do intervalo de uma operação (`if (!bestOp) continue`), e `correlateCancelledOrders` exigia interseção entre a vida da ordem e a vida do trade. O caso real é o oposto — a ordem pertence ao que veio DEPOIS, ou ao que veio ANTES quando não veio nada depois. Resultado: `HESITATION_PRE_ENTRY`, que existe no motor desde o #208 com peso 5, praticamente nunca disparava no padrão mais comum. **Atribuição:** próxima operação do mesmo instrumento → operação anterior do mesmo instrumento → último trade do dia. A janela de 2h é o critério de **aderência** e vale nos dois sentidos (decisão de Marcio, 21/08, corrigindo desenho anterior que só limitava para a frente): ordem a mais de 2h de qualquer trade não pertence àquele momento operacional, fica sem vínculo e morre pela INV-29 — em vez de virar evidência forçada pendurada num trade distante. **Leitura por tempo, derivada dos timestamps — sem campo novo, sem INV-15:** ≤5min antes da entrada e 5–30min → `HESITATION_PRE_ENTRY` (peso 5, com `pattern` TRIGGER/HESITATION); 30min–2h → `RECONSIDERATION_PRE_ENTRY`; depois da saída → `ABORTED_ATTEMPT_POST_TRADE`. **Os dois eventos novos NÃO pontuam** (DEC-AUTO-369-03): ficam fora de `EVENT_PENALTIES` e entram na taxonomia com valência positiva, porque desmontar uma ordem e esperar meia hora, ou montar depois do trade e recuar, é autocontrole — penalizar ensinaria a entrar rápido e a não recuar. Depois de prejuízo a tentativa recuada sobe para `MEDIUM` e carrega `afterLoss` na evidência: visível, sem tirar ponto. **Paridade:** espelhado em `executionBehaviorMirror`, `behavioralTaxonomyMirror` e `emotionalAnalysisMirror`; um invariante da taxonomia pegou erro no caminho (padrão de valência positiva não carrega `severityDefault`). **Mudança de comportamento registrada:** o teste que documentava `correlateCancelledOrders` NÃO vinculando ordem sem convivência foi reescrito para a regra nova, com irmão cobrindo o limite de 2h; confidence 0.6 nesses casos, porque vizinhança é heurística mais fraca que convivência. 19 testes novos; 3.701 no front, 246 em functions. **Achado operacional:** o `firebase deploy` disparado pelo `cc-close-issue.sh` reportou `Skipped (No changes detected)` para as 42 funções mesmo com três arquivos alterados — o redeploy manual de `onTradeUpdated` retornou `Successful update`, provando que o código novo não havia subido. Deploy completo refeito à mão. **Encerramento:** o script abortou em [7/8] porque o worktree usou nome fora do padrão `issue-NNN`; limpeza manual de worktree e branches.  
> **Última atualização (histórica):** 20/08/2026 — v0.40.19: Encerramento #366 v1.83.15 — **o import de ordens gravava antes de o aluno decidir** (PR #367, merge `bce355f6`). Sintoma: `Missing or insufficient permissions` na Revisão de Operações, com o relato de que a importação anterior tinha sido *descartada*. Tinha mesmo — e o descarte não descartava nada. `handleStagingConfirm` movia o lote inteiro de `ordersStagingArea` para `orders` uma tela ANTES da decisão por operação; o que fosse descartado depois já estava gravado, e o cliente não apaga `orders` (`firestore.rules`: `allow delete: if false`). Operação deixada em `pending` sumia sem registro, porque não entra em bucket nenhum no roteamento. E o marcador de descarte (`persistDiscardedOrders`) era um `update`, negado pelas rules e engolido por `console.warn` desde o #156 Fase E: **nunca funcionou**. O erro em si veio do #362 — id determinístico + `set(merge)` sobre doc existente é *update* para as rules, e `writeBatch` é atômico, então o lote inteiro morria. **Fix:** a gravação migrou para o passo final, em `persistImportDecisions` (`src/utils/orderImportPipeline.js`), na ordem obrigatória **ordens → trades → enriquecimento** — contrato, não estilo: `linkOrdersToCreatedTrade` roda dentro de `onTradeCreated` e desiste se não achar as ordens do batch, e inverter reintroduziria o #351 (194 de 198 órfãos medidos). Descartada e sem-decisão nunca chegam em `orders`. **Sem tocar em `firestore.rules`** (DEC-AUTO-366-01): o ingest pula o doc que já existe e toda escrita vira `create`, que já era permitido — abrir `update` seria pior, porque `allow create` não checa `studentId` e o id é derivável do UID do aluno (`${studentId}_${orderKey}`), sem harness de teste de rules no repo. **Porta de entrada:** dedup bi-chave `eid:`+`comp:` (`orderDedup.js`), necessária porque doc pré-#362 não grava `externalOrderId` e as duas chaves nunca colidem — chave única deixaria todo o legado passar; duplicatas chegam pré-excluídas no preview, com "importar mesmo assim" explícito. **Sem ghosts:** confirmação ao fechar com rascunho vivo, `beforeunload` para refresh/aba, X travado durante a gravação, e o rascunho virou visível e retomável no painel (card + manager espelhando o CSV) — `stagingBatches` existia no hook e não tinha um único leitor. **Achados corrigidos no caminho:** `BATCH_SIZE` 450 no ingest com `set`+`delete` por ordem = 900 operações, acima do teto de 500 do Firestore; retry após falha na criação de trades era impossível (`ingestBatch` lançava com o staging já vazio); ordens de operação **enriquecida** nunca eram ligadas ao trade, porque `enrich` não passa por `onTradeCreated` — agora o vínculo vem da decisão do aluno; retomada quebraria a correlação inteira por causa do `_rowIndex` não persistido (`stagingDocsToOrders` reatribui); voltar da revisão e reconfirmar criava um segundo lote e abandonava o primeiro; `lowResolution` do parser era descartado e o badge nunca aparecia; em view-as o staging gravava o UID do mentor como dono. **Delta INV-15:** `ordersStagingArea.importTimezone` — sem o fuso persistido a retomada não refaz a reconstrução, e repedir deixaria o aluno reconfirmar fuso diferente do original (a regressão que #285/#292 fecharam). **Passivo:** dry-run mediu 519 ordens abandonadas em 3 lotes (17 do lote que originou o issue; 500 de um import de fev-mar abandonado em 28/05, cujo aluno não tem nenhum trade no sistema; 2 canceladas soltas), nenhuma ligada a trade vivo; `scripts/issue-366-cleanup-abandoned-batches.mjs` aplica com log do doc inteiro como caminho de restauração. 3.676 testes verdes. **Dívidas registradas:** DT-043 (fills múltiplos colapsam num doc só), DT-044 (`cleanupOrphans` tenta delete em `orders`, caminho morto), DT-045 (`useOrders` sem índice composto, roda sempre pelo fallback).  
> **Última atualização (histórica):** 20/08/2026 — v0.40.18: Encerramento #363 v1.83.14 — **apagar um trade não apagava nada em volta dele** (PR #365, merge `cb6dfc5e`). Medição em produção 19/08: 5.129 notificações órfãs (89% da collection, todas não lidas), 26 ordens, 10 referências em `reviews`. Só `movements` estava limpo — a única dependência que o `deleteTrade` do cliente sabia apagar. A limpeza morava no lugar errado: no cliente, que não alcança `/orders` (`firestore.rules`: `allow delete: if false`), e espalhada por três caminhos de deleção com coberturas divergentes (`deleteTrade`, `deletePlanCascade`, `deleteStudentData`), nenhum completo. **Fix:** cascata em `functions/trades/cascadeDeleteTradeRefs.js`, chamada do `onTradeDeleted` como etapa 3 — o único ponto por onde toda deleção passa, com admin SDK. Apaga `movements` → `orders` → `notifications` → prefixo `trades/{tradeId}/` no Storage, nessa ordem, cada alvo em `try/catch` próprio (INV-03). **Apaga, não desvincula** (DEC-AUTO-363-01, decisão de produto de Marcio): perde-se o rastro da execução da corretora, e o #362 é a rede de segurança — com a reimportação idempotente, reimportar o extrato reconstrói as ordens. **Storage:** os prints HTF/LTF nunca foram apagados; `TradesJournal` chamava `deleteTrade(id, htfUrl, ltfUrl)` contra uma função de UM argumento e os dois URLs eram descartados em silêncio — apagar por prefixo dispensa a fiação e alcança o que a tela não conhecia (DEC-AUTO-363-05). **Fase C matada** (DEC-AUTO-363-04): as 10 referências vivem no `frozenSnapshot`, congelado por contrato do #259 — apagá-las reescreveria o que uma revisão publicada dizia na época; snapshot é registro histórico, não ponteiro vivo. `drawdownHistory` fora pelo mesmo princípio (#52). **Achados do code-review incorporados:** `useTrades.deleteTrade` para de apagar `movements` no cliente — apagá-los ANTES do trade corrompia o saldo quando o delete era negado (ciclo selado #259: movements sumiam, `onMovementDeleted` estornava o `currentBalance`, o delete estourava permission-denied e o trade seguia vivo sem o movement, silenciosamente) (DEC-AUTO-363-03); `deleteDocsInBatches` passa a carregar `err.deleted`, porque falha no meio da paginação reportava 0 apagados com milhares já fora do banco, invertendo o log — única evidência que a deleção deixa; e o helper foi extraído para `_shared/batchDelete.js` e adotado pelas TRÊS cascatas (plano, conta, trade), que eram três cópias do mesmo loop — a origem da divergência de cobertura. **Passivo liquidado:** `scripts/issue-363-cleanup-orphan-refs.mjs` apagou 5.137 notificações órfãs em produção (20/08); `notifications` caiu de 5.801 para 664 docs e as três collections ficaram com zero órfãos, confirmado por leitura independente. O script aborta se `trades` vier vazia (credencial/projeto errado faria tudo parecer órfão) e grava o doc inteiro de cada exclusão como caminho de restauração. 16 testes novos; 230 em functions, 3634 no front. **Em aberto:** a cascata apaga notificação de alerta do mentor a partir de um delete que o aluno pode disparar — `firestore.rules:303` só permite mentor apagar `/notifications`, mas o trade (evidência primária) já ia junto de qualquer forma; se virar problema, é escopo de imutabilidade (#184). E não há dead-letter para falha da cascata: sem o doc do trade não há re-drive, e retry no trigger estornaria PL em dobro (etapas 1 e 2 não são idempotentes) — gravar registro de falha exige aprovação INV-15.  
> **Última atualização (histórica):** 19/08/2026 — v0.40.16: Encerramento #357 v1.83.9 — **o motor acusava pânico numa operação que ficou a metade do risco permitido** (PR #358, merge `3c4551c7`). O trade WINV26 de 18/08 (+R$ 521,00) recebeu `STOP_PANIC / HIGH`, contradizendo a emoção declarada ("Calmo") e travando progressão de estágio — enquanto o mesmo painel exibia "Execução limpa · Disciplina · 90% · nenhum padrão negativo". **Três causas somadas.** (A) `detectStopTampering` ordenava os stops e comparava pares consecutivos assumindo que são versões do MESMO stop; com entrada escalonada cada incremento traz seu próprio bracket OCO e os stops COEXISTEM — havia dois vivos (169.780 para 5 contratos, 169.700 para 3), nenhum movido, e a comparação inventou um alargamento. (B) A premissa estava errada: abrir com OCO fixa, ajustar stop e alvo pela formação dos candles, aumentar posição e reajustar contra o médio é condução normal — e comparar preço ABSOLUTO ignora que, depois de piramidar, um stop mais longe em pontos pode representar risco igual ou menor. (C) `detectCleanExecution` decide "nenhum padrão negativo" olhando só os padrões do próprio shadow, ANTES da fusão com `events` — a premissa "eu vejo tudo" não se sustenta onde ele roda, e o dedup por família não resolve porque são famílias distintas. **Regra nova (Marcio, 19/08):** alerta é `distância × valor_do_ponto × contratos > RO do plano`, composto POR PERNA (cada entrada pode trazer OCO próprio); só contam os stops vigentes no fim da operação, porque um cancelado bem antes foi substituído e duplicaria o risco. Famílias: `RISK_OVER_RO` (Ganância, gate) substitui o gatilho do `STOP_PANIC`; `UNPROTECTED_SIZE` (gate) substitui `STOP_PARTIAL_SIZING` e passa a cobrir o caso de ZERO stops, que o antigo deixava passar em `if (!stops.length) return []` — o mais grave era o único descoberto; `SIZING_DISCIPLINE` (positivo) reconhece aumento de posição com risco mantido no RO. `STOP_PARTIAL_SIZING` deixa de mapear para `SUB_SIZING` ("risco muito ABAIXO do RO", semântica oposta). `CLEAN_EXECUTION` reavaliado DEPOIS da fusão em `buildBehaviorProfile`. Marcação `SEM STOP` / `RISCO > RO` inline no extrato do plano (canal 2 decidido por Marcio). **Recompute do histórico aplicado** (`scripts/issue-357-recompute-behavior.mjs`, reusa `recomputeBehaviorProfiles` da CF): 338 trades, 182 perfis gravados — 169 sem perfil prévio (backfill), 13 mudanças reais, 1 gate destravado, 9 alertas antes invisíveis. O trade de referência passou de `[STOP_PANIC, CLEAN_EXECUTION] gates=[STOP_PANIC]` para `[SIZING_DISCIPLINE, CLEAN_EXECUTION] gates=[]`, com evidência `riskAmount 127,01 · roAmount 252,00`. Testes: fixture SEM1 de 3 para 5 eventos (dois trades sem stop passaram a acusar), snapshot de não-regressão com uma linha trocada, 3 testes de reconciliação rodando o pipeline completo, 4 da marcação no extrato — 3615 verdes no front, 206 em functions, paridade ESM↔CJS mantida. Pendência registrada no #101: os alertas dependem da mailbox da Torre para alcançar o mentor.  
> **Última atualização (histórica):** 18/08/2026 — v0.40.15: Encerramento #351 v1.83.5 — **ordem executada no meio da operação sumia do trade** (PR #352, merge `1b868910`). Diagnóstico partiu de importação real (WINV26 18/08/2026, operação de +R$ 521,00): a compra de 3 contratos das 14:46:17 não aparecia no painel de ordens nem alimentava o sensor comportamental de execução. **Causa A:** `correlateOrders` correlacionava ordem↔trade por proximidade das PONTAS (±5 min de `entryTime`/`exitTime`) e nunca testava pertencimento ao intervalo — o fill do meio ficava a 47min46s da abertura e 28min31s do fechamento, fora da janela nas duas. A assimetria que fechou o diagnóstico: o stop cancelado do mesmo instante ERA correlacionado, porque `correlateCancelledOrders` usa sobreposição de intervalo; o fill real não. Fix: branch de contenção estrita em `[entryTime, exitTime]`, role derivado do side, score fixo 0,55 (perde para pontas a menos de 75s, ganha das mais distantes). **Causa D — a maior, descoberta só ao medir o passivo:** dos 198 fills órfãos corrigíveis em produção, **194 casavam pela regra de ponta que já existia** (`delta: 0s`) — nunca foram problema de janela, estavam órfãos porque o trade não existia quando a correlação rodou. `correlateOrders` e `ingestBatch` rodam no cliente contra os trades existentes; operação classificada como `new` só vira trade depois, em `createTradesBatch`, e nada voltava para ligar. Ou seja: **todo trade nascido do Order Import ficava com 100% das suas ordens órfãs.** Não corrigível no cliente — `firestore.rules:321` tem `allow update: if false` em `/orders`; foi para `onTradeCreated` etapa 7 (`functions/orders/linkOrdersToCreatedTrade.js`), casando `_partials` do trade contra os docs do mesmo `batchId` pelo fingerprint `instrument|side|filledAt|qty`, sem nunca sobrescrever correlação existente. **C:** scripts de backfill (dry-run + apply) reusando `correlateOrders` do runtime; dry-run rodado (473 fills órfãos, 198 corrigíveis, 273 em plano sem trade), **apply pendente de decisão**. **Achado registrado fora de escopo:** `persistDiscardedOrders` (#156 Fase E) também faz `update` em `/orders` pelo cliente dentro de `try/catch` com `console.warn` — falha silenciosamente em toda importação sob a regra atual; `userDecision: discarded` nunca foi gravado. Testes: 6 unitários (4 falham sem o fix, 2 são guardas), 3 de integração com recorte do CSV real, 9 em functions — 3597 verdes no front, 212 em functions.  
> **Última atualização (histórica):** 17/08/2026 — v0.40.14: Encerramento #347 v1.83.4 — **o modal de edição descartava o que o aluno tinha digitado** (PR #348, merge `e573473a`). Perda de dados em produção, reportada por Marcio: aluno abre o trade, escreve nas Observações, salva a **reflexão** no painel do Espelho que fica no rodapé do mesmo modal, clica em Salvar — e o texto não está mais lá, sem erro nem aviso. **Causa raiz determinística:** o efeito de hidratação do `AddTradeModal` tinha `[editTrade, isOpen, plans, exchanges, setups, emotions]` nas deps; qualquer troca de **identidade** re-rodava `setFormData` e sobrescrevia o formulário com o trade congelado no clique do lápis. Dois gatilhos: (1) `handleSubmitReview` (`StudentDashboard`, `TradesJournal`) faz update otimista recriando o objeto — e o `TradeReviewSection` fica fora do `<form>` com save próprio que não fecha o modal (#308), então o aluno segue preenchendo sem perceber que o form foi rebobinado; (2) `plans`/`exchanges`/`setups`/`emotions` são arrays recriados a cada `onSnapshot`. Assinatura que fechou o diagnóstico: o ramo de edição não limpa `htfFile`/`ltfFile` (só o de trade novo), então imagem colada sobrevivia ao reset e texto digitado não — exatamente o relato. **A:** hidratação chaveada por `editTrade?.id` + `hydratedKeyRef` (DEC-AUTO-347-01); defaults de trade novo viram efeito aditivo que só toca campo vazio. **B:** `isDirtyRef` trava os defaults assim que o usuário digita — defesa em profundidade contra a próxima dep acidental. **C:** o escopo original previa remover o `setEditingTrade`; durante a implementação ficou claro que isso **regride o #308** (o painel volta ao nudge logo após salvar, pois `editingTrade` é state congelado) — mantido e documentado nos dois call sites, inofensivo sob A (DEC-AUTO-347-02). **D:** campo "Observação" por operação **removido** do `OrderStagingReview` — era entregue em `onConfirm({ ..., observations })` e nunca lido por `handleStagingConfirm`, não persistia em lugar nenhum; decisão de produto de Marcio foi remover, não persistir (DEC-AUTO-347-03). Saiu junto a fiação morta e `op.autoObservation`; `analyzeStopMovements` segue com seu `observation` (contrato testado, alimenta `flags`/`movements` da detecção comportamental). **Sem campo Firestore novo (sem INV-15), sem CF, sem deploy.** Testes: `AddTradeModalHydration.test` +5 (arquivo novo) / `OrderStagingReviewObservation.test` +2 (arquivo novo) — red-green confirmado por stash: os 3 de preservação falham contra o código anterior; suíte 3588/3588 e build verdes. Locks CHUNK-04/CHUNK-10 liberados. v1.83.4 consumida. Worktree `~/projects/issue-347` removido. **Nota:** o conteúdo perdido pelo aluno é irrecuperável — nenhuma CF escreve `trade.notes`, então não houve sobrescrita no servidor; o texto nunca saiu do navegador.  
> **Última atualização (histórica):** 11/08/2026 — v0.40.13: Encerramento #345 v1.83.3 — **reflexão do aluno não gravava quando cobrada pela fila do dashboard** (PR #346, merge `e036ec95`). `publishReview` marca **todos** os trades ancorados na revisão como `status: DISCUSSED`, sem exigir `selfReview` — então trade importado (CSV/Order) ou pulado no registro que entrou numa revisão publicada ficava DISCUSSED **sem reflexão**. DISCUSSED é terminal/imortal a writes de cliente por decisão do #269 v2 (duas cláusulas em `firestore.rules` + guard no `tradeGateway`), mas a **UI discordava das rules**: a fila "Trades a refletir" (#327) listava esses trades e o gate de edição abria o formulário. O aluno preenchia, clicava **Salvar revisão**, o write era negado e a exceção morria em `console.error` — "nada acontece", e a fila cobrava o mesmo trade indefinidamente (mesma classe de falha silenciosa do #343 B). **Decisão de produto (DEC-AUTO-345-01):** a janela de reflexão **fecha** quando o trade é discutido — reflexão pós-feedback é contaminada ("faria de novo sem saber o resultado?" perde o sentido e o confronto declarado × detectado vira ruído); a UI passa a alinhar-se às rules em vez de contradizê-las. Descartado permitir reflexão tardia: exigiria enfraquecer as rules ou criar callable admin-SDK para furar o próprio invariante. **A:** regra vira predicado único `isReflectionWindowClosed(trade)` em `tradeReviewFramework` (DEC-AUTO-345-02) — replicar `status !== 'DISCUSSED'` nos 3 call sites de `canReview` triplicaria justamente o ponto de divergência que causou o bug; `PendingReflections` tira DISCUSSED da fila e do contador. **B:** `TradeReviewSection` ganha estado "Janela fechada" (read-only, sem botão, só para o aluno — o mentor segue com o alerta âmbar do `StudentReflectionPanel` #323). **C:** `submitError` visível com motivo técnico (DEC-AUTO-345-03) + guard de duplo clique; falha preserva as respostas digitadas. **Sem mudança em `firestore.rules`, `publishReview` ou `tradeGateway`** — os três já estavam corretos. Sem campo novo (sem INV-15), sem CF, sem deploy. Testes: `TradeReviewSection.test` +10 (arquivo novo) / `PendingReflections.test` +3; suíte 3581/3581, lint e build verdes. Lock CHUNK-04 liberado. v1.83.3 consumida. Worktree `~/projects/issue-345` removido. **Nota de processo:** o PR ficou **2 dias parado sem merge** (09/08 → 11/08) com CI verde — os alunos seguiram reportando o bug já corrigido no branch.  
> **Última atualização (histórica):** 06/08/2026 — v0.40.12: Encerramento #343 v1.83.2 — **assessment travado: modelo Claude aposentado derruba 5 CFs** (PR #344, merge `5ad539fb`). `claude-sonnet-4-20250514` foi aposentado pela Anthropic e passou a responder **404 `not_found_error`**. A CF `classifyOpenResponse` pontua as 12 respostas abertas **antes** de gravar `completedAt`, então o aluno que respondia as 34 perguntas do assessment 4D clicava em **Finalizar** e nada acontecia — o `catch` só escrevia em `console.error`. Dois alunos ficaram presos em `pre_assessment` com 34/34 respostas (Sandra Maria; Antonio Pina resetado via DEC-026 com histórico preservado). **A1/A2:** as 4 CFs de assessment (`classifyOpenResponse`, `analyzeProbingResponse`, `generateProbingQuestions`, `generateAssessmentReport`) e os 2 call sites do Prop Firm migram para `claude-sonnet-4-6` (DEC-AUTO-343-01) — mesma família, sem recalibrar rubricas; `RESPONSE_SCHEMA` do propFirm passa a referenciar `MODEL`/`PROMPT_VERSION` em vez de repetir o ID (era a cópia que sobrou quando o header já dizia "Sonnet 4.6"). **B1:** o fix do #166 estava neutralizado por dois motivos independentes — `handleProbingComplete` engolia a exceção e `ProbingQuestionsFlow` chamava `onComplete()` **sem `await`** (promise flutuante); ambos corrigidos, e `QuestionnaireFlow` ganha `submitting`/`submitError` (botão desabilitado, guard de duplo clique, mensagem visível). `aiModelVersion` passa a vir do retorno da CF — o front não conhece mais nenhum ID de modelo. **C1:** `startQuestionnaire` fazia `setDoc` sem merge e zerava `responses`, então a preservação prometida pelo DEC-026 valia só até reativar o assessment de um aluno resetado; agora reabre via `updateDoc`. **Deploy de CF obrigatório e executado.** Testes: `startQuestionnairePreservesResponses` +3 / `questionnaireFinalizeFeedback` +4; suíte 3568/3568 + build verdes; red-green confirmado revertendo C1. Locks CHUNK-09/CHUNK-17 liberados. v1.83.2 consumida — constante `VERSION` corrigida de 1.84.0 (aplicada em 27/07 apenas pela reserva pendente do #101) para 1.83.2, a última realmente lançada. Worktree `~/projects/issue-343` removido. **Pendência:** validar em produção com a Sandra (nenhuma invocação real da CF desde o deploy).  
> **Última atualização (histórica):** 20/07/2026 — v0.40.11: Encerramento #341 v1.83.1 — **Contas do mentor esconde alunos sem assinatura ativa** (PR #342, merge `67f26de3`). A seção Contas (visão do mentor) listava **todos** os alunos que já tiveram conta — inclusive inativos, ex-alunos e órfãos — porque `useAccounts` (modo mentor) lê todas as contas sem cruzar com assinatura, e `AccountsPage` renderizava todo grupo por `studentId`. A tela de Acompanhamento já filtrava por `classifyStudent`; Contas nunca recebeu esse filtro. Fix: um grupo só aparece se `classifyStudent(student, subs) !== null` — **paridade com o Acompanhamento** (VIP ativo, sem-sub e expired/cancelled somem; `overdue` em grace permanece; grupo `unknown` órfão some). Util puro novo `visibleStudentIds` replica a montagem de `subsByStudent` do `StudentsManagement` (ref. negativa #316 — assinaturas divergentes de `classifyStudent`). Grid do mentor extraído em `MentorAccountsGrid`, que monta `useStudents`/`useSubscriptions` **só quando renderizado (mentor)** — o caminho do aluno nunca dispara listener em `/students` ou collectionGroup. **Sem campo Firestore novo, sem CF, sem deploy** (só `AccountsPage.jsx` + 1 util). Testes: `mentorAccountsVisibility.test` +9; suíte 3561/3561 + build verdes. Lock CHUNK-16 liberado. v1.83.1 consumida. Worktree `~/projects/issue-341` removido.  
> **Última atualização (histórica):** 16/07/2026 — v0.40.10: Encerramento #339 v1.83.0 — exibir timezone do horário de entrada em todas as telas de trade (PR #340, merge `9bd68442`). O fuso do trade — embutido no offset do ISO (`entryTime`/`exitTime`) desde #285/#292 mas nunca exibido — passa a aparecer como `16:23 ET` em ~9 superfícies. Novo helper `shortTzLabelFromIso(iso)` → **ET/CT/BRT** (reidrata o IANA via `tzFromStoredIso`; como `getOffset` é date-aware e ET/CT ficam sempre 1h separados no mesmo dia, não há ambiguidade de offset) + SSoT de exibição `fmtTradeTime`/`fmtTradeDateTime` que usa o **relógio de parede do trade**, não o fuso do navegador. **Grupo A** (mostravam wall-clock sem label): `FeedbackPage`, `MentorDashboard` (×2), `StudentFeedbackPage`, `TradesList`, `TradeDetailModal`. **Grupo B** (mostravam no fuso do navegador → a mesma operação divergia conforme quem abria — DEC-AUTO-339-02): `reviewFormatters.fmtTime` passa a delegar ao SSoT (corrige `ReviewTradesSection`), `OrderStagingReview`, `ConversationalOpCard` e `ExtractTable`/Extrato (esta última foi achado da revisão do PR — tela que escapou do inventário inicial). ISO legado sem offset → hora sem label (fallback defensivo, não quebra); `ConversationalOpCard` de quebra corrigiu bug latente de input date-only renderizado no fuso do browser (podia mostrar o dia anterior). **Sem campo Firestore novo, sem CF, sem deploy** (só exibição + 1 helper puro). Testes: `tradeTimezone.test` +20 (offset→label, DST ET/CT, BRT fixo, naive/legado, `fmtTradeTime`/`fmtTradeDateTime`) / `reviewFormatters.test` ajustado ao novo contrato; suíte 3552/3552 + build verdes. DEC-AUTO-339-01/02 em `docs/decisions.md`. Locks CHUNK-04/08/16 liberados. v1.83.0 consumida. Worktree `~/projects/issue-339` removido.  
> **Última atualização (histórica):** 01/07/2026 — v0.40.9: Encerramento #315 v1.80.0 — evidência técnica mentor-only + imagens HTF/LTF opcionais + reflexão do aluno na composição de feedback (PR #322, merge `91f19484`). Três ajustes de "quem vê o quê", **sem campo Firestore novo nem CF** — só gating de exibição, remoção de validação e leitura de campo já existente (nenhum deploy, paridade prod↔main preservada). **A (CHUNK-11):** o accordion "Evidência técnica" do `BehaviorPanel`/`UndersizedBody` (`behaviorDisplay.jsx`) — dump cru de campos do schema do motor (`intervalMinutes`, `previousSide`, `actualRR`) — passou de visível-a-todos para **mentor-only** (guarda `isMentor && expanded && …`); o aluno fica com a narrativa humanizada (`narrativeFor`) + o texto educacional do undersized. Humanizar o universo de campos foi descartado (ROI baixo: painel colapsado/opt-in, epic CHUNK-11 ativo → cauda de manutenção); esconder do aluno entrega a melhoria percebida sem dívida. `BehaviorPanel` passa a **propagar `isMentor` ao `UndersizedBody`** (antes não passava — os dois caminhos, undersized e demais famílias, fecham a mesma regra). **B (CHUNK-04):** `AddTradeModal` não exige mais imagem HTF/LTF na criação (removido o bloco `newErrors.htf/ltf`; label `*` vermelho → `(opcional)` cinza); edição inalterada. **C (CHUNK-08):** a auto-análise do aluno (`trade.selfReview`) passou a aparecer na tela onde o mentor **compõe feedback** — `<TradeReviewSection>` read-only após o `BehaviorPanel` (o mentor cruza "faria de novo?" + respostas com o comportamento detectado) + **estado explícito** "o aluno ainda não fez a auto-análise deste trade" quando `selfReview` ausente (distingue "não refletiu" de "feature não existe"); `StudentReviewsPage` passa `showSelfReview` (paridade com a Sessão de Revisão semanal). Testes: `BehaviorPanel.test` +2 (aluno não vê evidência crua, geral e SUB_SIZING) / `ReviewTradesSection.test` +2 (botão da reflexão); suíte e build verdes. **Reserva de versão:** a original v1.78.0 (25/06) ficou obsoleta — main avançou a 1.79.1 (#318/#320) com a issue aberta; rebase `--onto origin/main` dropou o lock commit stale e o bump virou **1.80.0** (feat minor sobre a última consumida, sem regredir `version.js`). **Tooling:** o encerramento expôs bug de detecção de PR no `cc-close-issue.sh` — a busca fuzzy `--search "Closes #NNN"` casava menção em prosa de outro PR (pegou #317 do #316) e, com `--limit 1`, retornava o errado; corrigido para filtrar o keyword de fechamento exato no body (commit `2b7a5910`, validado #315→322/#316→317/#320→321/#318→319). Locks CHUNK-11/04/08 liberados. v1.80.0 consumida. Worktree `~/projects/issue-315` removido.  
> **Última atualização (histórica):** 25/06/2026 — v0.40.8: Encerramento #313 v1.77.0 — Reflexão na entrada do trade + copy de auto-análise (PR #314, merge `238882f9`). Reabre o posicionamento do #308: `TradeReviewSection` sai do lápis de trade fechado e passa a aparecer no **ato de registrar** — ao salvar trade novo (que já nasce com `result`: entry+exit+qty → result no gateway), `AddTradeModal` abre o passo de Reflexão do trade recém-criado em vez de fechar (botão "Pular por agora"); prop `startOpen` abre direto nas perguntas (pula o nudge). `handleAddTrade` (`TradesJournal`/`StudentDashboard`) passa a retornar o trade criado; fechamento vira responsabilidade do `onClose`. View-as do mentor (`overrideStudentId`) não dispara reflexão. O questionário por quadrante é o mesmo (trade já tem result) — muda só onde/quando aparece; mesmo componente cobre registro E lápis. **Copy:** reescrita para tom de auto-análise (Marcio: framing lúdico "passou no espelho"/"olhar no espelho" denigre o nome do produto) — "Espelho" (rótulo da auto-revisão) → "Reflexão", banner "Reflexo ·" → "Análise ·", nudge → "auto-análise", toggle mentor → "Ver a reflexão do aluno"; **não** toca o nome do produto/plano "Espelho" (marca, `self_service`). Respostas abertas continuam read-only pro mentor em `TradeDetailModal` + Sessão de Revisão. Build cliente verde. Lock CHUNK-04 liberado. v1.77.0 consumida. Worktree `~/projects/issue-313` removido.  Hotfix paralelo (`14cca576`): `issue-269-consolidate-duplicate-drafts.mjs` consolidou rascunho DRAFT duplicado por plano (1 caso: Daniel Barbosa / SWING EDUCACIONAL — resíduo da migração #269 que escolheu o ponteiro mas não apagou o DRAFT legado extra).  
> **Última atualização (histórica):** 24/06/2026 — v0.40.7: Encerramento #269+#262 v1.76.0 — Revisão por backlog (FK única `trade.reviewId`) + SWOT customizável + filtro matriz Alpha/Trial (PR #312, merge `7bc7eb57`). **Modelo v2 (CHUNK-04/08):** substitui o efêmero `reviewState`/`draftReviewId`/`review.includedTradeIds` (REMOVIDOS) por **FK única `trade.reviewId`** (imortal, `null`=backlog) + ciclo único com terminal **`status=DISCUSSED`**. Pertencimento = `trades WHERE reviewId==id`. A revisão DRAFT nasce sob demanda no 1º feedback (`getOrCreateOpenReview`, idempotente via ponteiro `plan.activeDraftReviewId`); `publishReview` marca membros `DISCUSSED` + congela `frozenSnapshot`. Gatilho único no `onTradeUpdated` (`OPEN→REVIEWED`, individual ou bulk). Some criação/picker/descartar de UI. **Filtro matriz (#269):** Revisão/feedback só para o **track Alpha** (`{alpha, trial-alpha}`, definido por `classifyStudent` sobre subscriptions — sem campo novo); Espelho/VIP/sem-sub não geram revisão. Gate em 3 pontos: `onTradeUpdated` não ancora fora de escopo, `useTrades` bloqueia feedback do mentor (individual+bulk), migração pula fora-de-escopo. `functions/_shared/studentClassify.js` é a porta canônica (paridade com `src/utils/studentClassify.js`). **#262:** `swotPromptBuilder` + `mentorConfig.swotStyle` (3 sliders no Cockpit) + `setMentorSwotStyle`; `generateWeeklySwot` consome o builder. **Migração (aplicada em prod):** `migrateReviewStateBackfill` (dry-run + safeguard D8) — 584 mudanças, 241 órfãos-com-feedback ancorados no rascunho vigente, 13 rascunhos provisionados; blinda `DISCUSSED` contra re-run. **Correções retroativas:** filtro matriz (60 trades un-anchorados + 7 drafts removidos de alunos fora de escopo hoje — tier é temporal) e 8 reviews órfãs de plano morto; cascade de reviews em `deletePlanCascade`/`deleteAccountCascade` impede recorrência. CFs deployadas (`onTradeUpdated`, `migrateReviewStateBackfill`, cascades). Suite functions 193/193 + build cliente verde. **Follow-up:** Espelho-no-registro (mover a reflexão do lápis de trade fechado para o ato de registrar — reabre #308) vira issue próprio. Locks CHUNK-03/04/08/16 liberados. v1.76.0 consumida. Worktree `~/projects/issue-269` removido.  
> **Última atualização (histórica):** 10/06/2026 — v0.40.6: Encerramento #308 v1.75.0 — Espelho (auto-revisão de trade) + fixes de import + email (PR #311 squash `6602b815`). **Espelho (CHUNK-04):** questionário processo × resultado por trade (`trade.selfReview` inline, DEC-AUTO-308-01); quadrante e confronto declarado × detectado derivados em display-time (não persistidos); não mexe no 4D. Escrita movida pro fluxo do **lápis** (`AddTradeModal`, só trade fechado, save próprio `submitTradeReview` independente do save dos campos); **olho** (`TradeDetailModal`) read-only — só exibe o Espelho já preenchido (DEC-AUTO-308-03). `firestore.rules`: aluno grava `selfReview`, isento do seal #259, imutável após `reviewState=DISCUSSED` (#269). Vocabulário "Espelho" (aluno) × "Revisão" (mentor, #269); banner do confronto renomeado "Reflexo ·" (DEC-AUTO-308-02). **Coverage gap no import de ordens (CHUNK-10):** banner lista ops em período sem plano vigente (data BR · ticker · motivo) e oferece Descartar / Aceitar no plano atual / Criar plano retroativo — gate duro virou resolúvel (`ConversationalReview`/`OrderImportPage`, match por referência das ops). **Timezone (#292, DEC-AUTO-308-04):** trade novo começa com fuso em branco e obriga o aluno a eleger (MEP/MEN depende do instante); edição deriva do banco (`tzFromStoredIso` casa offset gravado × fuso na data, DST-correto); order import + CSV wizard sem sticky silencioso (corrigiu "selecionei Brasília, veio NY"); exchange só sugere quando vazio, nunca trava; corrigido bug de closure stale do `importTimezone`. **Email de boas-vindas (CHUNK-01, deployado em prod):** `APP_NAME` nome de código → **Espelho** (DEC-031); `APP_URL` portal de marketing → **app.marcioportes.com.br**. Suite 3472 verde (+18 testes: `tzFromStoredIso`, resolução do coverage gap, gateway selfReview, confronto). `firestore.rules` + CFs do email (`createStudent`/`resendStudentInvite`) deployadas em prod. DEC-AUTO-308-01..04 em `docs/decisions.md`; `trade.selfReview` documentado em `firestore-schema.md`. Coverage gap + timezone são CHUNK-10 carregados neste branch CHUNK-04 (decisão em sessão). Encerramento via recovery manual (`closing-manual.md`) — `gh` GraphQL com 401 transitório do GitHub bloqueou o `cc-close-issue.sh` (REST ok; recuperou depois). Lock CHUNK-04 liberado. v1.75.0 consumida. Worktree `~/projects/issue-308` removido.  
> **Última atualização (histórica):** 25/04/2026 — v0.40.5: Encerramento #197 v1.46.1 — salvar/atualizar link de reunião e gravação na revisão semanal pós-publicação (PR #198 squash `af9662b0`). Mentor publicava revisão (CLOSED) e ficava preso: `meetingLink`/`videoLink` editáveis só em DRAFT (`ReviewToolsPanel`/`WeeklyReviewModal` gated em `isDraft`; `WeeklyReviewPage` sem campos de link). Caminho real impossível — link da gravação só existe DEPOIS da reunião terminar, depois de publicar a revisão. Fix em 4 fases: (B) novo `useWeeklyReviews.updateMeetingLinks(reviewId, {meetingLink, videoLink})` faz `updateDoc` parcial (`{meetingLink, videoLink, updatedAt}`) sem mudar status; valida via `validateReviewUrl` (regex https + allowlist hosts) antes de gravar; aceita parcial (campo `undefined` preservado); ambos undefined = no-op defensivo. (A) `WeeklyReviewPage` ganha Subitem 3 "Reunião" entre Notas (2) e Snapshot (4) — renumeração visível 3→9; `MeetingLinksSection` inline (padrão dos siblings) com 2 inputs URL + botão "Salvar links" + validação inline; ARCHIVED read-only com banner. (C) `ReviewToolsPanel` (Extrato) e `WeeklyReviewModal` tab "Reunião": novo botão dedicado "Salvar links" liberado em CLOSED (`linksDirty` separado de `draftDirty`), separado do "Salvar rascunho" (que segue exclusivo de DRAFT cobrindo takeaways/sessionNotes). Tri-superficial consistente. (G) Escopo expandido descoberto durante smoke: `ReviewQueuePage` filtrava só alunos com DRAFT (`StudentDraftProbe` → `where('status', '==', 'DRAFT')`), sem caminho para reabrir CLOSED. Refatorado em `StudentStatusProbe` genérico parametrizado por status; `closedCounts` paralelo ativado on-demand pelo toggle "Incluir publicadas" (default OFF — preserva intent original de fila como working items); copy do header e empty state condicionais. `firestore.rules` sem alteração (linhas 65-71 já cobriam mentor CLOSED→CLOSED com qualquer campo). Sem campo Firestore novo (INV-15 não acionada — `meetingLink`/`videoLink` aprovados em #102/v1.33.0). DEC-AUTO-197-01: campos são metadata operacional (não congelam no `frozenSnapshot`), editáveis em DRAFT e CLOSED por mentor; ARCHIVED bloqueia. 7 testes novos no hook (DRAFT/CLOSED felizes, strings vazias, URL inválida, host fora allowlist, no-op defensivo, parcial, erro do `updateDoc`). Suite 2489/2489. Smoke validado em `localhost:5173` (Marcio 25/04/2026): WeeklyReviewPage Subitem 3 + toggle "Incluir publicadas" resolvem o caminho fim-a-fim. Lock CHUNK-08 liberado. v1.46.1 marcada consumida. Worktree `~/projects/issue-197` removido. Issue doc arquivada em `docs/dev/archive/2026-Q2/`.  
> **Última atualização (histórica):** 25/04/2026 — v0.40.4: Encerramento #189 v1.46.0 — score emocional real no motor de maturidade (PR #196 squash `f013b23d`). Furo universal de progressão removido: `functions/maturity/preComputeShapes.js:129` deixou de retornar stub `{periodScore: 50, tiltCount: 0, revengeCount: 0}` (DEC-AUTO-119-task07-02 declarava como TODO desde v1.44.0) e passa a consumir `computeEmotionalAnalysisShape` do mirror CommonJS novo `functions/maturity/emotionalAnalysisMirror.js`. Mirror replica `calculatePeriodScore` + `detectTiltV2` + `detectRevengeV2` + `calculateTradeEmotionalScore` + `buildGetEmotionConfig` (paridade ESM↔CJS testada) do source `src/utils/emotionalAnalysisV2.js` (CHUNK-06). `recomputeForStudent` carrega collection `emotions` antes do pre-compute; falha de fetch é graceful (warn + fallback `{50,0,0}` preserva D6 "evolução sempre visível", INV-03 isolamento mantido). Fórmula DEC-AUTO-119-03 (`E = 0.60·periodScore + 0.25·invTilt + 0.15·invRevenge`), janela rolling STAGE_WINDOWS (D1) e DEC-020 (regressão nunca automática) inalteradas. Os 5 gates emocionais do framework (`emotional-out-of-fragile`, `emotional-55`, `emotional-75`, `emotional-85`, `zero-tilt-revenge`) passam a discriminar — antes travados em E=50. 17 testes paridade novos em `src/__tests__/functions/maturity/emotionalAnalysisMirror.test.js`. Suite 2438/2438 (baseline 2421 + 17). Sem novo campo Firestore (INV-15 não acionada). Sem UI nova (`MaturityProgressionCard` herda automático). Follow-ups documentados no PR para issues próprios: (1) `calculatePeriodScore([])=100` paridade ESM × D6=50 — só `computeEmotionalAnalysisShape` aplica D6; (2) aluno legado sem `emotionEntry` em todos trades pega E≈60 via consistency bonus em UNKNOWN/UNKNOWN — comportamento herdado, vale issue de revisão semântica; (3) cache em memória runtime do CF para collection `emotions` (otimização futura). Lock CHUNK-09 liberado. v1.46.0 marcada consumida. Worktree `~/projects/issue-189` removido. Issue doc arquivada em `docs/dev/archive/2026-Q2/`.  
> **Última atualização (histórica):** 25/04/2026 — v0.40.3: Encerramento #188 v1.45.0 — fast-track Sev1 mergeado (PR #195 squash `a7b89c89`). Produto v1.45.0. Entrega de 4 frentes em 8 fases A-H pair programming. **F1 — Lock comportamental do mentor (INV-15 aplicada, Sev1 core):** 5 campos novos em `trades` (`_lockedByMentor`, `_lockedAt`, `_lockedBy`, `_mentorEdits[]` append-only auditável, `_studentOriginal` snapshot imutável após 1ª edit) + metadata complementar `_unlockedAt`/`_unlockedBy`. Gateway `tradeGateway` ganha `editTradeAsMentor`/`lockTradeByMentor`/`unlockTradeByMentor` com whitelist 3 campos (`emotionEntry`/`emotionExit`/`setup`); rejeita não-mentor; rejeita já-locked; grava `_studentOriginal` apenas na 1ª edit. `firestore.rules` agora tem 3 gates combinados: ownership + lock dos 3 campos + metadata guard (só mentor toca campos de lock; CFs admin SDK bypassam). UI `MentorEditPanel` colapsável (3 selects + reverter ao original + modal de confirmação dupla com diff campo-a-campo + travar), `TradeLockBadge` no header do FeedbackPage, ícone Lock inline na ExtractTable, asterisco âmbar nos campos editados, bloco "Histórico de correções (N)" no TradeDetailModal. CF `onTradeUpdated.complianceFields` agora inclui `emotionEntry` (fix de bug pré-existente onde `BLOCKED_EMOTION` ficava estale após edit de emoção) — reconstrução de redFlags filtra/recompila contra `after.emotionEntry` vs `plan.blockedEmotions`. Import (CSV/Order) destrava lock server-side via CF `onTradeUpdated` quando `importBatchId` muda, preservando `_mentorEdits`/`_studentOriginal` (DEC-AUTO-188-03 — broker é fonte de verdade superior ao mentor; admin SDK bypassa rules). **F2 — Currency multi-moeda no MentorDashboard:** `aggregateTradesByCurrency` + componente `MultiCurrencyAmount` aplicado em P&L Total Turma, ranking, lista alunos, detalhe aluno, pending list, bulk modal. Stack vertical em multi-moeda, sem somar cross-currency; FX conversion fora de escopo (DEC-AUTO-188-05). **F3 — `PlanSummaryCard` no FeedbackPage:** colapsável (chat continua o foco), header com nome+moeda+badge arquivado, linha 1 RO/RR/Cap, linha 2 Bloqueadas, linha 3 Ciclo X/N; expand mostra Período/Ciclo Meta+Stop + PL atual %ciclo. Smoke-test polish: fontes -1pt + valores absolutos da moeda em RO/Período/Ciclo Meta+Stop. **F4 — TODOS os cards do StudentDashboard respeitam ContextBar sem exceção (DEC-AUTO-188-06, ordem direta em caps do Marcio):** `useDashboardMetrics` v2.1.0 aceita `context{accountId,planId,cycleKey,periodRange}`, filtragem central inclui janela temporal (end inclusivo 23:59:59), todas as métricas derivadas herdam (stats, MaxDD, payoff, EV, consistency, compliance, durations); `plContext.label` derivado de `periodRange.kind`; `filters.period` legado removido (SoT única); `Filters.jsx` 6→5 colunas; `PendingTakeaways` filtra por `planId`. Smoke-test polish: tooltips em todos os KPIs/quadrantes da Matriz Emocional 4D com fórmulas calculadas nos números reais do card. 2445/2445 testes (+44 novos: 7 `aggregateTradesByCurrency` + 11 `PlanSummaryCard` + 7 `useDashboardMetrics.contextBar` + 13 `tradeGatewayMentorLock` + 6 `TradeLockBadge`). 7 decisões registradas em `docs/decisions.md` (DEC-AUTO-188-01..07). 8 locks liberados (CHUNK-02/03/04/05/06/08/13/16). v1.45.0 marcada consumida. Worktree `~/projects/issue-188` removido. Issue doc arquivada em `docs/dev/archive/2026-Q2/`.  
> **Última atualização (histórica):** 25/04/2026 — v0.40.2: Encerramento #191 — fix de semântica do gate `compliance-100` (Metódico→Profissional) mergeado (PR #194 squash `eb4ff2ec`). Produto v1.44.1. Antes: `complianceRate100 = complianceRate` (alias do cálculo da janela total — gate avaliava aderência sobre histórico inteiro do trader em vez da janela recente que o nome promete). Agora: novo helper puro `computeCycleBasedComplianceRate({trades, plans, now, minTrades=20})` aplica a janela = união dos ciclos ativos do trader (todos os planos pelo `adjustmentCycle` Mensal/Trimestral/Semestral/Anual). Mínimo 20 trades CLOSED; se não atinge, retrocede simultaneamente 1 ciclo em CADA plano até bater 20 ou esgotar histórico (cap defensivo `MAX_LOOKBACK_CYCLES=36` ou iteração que não acrescenta nada). Insuficiente (`<20` mesmo após esgotar) → `null` → `evaluateGates` marca como `METRIC_UNAVAILABLE` (gate pendente: não promove e não rebaixa, DEC-020 preservada). Mirror espelhado em `functions/maturity/` (CommonJS) e `src/utils/maturityEngine/` (ESM); `preComputeShapes.js` aceita `now` e `recomputeMaturity.js` repassa. 20 testes novos (17 ESM cobrindo cenários A-E + invariantes + 3 paridade ESM↔CommonJS); suite 2421/2421 verde. DEC-AUTO-191-01 (janela = união de ciclos ativos por plano + fallback retroativo simultâneo) e DEC-AUTO-191-02 (estado insuficiente = null) registradas. Lock CHUNK-09 liberado. v1.44.1 marcada consumida. Worktree `~/projects/issue-191` removido.  
> **Última atualização (histórica):** 24/04/2026 — v0.40.1: Encerramento #183 — fast-track Sev1 mergeado (PR #186 squash `e46f2051`). Produto v1.43.1. `usePlans.addPlan` hardcodava `studentId: user.uid`; plano criado por mentor em nome do aluno ficava com UID do mentor e aluno nunca enxergava. Fix no hook (prioridade `planData.studentId > overrideStudentId > user.uid`; `createdBy`/`createdByEmail` gravados sempre) + wrapper `handleCreatePlanForSelectedAccount` em `AccountsPage` propagando `studentId/Email/Name` do account. Script `scripts/issue-183-repair-orphan-plans.mjs` (REMAP não-destrutivo via `account.studentId` com cascade em trades) executado em prod — 2 planos remapeados (`xT` → `marcio.portes@icloud.com`, `PL-REAL20K` → `rafael_perilo@hotmail.com`), 0 trades afetados, campos `_repairedByIssue183*` gravados para auditoria/rollback. 5 testes novos (1895/1895 total). DEC-AUTO-183-01 (REMAP vs DELETE) e DEC-AUTO-183-02 (critério `studentEmail == MENTOR_EMAIL`) registradas em `docs/decisions.md`. Lock CHUNK-03 liberado. v1.43.1 marcada consumida. Worktree `~/projects/issue-183` removido.  
> **Última atualização (histórica):** 24/04/2026 — v0.40.0: Encerramento #181 — refactor documental mergeado (PR #182 squash `977694cd`). `CLAUDE.md` 450 → 159 linhas (índice). `docs/PROJECT.md` 2.161 → 287 linhas (overview). Extração temática em `docs/invariants.md`, `anti-patterns.md`, `decisions.md`, `milestones.md`, `tech-debt.md`, `chunks.md`, `cloud-functions.md`, `firestore-schema.md`, `protocols/{opening,closing,autonomous,parallel}.md`. Write-hot isolado em `CHANGELOG.md` raiz (743L), `docs/registry/{chunks,versions}.md`. Templates disciplinados em `docs/templates/`. `scripts/archive-issue.sh` + wire em `closing.md` §4.3 (variante `--all-closed` arquivou 9 issues fechadas em `docs/dev/archive/2026-Q2/`). Regras R1-R5 formalizadas em `CLAUDE.md` (limites de tamanho, formato estrito de worker report ≤2,5KB, decisões em arquivo próprio, template enxuto de issue control, separação read-hot/write-hot). Meta de economia: -60 a -75% em sessões típicas. Não tocou código. Sem bump `src/version.js`. Lock CHUNK-META liberado em `docs/registry/chunks.md`. Issue doc arquivada em `docs/dev/archive/2026-Q2/`. Worktree `~/projects/issue-181` removido.  
> **Última atualização (histórica):** 24/04/2026 — v0.39.0: Abertura #181 — refactor documental para sustentabilidade de tokens. `PROJECT.md` será reestruturado (extração de CHANGELOG §12, Invariantes §5, Anti-patterns §10, Decisões §9, Protocolos §4.0/§4.3/§8/§13, Chunks §6, DTs §11, Milestones §4 para arquivos temáticos próprios). `CLAUDE.md` vira índice operacional (~200 linhas). Regras novas R1-R5 (limites de tamanho, formato estrito de worker report ≤2,5KB, decisões em arquivo próprio, template enxuto de issue control, separação read-hot/write-hot). Meta: reduzir consumo de tokens em 60-75% em sessões típicas. Não toca código (apenas docs + 1 script shell `scripts/archive-issue.sh`). Sem chunk lock de código (CHUNK-META conceitual registrado em §6.3). Sem bump `src/version.js`. Branch `refactor/issue-181-docs-consolidacao`. Baseado em v0.38.0. Plano completo em `/home/mportes/.claude/plans/curious-soaring-crystal.md`.  
> **Última atualização (histórica):** 24/04/2026 — v0.38.0: Encerramento #178 — `cc-watchdog.sh` entregue (PR #179 squash `552f1edf`). Watchdog per-worktree em ~450 linhas detecta 3 classes de stall em sessões autônomas §13. **Classe 1 subdividida em TRANSITÓRIO (retry imediato 1×) vs QUOTA (retry agendado no horário de reset parseado do log, 2 emails por incidente — detecção + resultado)**. Parser de reset testado em 6 formatos (`4:20pm`, `11:00 PM`, `23:45`, `9:30 a.m.`, `1:00am`, sem-reset). **Emails granularizados**: 7 tipos `WATCHDOG_CLASS_*` adicionados ao `VALID_TYPES` do `cc-notify-email.py` + payload aceita campo opcional `ttl_seconds` (default 14400 preservado, watchdog usa 3600). Resolve bug de rate limit coarse-grained que silenciava emails distintos do mesmo stall. **Bug colateral corrigido**: `check_class2` usava `ls <glob>` sob `nullglob` ativo — glob sem match fazia `ls` sem args listar cwd e pegar `CHANGELOG.md` como falso `result.log`; fix via array bash. Smokes validados com evidência real: log do #119 task 19 (`You've hit your limit · resets 8:40pm`) acionou parse correto + email REAL no iCloud em `EMAIL_DRY_RUN=0`; retry agendado com stub `claude` gerou email 2 `FALHOU rc=1`; Classe 2 stall + Classe 2 entregue (sem falso positivo); Classe 3 LISTENER_MISSING + Classe 1b QUOTA no mesmo iter saíram ambos. `scripts/cc-status.sh` entregue junto (visão humana agregada multi-worktree). Integração em `cc-worktree-start.sh` (lança via `nohup` após listener, PID em `.cc-mailbox/.watchdog-pid`) + `cc-worktree-stop.sh` (mata via PID antes de tmux). Opt-out `CC_WATCHDOG_DISABLE=1`. Fast-follows: versionar `cc-notify-email.py` em `scripts/` (hoje só em `~/cc-mailbox/bin/`, mods do PR não viajam junto); smoke de recovery real (kill tmux + relaunch). Sem chunk lock. Sem bump `src/version.js`. Issue doc arquivada. Worktree removido.  
> **Última atualização (histórica):** 23/04/2026 — v0.37.0: Abertura #178 (meta-infra paralela ao #119 em curso). `cc-watchdog.sh` per-worktree para detectar 3 classes de stall em sessões autônomas §13 (Classe 1 resume da Coord com API error → retry 1× automático; Classe 2 worker travado → email HUMAN_GATE; Classe 3 tmux/listener morto → relaunch idempotente via `cc-worktree-start.sh`). Motivação direta: #119 travou 2× (socket error 07:29 task 05 + quota hit 12:32 task 14) sem watchdog — humano só percebeu por acaso. Sem chunk lock (meta-infra precedente #169/#176). Sem bump `src/version.js`. Baseado em v0.36.0. Também entregue nessa janela: `cc-status.sh` visão humana agregada multi-worktree em `~/cc-mailbox/bin/`.  
> **Última atualização (histórica):** 23/04/2026 — v0.36.0: Abertura #119 — Motor de progressão Maturidade 4D × 5 estágios em modo autônomo. Lock CHUNK-09 (escrita); leitura CHUNK-04/05/06/08. v1.43.0 reservada. Escopo todas as 6 fases (A engine puro + B persistência/CF + C UI aluno + D IA + E review snapshot + F mentor Torre). 7 decisões antecipadas fechadas com Marcio via INV-18 bloco único (gates 1→2 com origem framework/produto explícita, Sharpe daytrade sobre retornos diários agregados, regressão visível ao aluno tom "espelho", INV-15 subcollection aprovada com análise de custo, etc.). Baseado na v0.35.0.  
> **Última atualização (histórica):** 23/04/2026 — v0.35.0: Encerramento #176 — scripts de orquestração §13 entregues. Template canônico `~/cc-mailbox/templates/coord-briefing.md` + `cc-spawn-coord.sh` (wrapper §13.8 passo 8b com precondição cwd=worktree, render template com placeholders do control file, captura session_id do JSON) + `cc-dispatch-task.sh` (wrapper passos 8d/36 com `flock` + `--resume` da Coord). **Smoke integration 5/5 OK** em worktree sintético `issue-998`: spawn retornou session_id válido, start.sh gravou `.coord-id`/`.coord-dir` READ-ONLY, dispatch FIRST acordou Coord via `flock + --resume`, Coord leu control file e escreveu `inbox/01-smoke.md` com `SMOKE_OK` e morreu. §13.11 atualizado com 3 componentes novos **IMPLEMENTADO**, protocolo declarado **OPERACIONAL END-TO-END**. Próxima sessão dispara "atacar #NNN em modo autônomo" sem improviso. Pendente apenas Fase D (rodada real com worker + validator + email + recovery). Sem chunk lock. Sem bump `src/version.js`.  
> **Última atualização (histórica):** 23/04/2026 — v0.33.0: Encerramento #174 v1.42.1 (PR #175 mergeado, merge commit `d871fad2`). Wire de `setupsMeta` em `MentorDashboard` via `filterSetupsForStudent(setups, selectedStudent?.studentId)` (util puro em `src/utils/setupsFilter.js`, 10 testes). Completa E4 da spec de #170 cortado injustamente durante o merge. Isolamento estrito: setup de aluno X não vaza para aluno Y; fallback `null` retorna apenas globais. MentorDashboard agora importa `useSetups` + memoiza filtro + passa `setupsMeta={selectedStudentSetups}` ao `<SetupAnalysis>`. Aderência RR renderiza corretamente na visão do mentor. 1890/1890 testes (+10). Lock CHUNK-16 liberado. Issue doc arquivada. Worktree `~/projects/issue-174` removido. Memória operacional `feedback_spec_scope_respeito.md` gravada para não repetir o corte.   Substitui `SetupAnalysis.jsx` por card de diagnóstico operacional com 4 KPIs em grid 2×2 (EV por trade · Payoff · ΔT W vs L com semáforo ±20%/±10% · Contribuição ao EV total). Aderência RR condicional (só quando `setups.targetRR`), Sparkline 6m, Insight 1-linha, ordenação por \|contribEV\| desc, accordion "Esporádicos (N)" para setups com n<3. Util puro `src/utils/setupAnalysisV2.js`. Zero campo Firestore novo. Consumido em `StudentDashboard` via `useSetups` já presente. MentorDashboard sem `setupsMeta` (não filtra por student, fast-follow). Fast-fix `0bffe1f1` pré-merge: overflow do card corrigido (header em 2 linhas, truncate no nome, sublabels curtos, `Xm` em vez de `Xmin`). 1880/1880 testes (+40: 23 util + 17 render). Lock CHUNK-02 liberado (AVAILABLE). Issue doc arquivada. Worktree `~/projects/issue-170` removido.  
> **Criado:** 26/03/2026 — sessão de consolidação documental  
> **Fontes originais:** ARCHITECTURE.md, AVOID-SESSION-FAILURES.md, VERSIONING.md, CHANGELOG.md, CHUNK-REGISTRY.md  
> **Mantido por:** Marcio Portes (integrador único)

### Versionamento do PROJECT.md (INV-14)

Este documento segue versionamento semântico:
- **MAJOR (X.0.0):** reestruturação de seções, mudança de invariantes existentes, remoção de seções
- **MINOR (0.X.0):** novas invariantes, novas seções, novos chunks, novas DECs, mudança de protocolo
- **PATCH (0.0.X):** correções textuais, ajustes de formatação, atualização de status de DTs

**Histórico de versões do documento:**

| Versão | Data | Sessão | Mudanças |
|--------|------|--------|----------|
| 0.1.0 | 26/03/2026 | Consolidação documental | Criação — merge de 5 documentos |
| 0.2.0 | 29/03/2026 | Branding e tiers | DEC-029 a DEC-038, milestones, DT-027/028 |
| 0.3.0 | 30/03/2026 | Probing rehydration | DEC-043/044, INV-13, template issue-NNN |
| 0.4.0 | 02/04/2026 | Design Revisão Semanal | DEC-045/046, design #102, bash migration |
| 0.5.0 | 03/04/2026 | Dashboard-Aluno MVP | DEC-047 a DEC-052, CHUNK-13 a 16, INV-14, protocolo chunks |
| 0.5.1 | 03/04/2026 | Registro de issues | Issues #106-#119 nos milestones, #3 reescrito, #19 fechado |
| 0.6.0 | 03/04/2026 | Revisão #52 Prop Firms | DEC-053, escopo #52 atualizado com regras Apex Mar/2026 |
| 0.6.1 | 03/04/2026 | Fix #89 + v1.22.1 | firestore.rules DEC-025 plans, índice movements, #120 aberto, #66 fechado |
| 0.6.2 | 03/04/2026 | Reescrita #31 Feedback Semântico | DEC-054, abordagem escalonada rule-based + Gemini Flash |
| 0.6.3 | 04/04/2026 | Limpeza milestones | Fechar #44/#55/#56/#117, DT-007 RESOLVIDO, contagens atualizadas |
| 0.7.0 | 05/04/2026 | Controle de Assinaturas | #94 v1.23.0, DEC-055/DEC-056, CHUNK-16 liberado |
| 0.8.0 | 05/04/2026 | Revisão documental | INV-15/16, DT-030/031, mapa CFs atualizado, convenções bash, #94 fechado |
| 0.9.0 | 05/04/2026 | CHUNK-17 + lock #52 | CHUNK-17 Prop Firm Engine criado no registry, lock registrado para #52 |
| 0.10.0 | 05/04/2026 | v1.24.0 #122/#123 | RenewalForecast + whatsappNumber, CHANGELOG v1.24.0, CHUNK-02/16 lock |
| 0.10.1 | 05/04/2026 | Encerramento #122/#123 | DEC-060/061/062 adicionados, locks CHUNK-02/16 registrados retroativamente em §6.3 |
| 0.10.2 | 06/04/2026 | #122/#123 mergeados | PR #124 mergeado, locks CHUNK-02/16 liberados (AVAILABLE), removidos de Locks ativos |
| 0.12.0 | 10/04/2026 | Order Import V1.1 redesign | #93 v1.26.0, DEC-063 a DEC-067, criação automática + confronto enriquecido |
| 0.12.1 | 11/04/2026 | Reforço INV-16 worktree | INV-16 reescrita (obrigatória sempre), padrão único `~/projects/issue-{NNN}`, passo worktree explícito em §4.0 e CLAUDE.md §Ativação Automática |
| 0.13.0 | 12/04/2026 | #136 Prop Plan semântica + Ylos | DEC-068 a DEC-073, CHANGELOG v1.26.1-v1.26.4, templates Ylos + engine TRAILING_TO_STATIC phase-aware, correção semântica mecânica plano PROP, locks CHUNK-03/17 |
| 0.11.0 | 09/04/2026 | Prop Firm Engine deployado | #52 Fases 1/1.5/2 v1.25.0, DEC-060/061/062, DT-034/035, correção ATR v2 |
| 0.14.0 | 13/04/2026 | #134 Prop Dashboard v1.27.0 | PropAccountCard gauges + PropAlertsBanner 3 níveis + sparkline drawdownHistory + tempo médio trades universal + PropPayoutTracker (qualifying days, eligibility, simulador saque), CHUNK-02/17 lock, 77 testes novos |
| 0.14.1 | 13/04/2026 | Encerramento #134 | PR #138 mergeado, locks CHUNK-02/17 liberados (AVAILABLE), issue doc movida para archive, DEC adicional: PhaseSelector (transição de fase semântica) + DebugBadge `embedded` prop |
| 0.14.2 | 13/04/2026 | Protocolo §4.3 — rm -rf worktree | Adicionada 2ª etapa obrigatória no passo 5 de encerramento: `rm -rf ~/projects/issue-{NNN}` após `git worktree remove` para limpar diretório físico residual (cache .vite, etc.) |
| 0.15.0 | 13/04/2026 | Encerramento #134 + reforço protocolo | AP-08 Build Verde App Quebrada, §4.0 reordenado (shared files antes do worktree), §4.2 validação browser obrigatória |
| 0.16.0 | 14/04/2026 | Encerramento #129 Shadow Behavior | v1.28.0, 15 padrões comportamentais, CF callable analyzeShadowBehavior, DEC-074 a DEC-079, CHANGELOG [1.28.0], lock CHUNK-04 liberado |
| 0.17.0 | 15/04/2026 | #133 AI Approach Plan v1.29.0 | CF generatePropFirmApproachPlan Sonnet 4.6, prompt v1.1 com 6 correções #136 (MECÂNICA DIÁRIA, RITMO DE ACUMULAÇÃO, read-only, coerência mecânica, Path A/B), validate.js com 7 grupos incluindo coerência mecânica, fallback determinístico sem consumo de cota, UI seção colapsável PropAccountCard, 24 testes novos, lock CHUNK-17 |
| 0.17.1 | 15/04/2026 | Encerramento #133 | PR #140 mergeado, lock CHUNK-17 liberado (AVAILABLE), issue doc movida para archive, worktree removido |
| 0.18.0 | 15/04/2026 | #118 Barra de Contexto Unificado + encerramento | v1.30.0, StudentContextProvider + ContextBar + cycleResolver, DEC-080 a DEC-083, CHANGELOG [1.30.0], §4.0 diretiva operacional Claude Code (autorização permanente de leitura), 46 testes novos, locks CHUNK-02/13 liberados, PR #141 mergeado |
| 0.18.1 | 15/04/2026 | §4.0 reserva de versão na abertura | Fase 3 ler `version.js` + reservar próximo minor + commitar junto com locks. §4.2 passa a aplicar versão reservada. Elimina conflito de versão na origem (lição aprendida após rebase #118 ter precisado bumpar 1.29→1.30 em cima do #133) |
| 0.19.0 | 15/04/2026 | #142 Order Import Tradovate v1.31.0 | FORMAT_REGISTRY extensível em orderParsers.js, auto-detect ProfitChart vs Tradovate por header signature (threshold 0.5 / 0.6), parser parseTradovateOrders com Papa.parse quote-aware, remove gatekeep hardcoded em OrderImportPage.jsx, detecção multi-delimitador (; e ,), shape canônico idêntico entre parsers, downstream agnóstico inalterado, 19 testes novos (2 Fase A + 17 Fase B), fixtures reais april/feb conta Apex, validado em browser |
| 0.19.1 | 15/04/2026 | Encerramento #142 | PR #143 mergeado, lock CHUNK-10 liberado (AVAILABLE), issue doc movida para archive, worktree removido |
| 0.20.0 | 15/04/2026 | Abertura #145 Mesa Prop v1.32.0 | Locks CHUNK-02/17, v1.32.0 reservada em version.js, Página dedicada Mesa Prop — extrair componentes prop do Dashboard (epic #144) |
| 0.20.1 | 15/04/2026 | Abertura #102 Revisão Semanal v1.33.0 | Lock CHUNK-16, v1.33.0 reservada em version.js, Revisão Semanal Fases A-D (#106 absorvido como Fase A) |
| 0.21.0 | 16/04/2026 | Abertura #146 fix Novo Plano v1.34.0 | Locks CHUNK-02/03 (bypass CHUNK-02 lock #145 — sessão solo autorizada), v1.34.0 reservada, mover criação de plano de DashboardHeader para AccountDetailPage |
| 0.21.1 | 16/04/2026 | Encerramento #146 v1.34.0 | PR #147 mergeado, locks CHUNK-02/03 liberados (AVAILABLE), issue doc arquivada, worktree removido, CHANGELOG [1.34.0] |
| 0.22.0 | 17/04/2026 | INV-17 + INV-18 — gates de arquitetura e spec review | INV-17 (Gate de Arquitetura de Informação — nível/domínio/duplicação/budget + mapa de slots fixos) e INV-18 (Spec Review Gate — validação de entendimento obrigatória antes de codificar, formato por tipo UI/CF/lógica/Firestore) adicionadas a CLAUDE.md e §3 Invariantes. §4.1 Gate Pré-Código ganhou itens de checklist para INV-17/INV-18. §5 checklist de impacto atualizado para "INV-01 a INV-18" |
| 0.22.1 | 19/04/2026 | Encerramento #145 v1.32.0 | PR #152 mergeado (redesign Mesa Prop em 4 zonas: status agora / retrospectivo / contrato da mesa / payout). 5 componentes novos (PropEquityCurve, PropHistoricalKPIs, TemplateCard, PlanoMecanicoCard, PropViabilityBadge) + lógica pura propViabilityBadge (6 estados phase-aware). useDrawdownHistory MAX_DOCS 100→1000. AI Approach Plan migrou para #148 (RESERVADO, gate 4D+30 shadow trades). Hotfix #149 cancelada (bug só existia em branch). Locks CHUNK-02 + CHUNK-17 liberados. Issue doc arquivada em docs/archive/. Spec Review Gate INV-18 aplicado (iteração 3). 16 testes novos, 1567/1567 passando |
| 0.22.2 | 19/04/2026 | #128 deepdive — SPEC v1.0 + IMPACT v0.2 em referência | SPEC-importacao-plano-v1.0 e IMPACT-importacao-plano-v0.2 adicionados a `docs/reference/` como material de domínio citável (não protocolo formal). Deepdive produziu: 4 INVs (INV-19 a INV-22), 4 APs (AP-09 a AP-12), 7 DECs (DEC-084 a DEC-090), 6 issues derivadas (ISSUE 1-6). ISSUE 7 (migração shadowBehavior) cancelada, virou DT-036 com trigger de reconsideração em > 5000 trades. Framework de bundle formal INV-19 abandonado em favor de modo interativo (pair programming assíncrono com coder). Invariantes da spec ficam como vocabulário/referência, não como gate. |
| 0.22.3 | 19/04/2026 | Cancelar ISSUE 6 — reconciliação vira DT-037 | ISSUE 6 (reconciliação de agregados INV-22) cancelada. DT-037 registrada com trigger de reconsideração em > 1000 trades OU primeiro incidente real de divergência OU escala que exija observabilidade defensiva. Priorização do Marcio: resultado concreto dia-a-dia + material para marketing/lead capture. Issues ativas do deepdive #128 agora: ISSUE 1 a ISSUE 5 (fundação + imports + plano + mesa). |
| 0.22.4 | 19/04/2026 | Encerramento #154 v1.36.0 | PR #155 mergeado (fast-forward, merge commit 6caf02c9). Card de conta em `AccountsPage` (visão aluno) e `StudentAccountGroup` (visão mentor) ganha botão "Novo plano" (ícone `PlusCircle` emerald); click passa flag `_autoOpenPlanModal` para `AccountDetailPage` que abre `PlanManagementModal` via `useEffect`. Preserva "casa do pai" (modal não duplica surface, AP-11 não violado). Resolve workaround crítico: hoje aluno criava conta Mesa → revertia para Real → corrigia plano. **Primeira issue entregue em modo interativo** (sem bundle formal INV-19, sem R1/R2/R3) — protocolo §4.0 disciplina básica (issue + worktree + control file + PR + Closes) foi suficiente. 1567/1567 testes passando, zero regressão. Worktree removido, issue doc arquivada. |
| 0.22.5 | 19/04/2026 | DT-038 — cancelar estrutura 3 camadas do trade | Revisão pragmática da ISSUE 1 do deepdive #128. Estrutura proposta (`_rawPayload` imutável + projeção canônica + `_enrichments[]` append-only) responde a perguntas que o produto não faz hoje: (A) preservação após N enrichments — `_enrichmentSnapshot` atual cobre 95%; (B) histórico granular — sem demanda de UI; (C) rollback específico — caso de borda raro; (D) auditoria evolutiva — sem necessidade. Migração custaria 2-3 dias + risco em prod. DT-038 registrada com 3 triggers de reconsideração. Parte técnica de ISSUE 1 desarmada. **ISSUE 3 cirúrgica destravada** (fix bypass #93 + UX conversacional + AutoLiq badge + segmentação por ticker), sem dependência de refactor arquitetural. Princípio operacional formalizado: "não é falta de controle, é ritmo" — cancelar rigor que responde perguntas ainda não feitas é cadência correta. Também: scripts `cc-worktree-start.sh` + `cc-worktree-stop.sh` criados (commit c52da7ef) para orquestração coordenador→coder via mailbox em `.cc-mailbox/` dentro do worktree (POC validado). |
| 0.22.6 | 19/04/2026 | Fase A de #156 entregue + DT-039 | **Primeira issue entregue em modelo coordenador+worker via mailbox file-drop** (este coordenador escreve prompt → listener tmux `cc-156` dispara `claude -p` headless no worktree → worker executa e relata em `.cc-mailbox/outbox/`). Task 01 (discovery) e Task 02 (Fase A) completas. Commit `1e034534` no branch `arch/issue-156-order-import-staging-conversacional`: `OrderImportPage.jsx` não escreve mais `shadowBehavior` direto em `trades` (removidas 52 linhas); hook `useShadowAnalysis` invoca CF canônica `analyzeShadowBehavior`. Novo arquivo `src/__tests__/invariants/tradeWriteBoundary.test.js` (106 linhas, 6 testes) — grep-based, falha build se novos writers aparecerem em `trades` fora da whitelist. 1573/1573 testes passando. DT-039 registrada: 4 arquivos legados mantidos em whitelist GRANDFATHERED (useTrades/useAccounts/usePlans CRUD + seedTestExtract) — refatoração fica para ISSUE 1 do épico #128 ou primeiro incidente que exija migração. |
| 0.22.8 | 20/04/2026 | Encerramento #102 v1.38.0 | PRs #157 (rules alunoDoneIds — merged `e9d5de8d` + deployado via `firebase deploy --only firestore:rules`) e #160 (squash `30af3a18`) mergeados em sequência. Entrega consolidada da **Revisão Semanal v2**: (a) `WeeklyReviewPage` nova com 8 subitens conforme mockup aprovado (Trades tabela + day-grouping, Notas da sessão, 8 KPIs com tooltip inline, SWOT IA 4 quadrantes, Takeaways checklist, Ranking top/bottom, Maturidade 4D, Navegação contextual) + Action Footer Publicar/Arquivar (gate de fechamento que faltava); (b) **carry-over de takeaways** `!done` entre revisões do mesmo plano, badge `↻ anterior`; (c) **PendingTakeaways** no dashboard do aluno (rule nova permite `alunoDoneIds` via arrayUnion em CLOSED, badge `aluno ✓` amber visível pro mentor na revisão); (d) **PendingReviewsCard** trigger secundário G8 no MentorDashboard (N-listener pattern, evita índice COLLECTION_GROUP novo). Coexiste com `PlanLedgerExtract` 3-col baseline (ReviewToolsPanel), preservado intacto para comparação. Bugfixes relevantes: hijack `viewingAsStudent → StudentDashboard` movido para DEPOIS do check `currentView==='onboarding'` no App.jsx; retorno contextual do ledger e assessment; `closeReview` preserva campos não-passados (undefined-check). DEC-086/087 adicionados. Issue **#159** criado como QA tracker (14 blocos ~120 checkboxes, validação em produção). Lock CHUNK-16 liberado (AVAILABLE). Issue doc arquivada em `docs/archive/`. Worktree `/home/mportes/projects/issue-102` removido (git worktree remove + rm -rf). 1727/1727 testes passing (baseline pré-sessão 1583 + carry-over +4 + outros merges). Zero regressão. |
| 0.22.9 | 20/04/2026 | Abertura #162 SEV1 hotfix | Plataforma fora do ar em produção — `ReferenceError: assessmentStudentId is not defined` em `src/pages/StudentDashboard.jsx:362` (prop `studentId` de `<PendingTakeaways>` referencia identificador inexistente). Introduzido pelo merge PR #160 (#102 v1.38.0, commit `30af3a18`). Lock CHUNK-02 registrado em §6.3 para `fix/issue-162-hotfix-assessment-student-id`. `src/version.js` bumped para v1.38.1 + entrada CHANGELOG reservada. Worktree `~/projects/issue-162` a criar no próximo passo §4.0. Fix: substituir por `overrideStudentId \|\| user?.uid` (padrão canônico linha 558 e hooks irmãos `useTrades/useAccounts/usePlans`). |
| 0.22.10 | 20/04/2026 | Encerramento #162 v1.38.1 | PR #163 mergeado (merge commit `3192353b`, squash). Fix 1-linha em `StudentDashboard.jsx:362` — `assessmentStudentId` → `overrideStudentId \|\| user?.uid`. Deploy Vercel validado em produção por Marcio ("plataforma voltou"). Adicionado teste invariante `studentDashboardReferences.test.js` (grep-based, padrão #156 `tradeWriteBoundary`). 1728/1728 testes passing (+1 vs baseline pré-hotfix 1727). Lock CHUNK-02 liberado (AVAILABLE). Issue doc arquivada em `docs/archive/`. Worktree `~/projects/issue-162` removido (git worktree remove + rm -rf). **Lições:** (a) QA tracker #159 não cobriu render do dashboard aluno com `<PendingTakeaways>` montado — gap de validação do #102; (b) `npm run lint` (eslint `no-undef`) teria pegado o erro em CI — candidato a fast-follow tornar required. |
| 0.23.8 | 21/04/2026 | §4.3 rm -rf obrigatório | `rm -rf ~/projects/issue-{NNN}` é passo obrigatório após `git worktree remove` — sessões anteriores omitiam, deixando resíduos físicos. Verificação `ls ~/projects/` adicionada ao protocolo. |
| 0.27.0 | 22/04/2026 | Encerramento #164 v1.41.0 | PR #171 mergeado (merge commit `f3d46895`). Dashboard Aluno ajustes: 4 entregas (E1 SWOT via `useLatestClosedReview` resiliente a `planId` stale / E2 card "Consistência Operacional" CV+ΔT / E3 Matriz Emocional 4D Opção D com sublabels e rename Maturidade / E5 EquityCurve tabs multi-moeda + curva ideal do plano). Cascata `selectedPlanId` → cards via `accountsInScope` em `useDashboardMetrics`. ContextBar preserva `accountId` do usuário e lista todos os planos em "Todas as contas". AccountFilterBar removido. **2 bugs out-of-scope carregados**: (a) trade edit `exchange: undefined` pós import CSV (fix em 3 camadas `useCsvStaging`+`AddTradeModal`+`useTrades`); (b) #102 PinToReviewButton salvava em `takeawayItems`/`takeaways` — corrigido para `sessionNotes` via novo `appendSessionNotes`. 1732 → 1840 testes (+108). 37 commits. Lock CHUNK-02 liberado (AVAILABLE). Issue doc arquivada em `docs/archive/`. Worktree removido. |
| 0.26.0 | 22/04/2026 | §13 amendments — bugs rodada #164 (cross-worktree, recovery, CLAIMS obrigatório) | Primeira execução real do §13 (issue #164) expôs 3 bugs com evidência direta: (1) **Cross-worktree do `.coord-dir`** — session `5cd03bd7` (pts/4) criada com cwd = main repo teve JSONL em project-scope `-acompanhamento-2-0`; listener do worktree invocando `--resume` falhou silenciosamente. §13.7 ganha `.coord-dir` com mesma disciplina READ-ONLY da INV-26; §13.8 passo 8b explicita pré-condição "cwd = worktree" como dura. (2) **§13 sem cláusula de recovery** — CC-Interface morreu; nova precisou sobrescrever `.coord-id` pra funcionar, violando INV-26 literalmente. Nova §13.15 "Protocolo de Recovery de CC-Interface" formaliza os 5 passos (process check, JSONL scope check, spawn sem --resume, escrita excepcional de .coord-id + .coord-dir, registro em §3.2). INV-26 ganha amendment explicitando recovery como única exceção ao READ-ONLY. (3) **Briefing sem exigência de CLAIMS** — worker E3 entregou em formato livre porque o prompt não pediu CLAIMS, deixando validação INV-27 totalmente manual. §13.9 ganha parágrafo obrigando cláusula explícita de CLAIMS em todo briefing, sob pena de STOP-HALLUCINATION. Escopo intencionalmente enxuto: 2 bugs adicionais ("fantasma de ação não-tomada", dedup-por-brewing-prolongado) foram desinflados por evidência circunstancial — ficam como observações em memória, não amendments estruturais. Lição de método: em fase de design pós-execução, desinflar antes de commitar — pressão social de "ser útil" + narrativa do humano podem inflar diagnóstico para além da evidência. |
| 0.25.0 | 21/04/2026 | §13 Implementação Autônoma + INV-27/28 + amendment INV-26 | Sessão de design de 5+ horas consolidada. Adiciona §13 com protocolo completo de modo autônomo (CC-Interface + CC-Coord + CC-Worker), 6 fases compactas, canais de comunicação com ownership de diretórios, máquinas de estado por ator, regra de trigger explícito ("atacar #NNN em modo autônomo"), critério opt-in vs modo interativo default, bloco CLAIMS obrigatório em report do worker, validator com 3 checks (commit/tests/files), 10 decisões de design enxutas (bugs 1-10), tipos de email + rate limit, notas operacionais sobre prompt cache. INV-27 formaliza validação externa contra cegueira epistêmica (modelo pode não detectar própria alucinação; auto-declaração "não aluciei" insuficiente). INV-28 estabelece email iCloud como canal primário de gate humano. INV-26 amendment explicita que CC-Interface também é READ-ONLY para `.coord-id`. Scripts python (`cc-notify-email.py`, `cc-validate-task.py`) e refactor de `cc-worktree-start.sh` ficam pra issue formal futura (INV-07/09); esta entrada é SPEC TEXTUAL, não implementação. Lição crítica: design aprovado em sessão não é autoridade até ser commitado no main — qualquer sessão nova só enxerga o que está no PROJECT.md. |
| 0.24.0 | 21/04/2026 | Abertura #164 — lock CHUNK-02 + v1.41.0 reservada | Dashboard Aluno ajustes: spec aprovada com 4 entregas (E1 SWOT reaproveita `review.swot` + fallback / E2 card "Consistência Operacional" CV P&L + ΔT W/L substitui RR Asymmetry e Tempo Médio isolado / E3 Matriz Emocional 4D Opção A com expectância+payoff+shift+ΔWR+sparkline / E5 EquityCurve com tabs por moeda + curva ideal do plano por trajetória linear de dias corridos quando planId único). E4 (cards desatualizados) removida — Marcio confirmou nenhum dos 10 cards stale. CHUNK-02 escrita; CHUNK-04/06/13/16 leitura. |
| 0.23.7 | 21/04/2026 | Encerramento #166 v1.40.0 | PR #168 mergeado (merge commit `ca74b289`). Fix Sev1: botão "Finalizar" em ProbingQuestionsFlow com try/catch + disabled + spinner; fromStatus='probing' em completeProbing; DebugBadge corrigido. 4 testes novos, 1732/1732 passando. CHUNK-09 liberado (AVAILABLE). Issue doc arquivada. Worktree removido. |
| 0.23.6 | 21/04/2026 | INV-26: `.coord-id` é responsabilidade do start script | Coord nunca sobrescreve `.coord-id` — valor gravado pelo `cc-worktree-start.sh` no boot do listener. Anti-pattern: inventar session ID quando `$CLAUDE_SESSION_ID` retorna vazio. Lição #166. |
| 0.23.5 | 21/04/2026 | Abertura #166 — lock CHUNK-09 + v1.40.0 reservada |
| 0.23.4 | 21/04/2026 | Encerramento #165 v1.39.0 | PR #167 mergeado (merge commit `0bdaa1a0`). sessionNotes no painel lateral, filtro trades revisados via `includedTradeIds`, botão contextual "Continuar Rascunho". 16 testes novos, 1744/1744 passando. Locks CHUNK-02/08 liberados (AVAILABLE). Issue doc arquivada. Worktree removido. |
| 0.23.3 | 21/04/2026 | INV-25: outbox antes de resume | Formaliza invariante do padrão coord/worker: output do worker persiste em outbox antes do `--resume`. Coord relê sempre do disco, nunca assume memória do worker. Origin: recovery manual #165. |
| 0.23.2 | 20/04/2026 | §4.0 coord/worker: coord deve abrir do worktree | Lição #165: `claude --resume` procura JSONL no projeto correspondente ao cwd de invocação. Coord aberto no main → listener no worktree não encontra sessão. Regra adicionada: após criar worktree, entrar nele (`cd ~/projects/issue-NNN`) antes de abrir a sessão coord. |
| 0.23.1 | 20/04/2026 | Abertura #165 — locks CHUNK-02/08 + v1.39.0 reservada |
| 0.23.0 | 20/04/2026 | §4.2 Gate Pré-Entrega — precauções #162 | Lições do SEV1 #162 incorporadas como ITENS OBRIGATÓRIOS do gate pré-entrega (antes eram apenas notas no CHANGELOG v1.38.1): (a) **`npm run lint` em arquivos tocados no branch** — zero `no-undef`, zero `no-unused-vars` críticos, zero regressão em regras já ativas. Custo ~5s/arquivo. Origem: ReferenceError `assessmentStudentId` teria sido pego aqui. (b) **Validação em browser por contexto de consumo** — aluno logado / mentor viewAs / override-embedded, quando aplicáveis. Origem: o #102 validou apenas o contexto mentor (WeeklyReviewPage) e deixou o contexto aluno (dashboard com `<PendingTakeaways>`) passar para prod sem render-check. Bump MINOR (mudança de protocolo §1 do versionamento). Sem lock de chunk — edição exclusivamente em shared file `docs/PROJECT.md`. |
| 0.22.7 | 20/04/2026 | Encerramento #156 v1.37.0 | PR #158 mergeado. Épico de 6 fases (A-F) consolidado em um único PR: (A) shadow writer bypass removido + invariante `tradeWriteBoundary`; (B) schema classificação persistente (5 classes) em `ordersStagingArea` + `autoLiqDetector`; (C) UX conversacional `ConversationalOpCard` substitui auto-create #93 + `AutoLiqBadge` + gate plano retroativo; (D) reconstrução robusta — segmentação por instrument + agregação N×M fills + gap 60min; (E) enrichment sem duplicata — helper puro `conversationalIngest` + `AdjustmentModal` diff fino + persist `discarded` em `orders`; (F) wire `onRequestRetroactivePlan` em App→StudentDashboard→OrderImportPage fechando gate + bump v1.37.0. **1689/1689 testes** (+122 vs baseline pré-#156 de 1567), invariante verde, zero regressão. Delta de shared files: `version.js` bumped, `firestore.rules` não tocado pela issue. Worktree removido, tmux `cc-156` killed, issue doc arquivada em `docs/archive/`, Product Board item movido para Done. **Infra operacional nova:** scripts `cc-worktree-{start,stop}.sh` + mailbox file-drop (`.cc-mailbox/`) + suporte opcional a `COORD_SESSION_ID` para notificação inversa via `claude --resume` validada em teste isolado — aplicável a partir do próximo épico. |
| 0.28.0 | 22/04/2026 | Abertura #170 SetupAnalysis V2 v1.42.0 | Lock CHUNK-02 (escrita), CHUNK-04/16 (leitura). v1.42.0 reservada em version.js + entrada CHANGELOG RESERVADA. Spec aprovada: card de setup com 4 KPIs operacionais em grid 2×2 (EV por trade, Payoff, ΔT W vs L com semáforo ±20%/±10%, Contribuição ao EV total) + Aderência RR condicional quando `setups.targetRR` existe + Sparkline 6m (reusa padrão Matriz 4D) + Insight 1-linha. Ordenação por \|contribEV\| desc; setups <3 trades em accordion "Esporádicos (N)" no rodapé. Util puro novo `src/utils/setupAnalysisV2.js` com 15-20 testes antes da UI. `SetupAnalysis.jsx` substituído mantendo API externa (prop `trades`) + nova prop opcional `setupsMeta`. Zero campo Firestore novo. Consumido em `StudentDashboard` e `MentorDashboard` (ambos já importam). Origem: spin-off do review de #164. Milestone v1.2.0 Mentor Cockpit. Modo autônomo degradado (scripts `cc-notify-email.py` + `cc-validate-task.py` ainda não existem — email manual via RC). |
| 0.29.0 | 22/04/2026 | Abertura #169 scripts Protocolo Autônomo §13 Fases A+B+C | Meta-infra: tirar §13 do modo degradado. Escopo desta sessão: (A) `~/cc-mailbox/{bin,log,templates}/` + `.env.example` + `cc-notify-email.py` (iCloud SMTP `smtp.mail.me.com:587` STARTTLS, rate limit por `(issue, type)` em 4h via `notify-state.json`, 7 TIPOs §13.10, body template com seção COMO RESPONDER, log agregado); (B) `cc-validate-task.py` com 3 checks §13.9 (commit_exists, tests_match, files_match), <300ms, STOP-HALLUCINATION em falha, regra `tests: skipped` apenas para `.md`/`docs/`, pytest unit; (C) refactor `cc-worktree-start.sh` 3-tier (aceita `INTERFACE_SESSION_ID` + `COORD_SESSION_ID`, grava `.coord-id` + `.coord-dir` READ-ONLY — INV-26, pré-condição `cwd = worktree` abortando execução, cria todos os dirs §13.7), template canônico `~/cc-mailbox/templates/worker-briefing.md` com cláusula CLAIMS obrigatória. **Fase D (rodada end-to-end + bump §13.11 "A ESCREVER" → implementado) separada** — requer issue-teste real em modo autônomo puro. Sem chunk lock (infra fora do produto Espelho). Sem bump em `src/version.js`. Branch `arch/issue-169-scripts-protocolo-autonomo`. |
| 0.40.0 | 24/04/2026 | Encerramento #181 — refactor documental mergeado | PR #182 squash `977694cd`. CLAUDE.md 450 → 159 linhas (índice com links). PROJECT.md 2.161 → 287 linhas (overview). 11 arquivos temáticos novos em `docs/` (invariants, anti-patterns, decisions, milestones, tech-debt, chunks, cloud-functions, firestore-schema + protocols/{opening,closing,autonomous,parallel}). Write-hot separado: `CHANGELOG.md` raiz (743L), `docs/registry/{chunks,versions}.md`. Templates em `docs/templates/{issue-control,worker-report}.md`. Script `scripts/archive-issue.sh` + variante `--all-closed` arquivou 9 issues fechadas em `docs/dev/archive/2026-Q2/` (089, 092, 094, 096, 097, 122, 156, 165, 166). Regras R1-R5 formalizadas em CLAUDE.md (limites de tamanho, formato estrito de worker report ≤2,5KB, decisões em arquivo próprio, template enxuto de issue control, separação read-hot/write-hot). Validação Fase 6: todos arquivos dentro do R1; 22 INVs + 8 APs + 17 CHUNKs com anchors íntegros; 0 refs PROJECT.md §N quebradas ativas. Meta: -60 a -75% de tokens em sessões típicas (abertura ~30K → ~10K; loop autônomo 19 tasks ~200-300K → ~50-70K; worker individual ~50K → ~12-15K). Não tocou código. Lock CHUNK-META liberado. Issue doc arquivada. Worktree `~/projects/issue-181` removido. |
| 0.40.37 | 30/08/2026 | Encerramento #408 — fila em árvore + o fato atômico no lugar certo | PR #412 squash `94dbeee5`. v1.85.0. Árvore aluno → dia → plano → trade sobre `buildPeriodState` (#402), sem persistência nova. `lembretesDoPeriodo` na revisão semanal. "Aberta sem previsão de stop" substitui "sem orçamento" e desce do lembrete do dia para a linha da operação. `orderingConfidence` passa a detectar `tied_instants` (dois espelhos + paridade) e a tela cala todo predicado de sequência quando a ordem não é afirmável — sem que o silêncio vire absolvição no `BehaviorPanel`. Adaptador `tradesDoSnapshotDaRevisao` para a projeção do snapshot. Doc fantasma removido da base (380 trades, todos com plano). `docs/data-dictionary.md` gerado da produção + regra no CLAUDE.md. DEC-AUTO-408-01..08. 4.475 testes. |
| 0.40.38 | 31/08/2026 | Encerramento #414 — relatório do mês no lugar do Diário | PR #415 squash `503c7739`. v1.86.0. Rota `journal` vira leitura: trade · observação de entrada + refs HTF/LTF · feedback do mentor. "Tem feedback" lê `mentorFeedback` legado E `feedbackHistory` com dedup; recorte por `trade.date`; total por moeda. `TradesJournal.jsx` apagado. Sem persistência nova. |
| 0.40.39 | 02/09/2026 | Encerramento #416 — a esteira de fechamento para de mentir | PR #417 squash `f12e147e`. v1.86.1. 13 correções, seis com a mesma causa: número/texto derivado de proxy em vez do dado. Monte Carlo errava 30× por divisor `1000` hardcoded — o motor estava certo, o erro era exibição (`pctOfBase` novo). SUSTENTAR sugeria replicar a entrada do trade de vingança, com o guard já existente no sustain vizinho. Custo emocional colorido pelo sinal do P&L. Hints do TPS por predicado sobre o dado. `ruleViolationRate` passa a honrar `feedsGates` via `GATE_CODES`, alinhando ao mapa aprovado em 01/06. Aderência exclui trade sem `compliance`. Gate de estratégia mede `RISK_FIELDS` em `editHistory`, não concentração de setup. `maxDD` determinístico. DEC-AUTO-416-01..23. 4.646 testes. |
| 0.40.40 | 03/09/2026 | Encerramento #418 — a etapa Ajustar explica Kelly e Monte Carlo | PR #419 squash `51f5ccbf`. v1.87.0. A única das oito etapas sem título e sem explicação passa a ter veredito → evidência → recomendação → decisão. Kelly vira "até quanto dá pra arriscar sem se tirar do jogo"; Monte Carlo vira "que faixa esperar", com probabilidade de perda como manchete. Histograma decorativo desde o #259 (altura pelo índice da barra) vira a distribuição real: `histogram` e `pLoss` aditivos no motor, com teste de não-regressão dos percentis. Cópia em `adjustExplainers.js` lê `triggeredRule`, nunca re-deriva regra; risks do advisor traduzidos. Junto: `R$` hardcoded, Kelly 0,0285% exibido como "0.0%", nome duplo do RR. Sem persistência nova. 4.696 testes. |
| 0.40.41 | 04/09/2026 | Encerramento #144 — a Torre é a única porta | PR #420 squash `c0ad32e3`. v1.88.0. Um sintoma ("não parece um SaaS profissional, não há encaixe de navegação"), três causas. **Não existia roteador:** a navegação era uma string em `App.jsx`, e por isso cada travessia tinha regra própria — oito estados de contexto de retorno e flags `_from*` no objeto do trade. `App.jsx` 562→28 linhas; casca vira `AppShell`, papel vira guard de rota, endereço vira SSoT em `routes/paths.js`, voltar vira histórico (`useVoltar`). **Dois sistemas de navegação concorrentes** faziam o mentor logar em Análises e não na Torre: menu de 9→5 itens, barra de abas e dicionário de tradução mortos, três filas viram Minhas Pendências (com Fechamentos, que só existia no menu). "Precisam Atenção" sai com perda de sinal declarada — recortava performance, a Torre recorta conduta. **Design system contornado:** container e título saem das 14 páginas para `AppShell`+`PageHeader`, escala de raio em três degraus, `rounded-md` absorvido, 44 superfícies viram primitiva. Três trechos de código morto revelados (bloco "Lista de Alunos" inalcançável, `AddTradeModal` órfão do App, `useMentorClosureInbox` do menu). DEC-144-01..08, PEND-144-01 (qual é o botão primário). Sem persistência nova, sem CF. 4.768 testes. |
| 0.40.42 | 04/09/2026 | Encerramento #421 — hotfix: tela do mentor não subia | PR #422 squash `abb5fd8e`. v1.88.1. TDZ em `selectedStudent`: era `useState` no topo, virou `useMemo` derivado da rota no #144 e foi declarado depois do primeiro uso. Atravessou build, lint e 4.768 testes porque **nenhum teste montava o MentorDashboard** — AP-08. `MentorDashboardMount.test.jsx` monta as quatro views e os três casos da ficha; verificado contra o código quebrado em produção. Sem mudança de UI nem de persistência. |
| 0.40.43 | 04/09/2026 | Encerramento #423 — menu do mentor recupera os destinos | PR #424 squash `764ab550`. v1.88.2. Regressão de desenho do #144: menu de 9→5 itens colocou os destinos dentro da Torre, abaixo de "A Turma", que renderiza a turma inteira sem limite — tudo do dia a dia caiu abaixo da dobra. Os cinco destinos voltam ao menu com badge, "Minhas Pendências" sobe para antes de "A Turma", "Precisam Atenção" volta como tela. DEC-423-01..05 revogam DEC-144-01/02/04: a Torre é a home, não a única porta. |
| 0.39.0 | 24/04/2026 | Abertura #181 — refactor documental (sustentabilidade de tokens) | PROJECT.md será reestruturado em arquivos temáticos (invariants, anti-patterns, decisions, protocols/*, cloud-functions, firestore-schema, chunks, tech-debt, milestones). CHANGELOG §12 extraído para `CHANGELOG.md` raiz. CLAUDE.md vira índice operacional. Regras R1-R5 novas (limites de tamanho, formato estrito de report de worker, etc.). Meta: -60 a -75% de tokens em sessões típicas. Não toca código. CHUNK-META registrado em §6.3. Branch `refactor/issue-181-docs-consolidacao`. |
| 0.38.0 | 24/04/2026 | Encerramento #178 — `cc-watchdog.sh` + granularização de emails | PR #179 mergeado (squash `552f1edf`). Watchdog per-worktree (~450 linhas) em `scripts/cc-watchdog.sh` + visão humana agregada `scripts/cc-status.sh` (~209 linhas) + integração em `cc-worktree-start.sh` (+22) / `cc-worktree-stop.sh` (+13). Loop 90s puro bash, zero API no idle. **Classe 1 subdividida**: TRANSITÓRIO (retry imediato 1×) vs QUOTA (parseia horário de reset do log via `date -d`, grava marcador `outbox/<N>-quota-retry-at` com epoch, dispara retry no tick seguinte ao reset, emite 2 emails por incidente — detecção + resultado). Parser de reset testado em 6 formatos. Classes 2 (worker travado >T_WORKER) e 3 (tmux/listener morto) como abertura. **Granularização de emails (fix de design pós-smoke)**: `~/cc-mailbox/bin/cc-notify-email.py` ganhou 7 tipos `WATCHDOG_CLASS_*` em `VALID_TYPES` + campo opcional `ttl_seconds` no payload (default 14400 preservado, watchdog passa 3600). Elimina rate-limit coarse-grained que silenciava emails distintos do mesmo stall — cada classe/evento tem slot independente. **Bug colateral corrigido**: `check_class2` com `ls <glob>` sob `shopt -s nullglob` ativo pegava `CHANGELOG.md` do cwd como falso `result.log`; fix via array bash (`local -a matches=(...); result="${matches[0]:-}"`). **Smokes validados contra evidência real**: log do #119 task 19 (`You've hit your limit · resets 8:40pm (America/Sao_Paulo)`) acionou parser correto → `2026-04-24 20:40:00 -03` + email REAL via SMTP iCloud `WATCHDOG_CLASS_1B_QUOTA_SCHEDULED`; retry agendado com stub `claude` mockado gerou email 2 `WATCHDOG_CLASS_1B_QUOTA_RESULT FALHOU rc=1` com marcador removido e `retry.done` gravado; Classe 2 stall (inbox 2h + sem result.log) → email `WATCHDOG_CLASS_2_WORKER_STALL`; cenário "worker entregou" (result.log com conteúdo) passa silencioso sem falso positivo; Classe 3 LISTENER_MISSING + Classe 1b QUOTA_SCHEDULED no mesmo iter ambos `SENT` (notifier-state.json com 2 entradas distintas, nenhum silenciado). Opt-out `CC_WATCHDOG_DISABLE=1`. Thresholds default: interval 90s, T_RESUME_MAX 5min, T_WORKER_MAX 60min. Flags persistentes `outbox/<N>-retry.done`/`outbox/<N>-stall.notified` anti-loop. Rate limit global do notifier (por tipo) deduplica alertas recorrentes mesmo após kill+restart. **Fast-follows documentados**: (1) versionar `cc-notify-email.py` em `scripts/` — hoje vive em `~/cc-mailbox/bin/` sem git, mods do PR não viajam junto; (2) smoke de recovery real (kill tmux + relaunch automático) — Classe 3 foi testada só com tmux fake. Sem chunk lock (meta-infra precedente #169/#176). Sem bump `src/version.js`. Worktree `~/projects/issue-178` removido. Issue doc arquivada em `docs/archive/`. |
| 0.37.0 | 23/04/2026 | Abertura #178 cc-watchdog.sh (meta-infra §13) | Watchdog per-worktree detecta 3 classes de stall em sessões autônomas §13: **Classe 1** resume da Coord com API error no último `coord-response.log` + sem inbox próximo → retry 1× automático via `flock + claude --resume`; **Classe 2** worker travado (inbox/N há >T_WORKER min, sem result.log) → email HUMAN_GATE sem matar; **Classe 3** tmux/listener morto → relaunch idempotente via `cc-worktree-start.sh` + email INFO. Lançado por `cc-worktree-start.sh` via `nohup ... &` com PID em `.cc-mailbox/.watchdog-pid`. Morre via trap em `cc-worktree-stop.sh` OU auto-self-kill se mailbox sumir. Reusa `.cc-mailbox/locks/coord.lock` (zero locks novos) e `cc-notify-email.py`. Thresholds default: interval 90s, T_RESUME_MAX 5min, T_WORKER_MAX 60min. Flags persistentes `.retry.done` / `.stall.notified` anti-loop. Motivação direta: #119 travou 2× (socket error 07:29 task 05, destravado manual em 46min; quota hit 12:32 task 14, destravado ~4h depois após reset 16:20). Também entregue na mesma janela mas fora do escopo #178: `~/cc-mailbox/bin/cc-status.sh` visão humana agregada multi-worktree (sem arg = dashboard; com arg = detalhe com histórico e warnings). Sem chunk lock (meta-infra, precedente #169/#176). Sem bump `src/version.js`. Branch `arch/issue-178-watchdog-sessoes-autonomas`. Baseado em v0.36.0. |
| 0.36.0 | 23/04/2026 | Abertura #119 Motor de progressão Maturidade 4D (modo autônomo) | Lock CHUNK-09 (escrita); leitura CHUNK-04/05/06/08 (todos AVAILABLE). v1.43.0 reservada em `src/version.js` + entrada CHANGELOG inline com nota `[RESERVADA — entrada definitiva no encerramento]`. Escopo declarado: todas as 6 fases (A engine puro `src/utils/maturityEngine.js` + B persistência subcollection `students/{uid}/maturity/{current\|history}` via CF `onTradeCreated`/`onTradeUpdated` + hook `useMaturity` + C UI card dedicado no StudentDashboard substituindo sparkline Maturidade do E3 da Matriz 4D + D IA `classifyMaturityProgression` Sonnet 4.6 em UP/regressão + E review.maturitySnapshot no WeeklyReviewPage + F semáforo mentor Torre de Controle). **7 decisões antecipadas fechadas com Marcio via INV-18 bloco único**: (1) `hasJournal(t)` = notes ≥10 chars OU emotionEntry preenchido; (2) Maturidade é dimensão **emergente** (f de E/F/O + gates_met_history + selfAwareness), não medida isolada; (3) Gates 1→2 com 6 gates (3 ancorados framework §5.3: maxDD<20%, compliance≥80%, emocional≥30; + 3 regras de produto: journal 50%, stop 80%, plan-linked 70%); (4) Gates 4→5 com 9 gates (emocional 85, financial 90, payoff 2.5, winRate 55% em 100+ trades, maxDD 3%, CV<0.5, zero tilt/revenge 90d, retorno anualizado 15%, Sharpe 1.5); (5) Sharpe em daytrade calculado sobre retornos diários agregados `(sum PL do dia / balance_inicio_dia)`, Sharpe anual = mean/std × √252 com piso 60 dias operados; (6) Regressão visível ao aluno no card do dashboard (stage atual + marcador vermelho + sugestão de stage "natural"), tom "espelho" transparente; (7) INV-15 aprovada — subcollection `students/{uid}/maturity/{current\|history}` com análise de custo: ~$0.60/mês em 1k alunos, ~$60/mês em 100k, gargalo real só acima de 50k. Fórmulas por dimensão (E/F/O/M) e regressão derivadas pelo Plan agent a partir do framework `trader_evolution_framework.md` + utils existentes (`calculateStats`, `calculateMaxDrawdown`, `calculatePayoff`, `calculateConsistencyCV`, `emotionalAnalysisV2.calculatePeriodScore/detectTiltV2/detectRevengeV2`, `compliance.calculateComplianceRate`) com janela rolling por stage 20/30/50/80/100 trades e política "evolução sempre visível" (confidence HIGH/MED/LOW, nunca null). Branch `feat/issue-119-maturidade-engine`. Worktree `~/projects/issue-119` a criar na próxima fase §4.0. Baseado em v0.35.0. |
| 0.35.0 | 23/04/2026 | Encerramento #176 — scripts de orquestração §13 | Entregues 3 componentes meta-infra: (1) `~/cc-mailbox/templates/coord-briefing.md` — template canônico da CC-Coord com placeholders `{{issue_num}}`, `{{issue_title}}`, `{{branch}}`, `{{worktree_path}}`, `{{control_file_path}}`. Cobre identidade, ciclo de vida "sempre morrer", wake-ups (DISPATCH_TASK, TASK_DELIVERED com fluxo result.log→report.md→`cc-validate-task.py`→decide, HUMAN_GATE_RESOLVED), resolução de ambiguidades (spec→PROJECT.md→padrão→§3.2 com DEC-AUTO-NNN-XX), tipos de gate humano §13.10 + rate limit, checklist antes de morrer. (2) `~/cc-mailbox/bin/cc-spawn-coord.sh` (~110 linhas bash) — wrapper §13.8 passo 8b: precondição dura `readlink -f pwd == $WORKTREE` abortando exit 2, localiza control file via glob, extrai `{{issue_title}}` da primeira linha, render via perl com escape-safe `\Q...\E`, invoca `claude --permission-mode auto --output-format json -p`, extrai `session_id` via `jq` (com fallback `grep`), valida formato UUID, imprime `COORD_SESSION_ID=<uuid>` parsable via cut. (3) `~/cc-mailbox/bin/cc-dispatch-task.sh` (~90 linhas bash) — wrapper §13.8 passos 8d/36: lê `.coord-id` + `.coord-dir` (INV-26 read-only), valida UUID, `flock -w 30 .cc-mailbox/locks/coord.lock` + `cd $COORD_DIR` + `claude --resume --permission-mode auto -p "DISPATCH_FIRST_TASK\|DISPATCH_TASK slug=...\|HUMAN_GATE_RESOLVED ref=..."`. Log estruturado em `.cc-mailbox/log/dispatch.log`. **Smoke integration 5/5 OK** em worktree sintético `issue-998` criado a partir do branch do #176: spawn retornou UUID `2fbe3f35-d77d-...`, start.sh com esse ID gravou `.coord-id`/`.coord-dir` READ-ONLY, kill tmux para isolar, dispatch FIRST invocou `--resume` sob flock, Coord acordou, leu control file (plano sintético pedindo `inbox/01-smoke.md` com `SMOKE_OK`), obedeceu literal, morreu. Arquivo `inbox/01-smoke.md` contém exatamente 9 bytes `SMOKE_OK` como esperado. **§13.11 atualizado** com 3 novas entradas IMPLEMENTADO e status do protocolo promovido para OPERACIONAL END-TO-END. Próxima sessão dispara "atacar #NNN em modo autônomo" e o protocolo trilha mecanicamente sem improviso do modelo no spawn/dispatch. **Fora do escopo, próximas sessões**: rodada end-to-end real (Fase D) com worker + validator + email real + recovery §13.15, `cc-notify-whatsapp.sh`. Sem chunk lock. Sem bump `src/version.js`. Worktree `~/projects/issue-176` removido. Issue doc arquivada em `docs/archive/`. |
| 0.34.0 | 23/04/2026 | Abertura #176 scripts de orquestração §13 | Fecha gaps do §13.8 passos 8b (spawn da Coord) e 8d (dispatch da primeira task) identificados no review pós-#169. Escopo: (1) `~/cc-mailbox/templates/coord-briefing.md` (template canônico com placeholders — identidade da Coord, ciclo de vida "sempre morrer", TASK_DELIVERED flow result.log→report.md→validator→decide, tratamento de STOP + email, resolução de ambiguidades spec→PROJECT.md→padrão→§3.2, tipos §13.10); (2) `cc-spawn-coord.sh` (wrapper do passo 8b com precondição cwd=worktree, render do template, `claude --output-format json -p`, captura de `session_id`); (3) `cc-dispatch-task.sh` (wrapper do passo 8d com `flock` + `--resume` da Coord). Smoke test em worktree sintético. Objetivo: próxima sessão dispara "atacar #NNN em modo autônomo" e o protocolo trilha sem improviso do modelo. **Fora do escopo**: rodada end-to-end real (Fase D separada), re-teste Recovery §13.15, `cc-notify-whatsapp.sh`. Sem chunk lock. Sem bump `src/version.js`. Branch `arch/issue-176-scripts-orquestracao-protocolo`. |
| 0.33.0 | 23/04/2026 | Encerramento #174 v1.42.1 | PR #175 mergeado (merge commit `d871fad2`). Wire definitivo de `setupsMeta` em `MentorDashboard`: util puro novo `src/utils/setupsFilter.js` com `filterSetupsForStudent(setups, studentId)` — retorna globais + pessoais do aluno indicado, isolamento estrito (setup de aluno X nunca vaza para Y), fallback `studentId` null → só globais (posição neutra). `MentorDashboard.jsx` importa `useSetups`, memoiza `filterSetupsForStudent(allSetups, selectedStudent?.studentId)` em `selectedStudentSetups`, passa ao `<SetupAnalysis setupsMeta={selectedStudentSetups}>`. Aderência RR (linha condicional do card) renderiza corretamente na visão do mentor quando aluno tem setups com `targetRR` cadastrado. 10 testes unitários do filtro (defensivo null/undefined/não-array + studentId vazio + isolamento estrito + preservação de targetRR + pureza). 1880 → 1890 testes (+10). Zero regressão. Commit chain: `2547a980` (control file) → `89c842ef` (fix + testes). **Memória operacional adicionada** (`feedback_spec_scope_respeito.md`): cortes de escopo declarados em spec NUNCA sem discutir com Marcio — "decidir sozinho" só vale para decisões cosméticas. Lock CHUNK-16 liberado (AVAILABLE). Issue doc movida para `docs/archive/`. Worktree `~/projects/issue-174` removido. |
| 0.32.0 | 23/04/2026 | Abertura #174 — fix MentorDashboard setupsMeta (E4 de #170) | Lock CHUNK-16 (escrita). v1.42.1 reservada como patch. Detectado no review pós-merge do #170 pelo Marcio: a spec de E4 dizia "Consumido em StudentDashboard e **MentorDashboard** (ambos já importam)" — CC passou wire só no StudentDashboard e rotulou MentorDashboard como "fast-follow" sem discutir. Violação registrada em memória operacional (`feedback_spec_scope_respeito.md`). Escopo mínimo: importar `useSetups` no MentorDashboard + filtrar por `s.isGlobal \|\| s.studentId === selectedStudent?.uid` (memoizado) + passar ao `<SetupAnalysis>`. ~15 linhas de código + teste garantindo que setup de outro aluno não aparece. Branch `fix/issue-174-mentor-setups-meta`. |
| 0.31.0 | 23/04/2026 | Encerramento #170 v1.42.0 SetupAnalysis V2 | PR #173 mergeado (merge commit `15a6dca3`). Substitui `SetupAnalysis.jsx` (barra proporcional + WR) por card de diagnóstico operacional com 4 KPIs em grid 2×2 (Financial EV por trade · Financial Payoff · Operational ΔT W vs L com semáforo ±20%/±10% + tempos brutos · Impact Contribuição ao EV total). **Aderência RR** sub-linha condicional — renderiza apenas quando `setups.targetRR` existe (banda [target×0.8, target×1.2]). **Sparkline 6m** (PL acumulado mensal) reusando padrão visual da Matriz 4D. **Insight 1-linha** priorizando ofensor com contribEV<-20% → best performer payoff≥1.5 → aderência RR<50% → fallback positivo. **Ordenação** por \|contribEV\| desc; setups com `n<3` em accordion "Esporádicos (N)" colapsado por default (expandido quando nenhum setup atinge n≥3). Util puro novo `src/utils/setupAnalysisV2.js` (245 linhas, zero campo Firestore novo). API externa preservada (prop `trades`) + prop opcional `setupsMeta`. Consumido em `StudentDashboard` via `useSetups` (1-linha). **MentorDashboard não alterado** — setups globais/pessoais mistos sem filtro por `selectedStudent.uid`; aderência RR omitida (condicional). Wire mentor-side → fast-follow ou issue dedicada. **Fast-fix pré-merge** (commit `0bffe1f1`): card overflow corrigido via header em 2 linhas (nome+badge, depois PL+WR), `truncate min-w-0` no nome com `title` tooltip, `shrink-0` + `whitespace-nowrap` no badge/ícones, sublabels encurtados (EV por trade→por trade; ΔT W vs L→W vs L; Contribuição ao EV→ao EV total), tempos brutos `Xm` em vez de `Xmin`, `overflow-hidden` no card container. 1880/1880 testes (+40: 23 util + 17 render) pós-rebase. **Histórico do branch**: squash merge do #172 incluiu a diff da abertura #170 (commit `3b69ea4b` local estava no histórico do branch arch/#169), resultado é que `version: 1.42.0` e lock CHUNK-02 entraram no main via #172 antes do merge do #173; rebase do branch pós-#172 droppou o commit duplicado (`git rebase --skip`). Lock CHUNK-02 liberado (AVAILABLE). Issue doc arquivada em `docs/archive/`. Worktree `~/projects/issue-170` removido. |
| 0.30.0 | 23/04/2026 | Encerramento #169 Fases A+B+C | PR #172 mergeado (squash `4afa033f`). Scripts entregues em `~/cc-mailbox/bin/`: `cc-notify-email.py` (280 linhas, email real testado vivo no iCloud do Marcio), `cc-validate-task.py` (233 linhas, 12/12 pytest em 0.92s, integration vs commit real do #170 OK e vs `deadbeef` STOP-HALLUCINATION) + test suite `test_cc_validate_task.py` (266 linhas, 12 testes). Refactor `scripts/cc-worktree-start.sh` 3-tier com pré-condição cwd=worktree (aborta exit 2), todos os 7 dirs §13.7 (antes só 3: inbox/outbox/processed; agora +coord-inbox, locks, notify-scratch, log), `.coord-id`/`.coord-dir`/`.interface-id` gravados e `chmod 444` (READ-ONLY, INV-26 + amendments v0.25.0/v0.26.0), listener usa `flock -w 30 locks/coord.lock` antes de `--resume` + log estruturado. Template `~/cc-mailbox/templates/worker-briefing.md` com cláusula CLAIMS obrigatória (§13.9 amendment v0.26.0). **Dry-run integration 5/5 OK** em worktree sintético `issue-999`: start.sh cria infra completa, validator aprova CLAIMS real, rejeita hash `deadbeef` com STOP-HALLUCINATION, rejeita skipped com `.js`, email em dry-run renderiza body completo com OPÇÕES + COMO RESPONDER. **Efeito colateral do squash merge**: diff das aberturas #169 + #170 (locais `3b69ea4b` e `315cecb9` no histórico do branch) entraram junto com o squash — resultado é `version: 1.42.0` reservada e lock CHUNK-02 para #170 já presentes no main consistentes com `#170 OPEN`. **Fora do escopo, próximas sessões**: Fase D (rodada real com issue-teste + bump §13.11), `cc-notify-whatsapp.sh` opcional, fast-follow `EMAIL_DRY_RUN=1` também grava per-worktree log, re-teste Recovery §13.15 pós-amendment. Issue doc movida para `docs/archive/`. Worktree `~/projects/issue-169` removido. Credencial SMTP reusada de `~/morning_call_auto/.env` (decisão operacional do Marcio). |

**Regra de uso:**
- Toda sessão que modificar este documento DEVE incrementar a versão e adicionar entrada na tabela acima
- Toda proposta de atualização DEVE declarar "baseado na versão X.Y.Z" para detecção de conflito
- Na abertura de sessão, comparar versão do repo com versão em mãos — se divergir, o arquivo está stale e deve ser relido


---

## COMO USAR ESTE DOCUMENTO

Este é o **overview enxuto** do projeto. Detalhes técnicos moram em arquivos temáticos:

### Índice operacional

| Tema | Arquivo | Cadência |
|------|---------|----------|
| Invariantes (INV-01..28) | `docs/invariants.md` | read-static |
| Anti-patterns (AP-01..12) | `docs/anti-patterns.md` | read-static |
| Decision Log (DEC-001..090+) | `docs/decisions.md` | append-only |
| Dívidas Técnicas (DT-001..039) | `docs/tech-debt.md` | write-hot curto |
| Milestones e Roadmap | `docs/milestones.md` | write-medium |
| Cloud Functions | `docs/cloud-functions.md` | read-cold |
| Firestore Schema (Collections) | `docs/firestore-schema.md` | read-cold |
| Chunks (domínios CHUNK-01..17) | `docs/chunks.md` | read-static |
| Protocolo §4.0 / §4.1 / §4.2 (Abertura + Gates) | `docs/protocols/opening.md` | read-static |
| Protocolo §4.3 / §4.4 (Encerramento + Verificação) | `docs/protocols/closing.md` | read-static |
| Protocolo §13 (Modo Autônomo coord/worker) | `docs/protocols/autonomous.md` | read-static |
| Protocolo Sessões Paralelas | `docs/protocols/parallel.md` | read-static |
| Registry de locks ativos | `docs/registry/chunks.md` | write-hot (isolado) |
| Registry de versões reservadas | `docs/registry/versions.md` | write-hot (isolado) |
| CHANGELOG (v0.1.0..v1.43.0) | `/CHANGELOG.md` (raiz) | write-hot |
| Template de issue control | `docs/templates/issue-control.md` | read-static |
| Template de worker report (§13) | `docs/templates/worker-report.md` | read-static |

### Regras de atualização

- Mudança em invariantes / decisões / chunks / protocolo → bump MINOR no header do arquivo temático próprio + entrada em histórico.
- `PROJECT.md` bumpa apenas quando a **estrutura do índice** muda ou quando seções inline (§1 Stack, §5 Checklist, §11 Mapa Deps, §12 Convenções) são editadas.
- Rastreabilidade: toda DEC/INV/AP/DT/CHUNK/CF nova tem ID sequencial + issue de origem + data.
- Nunca remover entradas antigas — marcar como `SUPERSEDED` se inválida.

### O que NÃO vive em arquivo nenhum permanente
- Especificação por issue → `docs/dev/issues/issue-NNN-*.md` (deletado no encerramento via `scripts/cc-close-issue.sh` — GitHub é SSoT do detalhe; refactor #199 Fase 2.4)
- Deploy / install / migration → `docs/ops/`
- Arquivos de referência citáveis (specs pré-aprovação, SPEC/IMPACT) → `docs/reference/`

---

## 1. STACK & INFRAESTRUTURA

| Camada | Tecnologia | Notas |
|--------|-----------|-------|
| Frontend | React 18 + Vite | SPA, glassmorphism dark theme |
| Styling | Tailwind CSS | Utility-first |
| Backend | Firebase (Firestore, Cloud Functions, Auth, Storage) | Serverless |
| Deploy | Vercel | Frontend only; Cloud Functions via Firebase CLI |
| Testes | Vitest + jsdom | Cobertura obrigatória em business logic |
| Versionamento | Git + GitHub | Issues numeradas, branches `feature/issue-NNN-descricao` |

### Ferramentas do ambiente de desenvolvimento

| Ferramenta | Versão | Uso |
|-----------|--------|-----|
| Node.js | 22.x (migrado de 20 — DT-016 resolvido v1.22.0) | Runtime local + Cloud Functions |
| Firebase CLI | latest | Deploy de CFs e Firestore rules |
| GitHub CLI (`gh`) | 2.86.0 | Gestão de issues, PRs e milestones via script |
| bash | Linux | Shell padrão — commits em linha única obrigatório |
| Obsidian | latest | Leitura e edição de `.md` — abrir repo como vault |
| Vite | 4.x | Dev server + build |

**Convenções bash — obrigatórias:**

1. **Commit messages** — sempre em linha única (`git commit -m "mensagem"`)
2. **ZIPs** — `unzip -o <arquivo>.zip` na raiz do projeto (substitui `Expand-Archive`)
3. **Scripts** — ASCII-only em strings passadas ao `gh` CLI (acentos podem causar encoding issues)

**GitHub CLI — comandos frequentes:**
```bash
gh issue list --state open          # listar issues abertos
gh issue create --title "..." --body "..." --label "type:feat"
gh issue edit NNN --title "..." --add-label "milestone:v1.1.0"
gh issue close NNN
gh pr create --title "..." --body "..."
```

---


## 5. CHECKLIST DE IMPACTO PARA NOVAS FEATURES

Antes de propor qualquer feature, executar mentalmente:

1. Quais collections são tocadas? (leitura E escrita)
2. Quais Cloud Functions disparam? (triggers onCreate/onUpdate)
3. Quais hooks/listeners são afetados? (re-renders, queries)
4. Há side-effects em PL, compliance, emotional scoring?
5. Dados parciais/inválidos podem entrar no caminho crítico?
6. A feature respeita todas as INV-01 a INV-18?
7. Qual o blast radius se algo der errado?
8. Existe rollback viável?
9. Quais testes existentes podem quebrar?
10. DebugBadge está presente em todos os componentes novos/tocados?

---


## 11. MAPA DE DEPENDÊNCIAS

### Collections Firestore e consumidores

```
trades (collection principal)
├── Escritor: addTrade — GATEWAY ÚNICO (INV-02)
├── CFs: onTradeCreated, onTradeUpdated
├── Campo _partials: array INLINE no documento (INV-12) — NÃO subcollection
└── Consumers: StudentDashboard, TradingCalendar, AccountStatement, FeedbackPage,
               PlanLedgerExtract, MentorDashboard

plans → cycles, currentCycle, state machine (IN_PROGRESS→GOAL_HIT/STOP_HIT→POST_GOAL/POST_STOP)
  └── pl é IMUTÁVEL pós-criação (C1 #259); saldo derivado on-the-fly (C2);
      lastClosedCycleEnd + sealedCycleRanges + lastCycleClosureId como cache do ritual
accounts → currency, balance, broker
emotions → scoring -4..+3 normalizado 0-100, TILT/REVENGE detection
csvStagingTrades → staging CSV, nunca dispara CFs diretamente
orders → staging de ordens brutas (CHUNK-10)
cycleClosures → doc imutável do ritual de fechamento (CHUNK-04/16, #259)
  └── ID determinístico {planId}_{cycleKey}; schemaVersion=3 traz cycleBaseline
      (plInicial/saldoFinal/plFinal) + preClosePlanSnapshot (foto pré-close pra
      reabertura) + behavioralSummary (critical, denialFlag) + snapshot/metrics/
      patterns/aar/maturity/swot/mentor/forward
students/{id}/assessment/ → questionnaire, probing, initial_assessment (CHUNK-09)
students/{id}/subscriptions → type, status, accessTier, payments subcollection (DEC-055/056)
  └── payments → amount, date, proof, plan vigente no momento
```

### Cloud Functions

| Function | Trigger | Responsabilidade |
|----------|---------|-----------------|
| `onTradeCreated` | trades create | Atualiza PL do plano, compliance stats |
| `onTradeUpdated` | trades update | Recalcula PL, compliance |
| `classifyOpenResponse` | callable | Classifica respostas abertas via API Claude |
| `generateProbingQuestions` | callable | Gera 3-5 perguntas de sondagem adaptativa |
| `analyzeProbingResponse` | callable | Analisa respostas do probing |
| `generateAssessmentReport` | callable | Gera relatório completo pré-mentor |
| `checkSubscriptions` | onSchedule (8h BRT) | Detecta vencimentos, marca overdue, expira trials, sincroniza accessTier, envia email |
| `closeCycle` | callable | Cria `cycleClosures` doc imutável (10 seções A-J) + atualiza plan (pl, lastClosedCycleEnd, sealedCycleRanges, currentCycleNumber+1). Gate de equity do ciclo + permissão aluno/mentor. C3 #259 |
| `reopenCycle` | callable | Deleta closure doc + restaura plano via `preClosePlanSnapshot`. Gate de cadeia: só último closure (`plan.lastCycleClosureId`). C4 #259 |
| `setMentorClosureComment` | callable | Mentor adiciona `closure.mentor.closingComment` (janela 7d). #259 |
| `deleteAccountCascade` | callable | Apaga conta em 7 estágios: movements → trades → orders → cycleClosures → plans → account. #259 B12.5 |
| `deletePlanCascade` | callable | Apaga plano + trades/orders/movements vinculados + closures. #259 B12.5 |

---


## 12. CONVENÇÕES DE DESENVOLVIMENTO

### Branches e commits
```
feature/issue-NNN-descricao   ← nova feature ou refactor
fix/issue-NNN-descricao       ← bug fix
debt/issue-NNN-descricao      ← dívida técnica
arch/issue-NNN-descricao      ← mudança arquitetural
```

Commit messages em linha única (bash):
```
feat: descrição da feature (issue #NNN)
fix: descrição do fix (issue #NNN)
debt: descrição da dívida resolvida (issue #NNN)
docs: atualizar PROJECT.md sessão DD/MM/YYYY
```

### Classificação de issues (prefixo no título)
```
feat:   nova funcionalidade
fix:    correção de bug
debt:   dívida técnica / tech debt
arch:   decisão arquitetural / refactor estrutural
ops:    infra, deploy, Cloud Functions, Node.js
epic:   agrupa outros issues (não implementável diretamente)
```

### Testes
- Framework: Vitest + jsdom
- Localização: `src/__tests__/utils/` para novos utils
- Padrão: bug fix → reproduzir bug em teste → corrigir → teste passa
- Nunca regressão — testes existentes devem continuar passando

### UI
- Theme: Glassmorphism dark
- DebugBadge: obrigatório em tudo, com `component="NomeExato"`
- Datas: DD/MM/YYYY sempre
- Semana: começa na segunda-feira

---


---

*Overview enxuto mantido a partir da consolidação documental #181 (v0.39.0).*
*Arquivos temáticos em `docs/*.md`, `docs/protocols/`, `docs/registry/`, `docs/templates/`.*
