# ============================================
# CI — Testes Automatizados
# Issue #22 — Acompanhamento 2.0
# ============================================
# 
# Roda em todo PR para main e push direto em main.
# Gate de qualidade: PR com teste falhando → merge bloqueado.
#
# Configurar branch protection manualmente:
#   Settings → Branches → Add rule → main
#   ✅ Require status checks: "test"
#   ✅ Require branches to be up to date

name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    name: Unit & Integration Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test:run

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          retention-days: 7

  # ============================================
  # Fase 3: Descomentar para E2E
  # ============================================
  # e2e:
  #   name: E2E Smoke Tests
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npx playwright install --with-deps chromium
  #     - run: npm run build
  #     - name: Run E2E tests
  #       run: npm run test:e2e -- --project=chromium
  #     - name: Upload E2E report
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: playwright-report
  #         path: playwright-report/
  #         retention-days: 7
